# 인계문 4 — 세션 38부터

**이 파일이 현재 인계문이다.** 구 인계문 전체를 읽지 않는다.

- [`doc/handoff3.md`](./handoff3.md) — 세션 34~37 (결과 화면 · iOS 코드 제거 · learn 메인 · flashcards · 학습 카드 안 치수)
- [`doc/handoff2.md`](./handoff2.md) — 세션 28~33 (색 토큰화 · 오버레이 인셋 · 드럼 레이블/바닥선 · 드럼 햅틱)
- [`doc/handoff1.md`](./handoff1.md) — 세션 20~27 (오디오 무음 해결 · 악기 화면 디자인 통일)
- [`doc/handoff.md`](./handoff.md) — 세션 1~9 아카이브 (사운드 초기 작업)
- [`doc/handoff_3.md`](./handoff_3.md) — `3-branch`에서 가져온 기록. **이 브랜치의 인계문이 아니다**

현재 브랜치 **`main`**. 세션 38 시작 시점의 HEAD는 `21b71f3`(세션 37 문서 커밋)이고
**작업 트리는 깨끗했다.** origin/`main`보다 앞서 있다. **푸시는 사용자가 한다.**
(로컬 ref 기준이니 정확한 수는 `git fetch` 후에 세야 한다.)

**세션 38 현재**: flashcards 스와이프 제스처 제거 + 진행바 재구성이 **커밋됐고**(`d38df37`),
**Note10+ 실기기 확인도 끝났다 — 사용자 확인 「정상 동작」**이다.
**열린 코드 항목은 없다.** 남은 것은 작은 폰·태블릿 확인(기기 없음, 보류)과 푸시뿐이다.

---

## 화면 부위를 부르는 이름 (세션 38에 사용자가 정했다)

**대화에서는 이 이름을 쓴다.** 괄호가 코드 이름이다.

### flashcards 화면 (`app/(tabs)/flashcards/index.tsx`)

```
┌─────────────────────────────────────┐
│              배경                    │  class_R.webp
│  [        제목        ] [익힘배지]   │  헤더
│  ──●────────────  1 / N             │  진행바 + 진행숫자
│     ┌─────────────────────┐         │
│     │      카드박스        │         │  ← topCard / topCardBackground
│     └─────────────────────┘         │
│     [◀]   [학습완료]   [▶]          │  카드네비 (앱 탭바와 다름)
├─────────────────────────────────────┤
│         앱 하단 탭바                 │  app/(tabs)/_layout.tsx
└─────────────────────────────────────┘
```

| 이름 | 화면에 보이는 것 | 코드 |
|---|---|---|
| **제목** | `📖 단어 카 드` | `headerTitlePill` |
| **익힘배지** | `✅ N개 익힘` | `completedBadge` (배경은 SVG `CompletedBadgeBg`) |
| **진행바** | 가로줄 + 눈금 + 마커 | `progressContainer` |
| **진행숫자** | `1 / N` | `progressTextPill` |
| **카드박스** | 카드를 감싸는 사각형 | 크기·위치는 `topCard`, 배경·테두리는 `topCardBackground` |
| **카드박스 오버레이** | 사진 위 베이지 반투명 판 | `cardOverlay` |
| **카드네비** | ◀ 학습완료 ▶ | `bottomNavigationContainer` |
| **앱 탭바** | 맨 아래 탭 아이콘 | `_layout.tsx` Tabs |
| **익힘모달** | 익힘배지를 누르면 나오는 **완료 카드 목록** | `showCompletedModal` → `modalOverlay` > `modalContent` > `modalHeader`·`modalBody` |
| **완료화면** | 카드를 다 익혔을 때 (카드박스 **안**에 들어간다) | `completionContainer` |

### 카드박스 안 (`components/game/WordFlashcard.tsx`)

```
┌──────────── 카드박스 ────────────┐
│   ┌──────┐   VS   ┌──────┐       │  단어1 / 단어2
│   │단어1 │        │단어2 │       │
│   │ 🔊   │        │ 🔊   │       │  단어1 스피커 / 단어2 스피커
│   └──────┘        └──────┘       │
│         ∿∿  파형  ∿∿             │  파형 (Skia)
│        [ 전체 듣기 ]              │  전체듣기
└──────────────────────────────────┘
```

| 이름 | 코드 |
|---|---|
| **단어1 / 단어2** | `wordCard` ×2 |
| **단어1 글자 / 단어2 글자** | `wordText` |
| **단어1 스피커 / 단어2 스피커** | `playButton` |
| **VS** | `vsContainer` |
| **파형** | `Waveform` (Skia) |
| **전체듣기** | `playAllButton` |

### 헷갈리기 쉬운 것 — **익힘모달 ≠ 미션모달**

`modalContent`는 **판 스타일 이름이지 화면이 아니다.** 두 파일에 같은 키가 있고
**StyleSheet가 파일마다 따로라 이름만 같다.** 한쪽을 고쳐도 다른 쪽은 그대로다.

| | **익힘모달** | **미션모달** |
|---|---|---|
| 파일 | `app/(tabs)/flashcards/index.tsx` | **`components/MissionProgressIcon.tsx`** (`components/game/`가 아니다) |
| 여는 것 | 익힘배지 | 미션아이콘 (게임·악기 화면 우상단) |
| 스위치 | `showCompletedModal` | `modalVisible` |
| 모양 | 아래서 올라오는 노란 시트 (`backgroundStar` `#FFF9E6`) | 가운데 흰 박스 |

> **「`modalContent`를 고쳐 달라」는 말은 어느 화면인지 알 수 없다.**
> 화면을 말할 때는 **익힘모달 / 미션모달**, 판만 가리킬 때 `modalContent`.

---

## 진행 현황

세션 20~27에서 닫힌 항목은 [`handoff1.md`](./handoff1.md) 표에,
세션 28~33은 [`handoff2.md`](./handoff2.md), 세션 34~37은 [`handoff3.md`](./handoff3.md) 표에 있다.
여기에는 **아직 열려 있는 것만** 옮겨 적고, 세션 38부터 새로 생기는 항목을 쌓는다.

| 항목 | 상태 |
|---|---|
| 🗑️ flashcards 스와이프 제스처 제거 | ✅ **완료 · 커밋 `d38df37` · 실기기 확인** (세션 38) |
| 📊 진행바 — 눈금을 카드 수에 맞추고 마커·눈금·채움을 한 식으로 | ✅ **완료 · 커밋 `d38df37` · 실기기 확인** (세션 38) |
| 🔊 전체듣기 중 파형 · 재생 중 단어카드 강조 | ✅ **완료 · 커밋 `d38df37` · 실기기 확인** (세션 38) |
| 📐 작은 폰(320~360dp) · 태블릿 확인 | ⏸️ **보류 — 기기 없음** (사용자 판단, 세션 36 · 항목이 세션 38에서 늘었다) |
| ⚠️ `Source error` 근본 원인 | ❌ 미해결 · **릴리스 빌드 필요라 보류** (사용자 판단, 세션 21) |
| 🧪 테스트 스위치 세 개(정답 힌트 · 초기화 버튼 2종) | ⛔ **지우지 않는다** (사용자 결정, 세션 29) |
| 🥁 드럼 탭도 같은 골격으로? | ⛔ **안 한다** (사용자 결정, 세션 27) |
| 📚 flashcards 탭 UI 개선 | ✅ 완료 (세션 36~37, [`handoff3.md`](./handoff3.md)) |
| 🎧 learn 메인 화면 디자인 개선 | ✅ 완료 (세션 35~36, [`handoff3.md`](./handoff3.md)) |

---

## 세션 38 작업 로그 (2026-09-03)

「이어서」로 시작했다. 열린 코드 항목은 없었고 인계문과 저장소가 일치했다.
그 뒤 **사용자가 화면 부위를 부르는 이름을 정해 주고**, flashcards UI 개선을 지시했다.

| 커밋 | 내용 |
|---|---|
| `d38df37` | **코드** 3파일 · 279+ / 310− — 제스처 제거 · 진행바 재구성 · 전체듣기 중 파형 |
| (이 문서) | **문서** — 인계문을 `handoff4.md`로 전환 + `doc/conversation_38.md` |

### 0. 인계문을 handoff4로 옮겼다

사용자 지시다. `handoff3.md`는 **세션 34~37 아카이브**로 머리만 바꾸고 본문은 그대로 뒀다
(세션 34가 handoff2를 넘길 때와 같은 방식이다).
`CLAUDE.md`의 규칙 2-2 / 2-3 / 2-4가 가리키는 "현재 인계문"도 함께 `handoff4.md`로 바꿨다 —
**이걸 안 바꾸면 다음 세션이 handoff3을 읽고 이 파일을 못 본다.**

주의사항은 **복사하지 않고 handoff3·handoff2·handoff1을 가리키게** 했다.

### 1. 이름부터 맞췄다 — 그리고 내가 한 번 틀렸다

사용자가 화면 부위 이름표를 줬다(위 「화면 부위를 부르는 이름」). 코드와 대조해 12개가
전부 실제 스타일 키와 맞는 것을 확인하고, 표에 없던 넷(**VS** · **단어 글자** ·
**단어 스피커** · **카드박스 오버레이**)에 이름을 더했다.

그런데 내가 표를 옮겨 적으며 **「익힘모달 = `modalContent`」**라고 썼고, 사용자가 정정했다.
`modalContent`는 **판 스타일 이름**이고, `components/MissionProgressIcon.tsx`에도
**같은 키가 따로 있다.** 파일마다 StyleSheet가 따로라 이름만 같은 것인데,
그대로 두면 「`modalContent` 고쳐 줘」가 어느 화면인지 알 수 없다.

> **스타일 키 이름을 화면 이름으로 쓰지 말 것.** 위 「익힘모달 ≠ 미션모달」 표가 이 실수의 결과다.

### 2. 내가 또 방향을 잘못 말했다 — 진행바 마커

「마커가 90%까지만 움직여 끝에 닿지 않는다」고 보고했는데 **틀렸다.**
레일이 래퍼 폭의 **90%**라 `progress × 90%`는 **레일 길이와 정확히 같다.** 마커는 전 구간을 돈다.

실제 문제는 **기준이 셋이었던 것**이다:

| | 무엇을 기준으로 삼았나 |
|---|---|
| 레일 (`progressLine`) | 폭 90% + 가운데 정렬 → 래퍼 기준 **5%~95%** |
| 마커 | `left: 비율 × 90%` + `marginLeft: -1` → 중심이 늘 **4px 왼쪽** |
| 눈금 | `space-between` 컨테이너 → **또 다른 기준** |

셋을 **`시작 5% + 비율 × 90%` 한 식**으로 묶었다(`railPercent()`). 마커의 `marginLeft`도
`-1`에서 **`-마커폭/2`**로 바꿨다 — 「중심이 그 지점을 가리킨다」는 뜻이 값에 드러나야 한다.

> **`app.json`/`_layout.tsx` 때(세션 34)와 같은 종류의 실수다.**
> 값 하나(`Math.min(90, …)`)만 보고 「잘린다」고 단정했다. 그 90이 **레일 폭과 같은 수**라는 것을
> 같이 보지 않았다. 퍼센트는 **무엇의 퍼센트인지**까지 봐야 한다.

### 3. 눈금이 카드 수를 따라간다 (`d38df37`)

`[0, 1, 2, 3, 4, 5]` **하드코딩**이라 카드가 20장이든 3장이든 늘 6개였다.
**눈금 하나 = 카드 한 장**으로 바꾸고, 레일에 **지나온 만큼 채움**을 깔았다.

- 채움 `progressLineFill` — 초록(`successOnWhite`). 눈금만으로는 어디까지 왔는지 읽기 어렵다
- **지나온 눈금은 흰색으로 뒤집는다.** 초록 채움 위에 초록 눈금을 두면 묻는다
- 조건은 `i < currentIndex`다. `i <= currentIndex`로 하면 **첫 카드에서 채움이 0인데
  눈금이 흰색**이 되어 흰 레일 위에서 사라진다. 현재 위치의 눈금은 마커가 덮는다
- 크기는 `getProgressTickSize(카드수)` — 레일을 카드 수로 나눈 한 칸의 절반을 넘지 않게 줄이되 하한 4

| 기기 | 레일 폭 | 20장일 때 눈금 |
|---|---|---|
| **411dp (실기기)** | 343 | **10 — 종전과 같다** (계산값 9.93) |
| 320dp | 261 | 8 (15장을 넘길 때부터 줄어든다) |

**곁들여 나온 것 — 마커 아이콘이 잘리고 있었다.** 상자가 32px인데 아이콘이 36px이고
`overflow: 'hidden'`이라 **좌우 2px씩 깎여 있었다.** `overflow`만 뺐다.

### 4. 전체듣기 중에 파형이 없었다 (같은 커밋)

조건이 `showWaveform && !isPlayingAll`이라, **소리는 이어서 나는데 파형 자리만 비어 있었다**
(자리는 `waveformContainer`가 늘 차지한다). `!isPlayingAll`을 뺐다.

**어느 단어가 울리는지도 표시가 없었다.** 전체듣기는 단어1 → 단어2로 혼자 넘어가는데
두 장을 똑같이 흐리게만 했다. 지금은 울리는 쪽을 강조하고 **반대쪽만** 흐리게 한다.

- `wordCardPlaying` — 초록 테두리 + 연한 초록 배경(`backgroundSuccess`) + `elevation: 8`
- **테두리는 늘 자리를 차지한다** (`borderWidth: 2` + `borderColor: 'transparent'`).
  재생 중에만 색을 넣으므로 **카드 크기가 흔들리지 않는다**
- 스피커 아이콘: 쉴 때 `volume-medium-outline`, 울릴 때 `volume-high` + `successOnWhite`
- 접근성 속성(`Role` · `Label` · `State({ disabled, busy })`)이 **하나도 없었다**

Yoga는 `minWidth`가 **border-box**라, 단어가 2자 이하(데이터 최대 '고기'·'머리')면
`64 + 패딩 40 + 테두리 4 = 108 < 120`이라 **폭은 그대로**다. 높이만 4px 는다.

### 5. 완료화면에서 `1 / 0`이 떴다 (같은 커밋)

남은 카드가 0인데 진행숫자가 `{currentIndex + 1} / {cardCount}`를 그대로 그렸다.
**진행바·진행숫자를 통째로 숨긴다** — 가리킬 진행이 없다.

**학습완료 버튼도 `disabled`로 막았다.** `handleCompleteCard`가 맨 앞에서 `return`하므로
아무 일도 안 하는데 **눌리는 시각 반응은 그대로 났다** (세션 36에서 화살표에 한 것과 같은 처리).
글자색도 `textSecondary`로 내렸다 — 회색 바탕 위 흰 글자는 **1.5:1**이라 읽히지 않는다.

### 6. 제스처를 지웠다 (같은 커밋)

**사용자가 지울 것과 남길 것을 직접 나눠 줬다.**

| 지운 것 | 남긴 것 |
|---|---|
| `PanGestureHandler` (`enabled={false}` 포함) | `panX` · `panY` · `scale` · `opacity` · `rotation` |
| `handleGestureEvent` · `handleSwipe` | `animateSwipe` · `resetAnimation` · `handleCompleteCard` |
| `undoStackRef` (스와이프 전용) | `GestureHandlerRootView` |
| `LAYOUT.dropZoneThreshold` · `dropZoneThresholdRatio` | |
| `animateSwipe`의 `'down'` 분기 (타입도 `'left' \| 'right'`로) | |

`'down'`은 **아래로 떨어뜨려 학습완료하던 스와이프 전용**이었다. 지금 학습완료는
`handleCompleteCard`의 「익힘배지로 날아가기」가 따로 하고, 그쪽은 `Animated.parallel`을
직접 쓰므로 겹치지 않는다. **`panY`는 그 날아가기(`:211`)가 쓰므로 남는다.**

> `'down'`을 지울지는 **내가 물었고 사용자가 결정했다.** 지시에 「남길 것: `animateSwipe`」가
> 있어서 내 판단으로 안쪽 분기를 지우지 않고 물은 것이다.

**곁들여 죽은 상태 셋을 지웠다** — `completionMessage`·`setCompletionMessage`는 읽는 곳이 없고,
`completionTimeoutRef`는 **설정하는 곳 없이 정리만 하는 `useEffect`**를 달고 있었다.

`npx tsc --noEmit` 새 오류 0건. 남은 `hooks/useSyncGameData.ts(44,51)`은 세션 29에서
무관한 기존 항목으로 확인된 것이다.

### 7. 실기기 확인 결과 — 사용자 확인

Note10+(411×868)에서 **「정상 동작」**이다. 진행바(눈금 20개 + 채움 + 마커) ·
전체듣기 중 파형과 단어카드 강조 · 완료화면 · ◀▶ 넘김 연출까지 포함한다.

---

## 주의사항 / 확인된 의도 동작

> **세션 20~37의 주의사항은 전부 유효하다.** 여기 옮겨 적지 않았으니
> 해당 코드를 만지기 전에 원문을 볼 것.
>
> - [`handoff3.md`](./handoff3.md) 「주의사항 / 확인된 의도 동작」 — 세션 34~37
>   (게이지 실측 · `RESULT_TIERS` · 등급 색 · `elevation`만 · learn 언마운트 · 가로 대응 금지 ·
>   `audioPlayer.progress` · 난이도 버튼 역산 · `successOnWhite` · `adb` ·
>   `WordGame.tsx` `* 0.4` · flashcards 하단 네비 인셋 · 카드 밀어내기 · `surfaceOnImage` ·
>   「전체 듣기」 위치 · `scaleInCard*` · 파형 폭)
> - [`handoff2.md`](./handoff2.md) 「주의사항」 — 세션 28~33
> - [`handoff1.md`](./handoff1.md) 「주의사항」 — 세션 20~27

세션 38부터 생기는 주의사항은 이 아래에 쌓는다.

### 되돌리면 안 되는 것 (세션 38)

- **`animateSwipe`에 `'down'`을 되살리지 말 것.** 스와이프 학습완료 전용이었고 제스처는 없다.
  학습완료 연출은 `handleCompleteCard`가 `Animated.parallel`로 직접 만든다.
  **`panY`는 그 날아가기가 쓰므로 죽은 코드가 아니다** — 지우면 카드가 세로로 안 움직인다.
- **스와이프 경로를 다시 넣지 말 것.** `enabled={false}`로 꺼둔 채 남겨 두면
  세션 36처럼 「죽은 코드인가 아닌가」를 또 판단해야 한다. 카드는 **카드네비로만** 넘긴다.
- **진행바의 마커·눈금·채움을 각자 다른 기준으로 되돌리지 말 것.**
  레일은 래퍼 폭의 90%가 가운데 정렬이라 **5%에서 시작한다.** 셋 다 `railPercent()`
  (`시작 + 비율 × 폭`) 하나를 쓴다. 마커 `marginLeft`는 **`-마커폭/2`**여야
  「중심이 그 지점을 가리킨다」가 된다 (전에는 `-1`이라 4px 왼쪽이었다).
- **눈금 개수를 고정 숫자로 되돌리지 말 것.** `[0,1,2,3,4,5]`는 카드가 20장이어도 6개였다.
  **눈금 하나 = 카드 한 장**이고, 크기는 `getProgressTickSize()`가 레일 폭에서 정한다.
- **지나온 눈금 조건을 `i <= currentIndex`로 바꾸지 말 것.** 첫 카드에서 채움이 0인데
  눈금만 흰색이 되어 흰 레일 위에서 사라진다. `i < currentIndex`가 맞다.
- **마커 상자에 `overflow: 'hidden'`을 다시 넣지 말 것.** 상자 32px < 아이콘 36px이라
  좌우 2px씩 잘린다.
- **파형을 `!isPlayingAll`로 다시 막지 말 것.** 소리가 나는 동안 자리만 비어 보인다.
- **`wordCard`의 `borderWidth: 2` + `borderColor: 'transparent'`를 지우지 말 것.**
  테두리가 늘 자리를 차지해야 재생 강조가 켜질 때 **카드 크기가 흔들리지 않는다.**
  (Yoga의 `minWidth`는 border-box라 2자 단어에서는 폭도 그대로다)
- **완료화면에서 진행바·진행숫자를 다시 그리지 말 것.** `1 / 0`이 뜬다.
  학습완료 버튼의 `disabled`도 같다 — 누르면 시각 반응만 나고 아무 일도 안 한다.
- **`modalContent`를 화면 이름으로 쓰지 말 것.** 익힘모달과 미션모달에 같은 키가
  **따로** 있다 (위 「익힘모달 ≠ 미션모달」).

---

## 다음 작업

> **코드로 열려 있는 항목은 없다.** 1번은 사용자 몫, 2번은 기기가 없어 보류, 3번은 릴리스 빌드 대기다.

### 1. 푸시

`main`이 `origin/main`보다 앞서 있다. 정확한 수는 아래 명령으로 센다
(숫자를 적어두면 커밋할 때마다 어긋난다).

```
git fetch && git rev-list --count origin/main..main
```
**푸시는 사용자가 직접 한다.**

> **세션 35까지의 커밋과 `c2cb6eb`는 사용자가 푸시했다.** 그 뒤로
> `506983d` · `b81df21` · `8513f1b` · `d79cf49` · `98775e6` · `21b71f3` ·
> **`d38df37`(세션 38)** 와 문서 커밋들이 쌓여 있다.

### 2. ⏸️ 작은 폰 · 태블릿 확인 — **보류 (기기 없음)**

> **사용자 판단이다** (세션 36). 지금 있는 기기는 Note10+(411×868) 하나뿐이고,
> **이 폭에서는 반응형 작업이 기존값 그대로**라 아무것도 검증되지 않는다.

기기가 생기면 볼 것. 근거 수치는 [`handoff3.md`](./handoff3.md)의 세션 35 로그 2번 ·
세션 36 로그 4번 · 세션 37 로그 4번 표에 있다.

| 기기 | 볼 것 (세션 35~37에서 쌓인 것) |
|---|---|
| **320×640** | learn 난이도 버튼 107(버튼줄 229 / 폭 230 — **여유 1px**) · flashcards 카드 밀어내기 162 · 카드 안 단어카드 92(버튼줄 228 / 안쪽 폭 228 — **여유 0**) |
| **360×740** | 버튼 127 · 버튼줄 269/270 · 카드 안 단어카드 105 |
| **태블릿(600~800)** | 난이도 버튼 180 · 진행바 위치 상한 190 · 카드 안 단어카드 180, 남는 세로 111을 단어 블록과 버튼이 **나눠 갖는지** |

세션 38에서 볼 것이 늘었다:

| 기기 | 볼 것 |
|---|---|
| **320dp** | 진행바 눈금이 **8px로 줄어든다**(레일 261). 20장이 서로 붙지 않는지 |
| **작은 폰 전반** | 단어카드가 재생 강조로 **높이 +4px** 된다. 320×640은 세로 여유가 16이었다 |
| **태블릿** | 눈금 12 · 마커 44. 마커 `marginLeft`가 -22로 바뀌었다 |

### 3. `Source error` 근본 원인 — **보류**

> **개발(디버그) 빌드에서는 확인할 수 없어 미룬 항목이다** (사용자 판단, 세션 21).

개발 서버 HTTP 로드 실패로 **추정**만 된 상태. 다음 단서는 **릴리스 빌드에서도 이 경고가 뜨는지**다.
`doc/audio-무음-원인과-방향.md` 13-2에 있다. 무음(출력 층)과 **다른 층(입력 층)**이라
`adb logcat` 명령이 다르다.

---

## 브라우저용 페이지

만드는 법·고치는 법·함정은 [`handoff2.md`](./handoff2.md) 「브라우저용 페이지」에 그대로 있다.
**고칠 때는 반드시 그 표의 주소를 함께 줄 것** — 주소 없이 부탁하면 새 페이지가 하나 더 생긴다.

| 페이지 | 원문 | 주소 |
|---|---|---|
| 실측 레이아웃 노트 | `doc/실측-레이아웃-onLayout.md` | `https://claude.ai/code/artifact/e995e6f2-5edd-43ca-9059-bc7d343ffe62` |
