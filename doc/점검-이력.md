# 점검 이력 — 어느 화면을 언제 훑었나

[`점검표.md`](./점검표.md)로 화면을 훑을 때마다 여기를 손본다.
**목적은 「무엇을 고쳤나」가 아니라 「무엇을 아직 안 봤나」다.**

이 표는 **앱의 UI 파일 전부**(44개 · **14,728줄**)를 담는다. 손으로 고른 목록이 아니다 —
빠진 파일은 「안 봤다」로도 안 보이기 때문이다.

## 이 문서는 절반이 갱신, 절반이 누적이다

**섞어 쓰는 게 아니라 나눠 쓰는 것이다.** 어느 절을 손보는지 먼저 본다.

| 절 | 방식 | 왜 |
|---|---|---|
| **화면별** · **한눈에** · **다음 점검 후보** | **갱신** (덮어쓴다) | 「**지금** 무엇을 안 봤나」를 답한다. 누적하면 `flashcards` 줄이 세션마다 늘어나 **안 본 것이 파묻힌다** — 이 문서를 만든 이유가 사라진다 |
| **세션 노트** (아래쪽) | **누적** (아래로 쌓는다) | 「그때 무엇이 나왔나」를 답한다. 덮어쓰면 **같은 파일에서 두 번 다른 것이 나왔다**는 사실이 사라진다 |

> 갱신되는 표에서 사라진 이력은 **세션 노트가 받는다.** 근거 원문은 **그때의 인계문**에 있다
> (세션 64~ `handoff13.md` · 62~63 `handoff12.md` · 60~61 `handoff11.md` · 55~59 `handoff10.md` · 52~54 `handoff9.md` · 51 `handoff8.md` · 49~50 `handoff7.md` · 47~48 `handoff6.md` · 43~46 `handoff5.md` ·
> 38~42 `handoff4.md` · 34~37 `handoff3.md`).
> 세 문서가 각각 **지금 상태 · 그때 결과 · 왜 그렇게 했나**를 나눠 갖는다.

## 읽는 법

| 칸 | 뜻 |
|---|---|
| **범위** | 「전수」 = 점검표 일곱 갈래를 다 돌림 · 「부분」 = 그 세션에 만진 자리만 · **빈칸 = 한 번도 안 봤다** |
| **점검 커밋** | 그 점검이 **본 상태**. 이게 있어야 「그 뒤 바뀌었나」를 기계로 판정한다 |
| **터치/`Role`** | 터치 요소 수 / `accessibilityRole`이 붙은 수 (점검표 3절) |

**`?`는 「모른다」다.** 세션 40 이전 점검은 커밋을 남기지 않았다 —
바뀌었는지 알 수 없으므로 **다시 훑어야 하는 것으로 친다.**

## 다시 훑어야 하나 — 판정하는 법

```
git diff --name-only <점검 커밋> HEAD -- <파일>     # 비면 그 뒤로 안 바뀜 = 건너뛴다
git status --short -- <파일>                        # 미커밋 변경도 함께 본다
```

**이 값은 표에 적지 않는다.** 적는 순간 낡는다 — 만날 때마다 돌린다.

**비지 않았다고 파일 전체를 `/점검`으로 다시 돌리지 않는다.**
이미 전수한 화면이다. **바뀐 줄과 그 호출 길만** 본다.

- 동작이 새거나(타이머 · 채점 · 언마운트) 0-2절에 없는 것이 나오면 그때 파일을 넓힌다
- 바뀐 자리가 이미 0-2절이면 **「새것 없음」**하고 멈춘다
- 어느 쪽이든 **점검 커밋은 지금 HEAD로 갱신한다** — 안 그러면 다음 세션이 같은 파일을 또 후보로 꺼낸다

---

## 화면별

화면 하나가 파일 하나가 아니다. **딸린 파일까지 다 봐야 「그 화면을 봤다」**이다.
아래 묶음은 상상이 아니라 각 화면의 `import`를 따라간 것이다.

### 📚 단어 카드 (flashcards 탭)

| 파일 | 줄 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|
| `app/(tabs)/flashcards/index.tsx` | **1263** | **세션 42** | **전수** | `326320a` | 22 / 9 |
| `components/game/WordFlashcard.tsx` | **328** | **세션 54** | **전수** | `563541a` | **3** / 3 |
| `components/game/Waveform.tsx` | **80** | **세션 54** | **전수** | `563541a` | 0 / 0 |
| `app/(tabs)/flashcards/_layout.tsx` | 5 | **세션 57** | **전수** | `f2828a5` | 0 / 0 |

> `WordFlashcard`의 터치를 **7 → 3**으로 고쳤다. 7은 `grep -c`가 센 줄 수(여는 태그 3 +
> 닫는 태그 3 + `import` 1)였고 실제 터치는 **3개**(단어 카드 둘 + 전체 듣기)다.
>
> 묶음에 **`components/game/waveformData.ts`(13)**이 하나 더 있다 — `WordFlashcard`가
> `import`한다. UI 파일이 아니라 아래 「한눈에」에는 안 센다 (세션 47 `constants/refriItems.ts`와 같다).
> `hooks/useWordAudioPlayer.ts`(246)는 **learn과 공용**이고 세션 52에 0건으로 봤다.

### 🎧 learn 탭 (소리 구별 퀴즈)

| 파일 | 줄 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|
| `app/(tabs)/learn/index.tsx` | **749** | 세션 52 · 60 · 61 · 62 · **68** | **전수** | `123a389` | **4** / **5** |
| `components/game/WordGame.tsx` | **455** | 세션 52 · 60 · 61 · 62 재점검 · **69** | **전수** | **`b4f7f1c`** | 0 / **0** |
| `screens/DrumGameOverScreen.tsx` | **322** | 세션 56 · **60 재점검** | **전수** | `90076d4` | 2 / 2 |
| `app/(tabs)/learn/_layout.tsx` | 2 | **세션 52** | **전수** | `10c0357` | 0 / 0 |

> `DrumGameOverScreen`은 **learn과 드럼이 함께 쓴다.** 한쪽에서 고치면 다른 쪽도 바뀐다.
> **세션 50에 드럼 쪽만 `Modal`로 고쳐, learn의 같은 자리는 세션 52까지 남아 있었다.**
>
> 딸린 로직 셋도 함께 봤다 — `hooks/useWordGameLogic.ts`(161 → 198 → 225 → **228**) ·
> `hooks/useWordAudioPlayer.ts`(246, **flashcards와 공용**) · `hooks/useStopAudioOnBlur.ts`(46).
> UI 파일이 아니라 「한눈에」에는 안 센다 (드럼 `useDrumLogic`과 같다).
> **고친 넷 중 셋(1·2·3번)이 `useWordGameLogic.ts`에 있었다.**
>
> **`WordGame`의 「0 / 0」은 터치가 없다는 뜻이 아니다** — 조작 4개(시작하기 · 선택지 2 ·
> 다시 듣기)가 `GestureDetector` + `Animated.View`라 터치 요소로 잡히지 않는다.
> `DraggableImage`(`PanResponder`)와 같은 모양이고, **토크백으로는 하나도 누를 수 없다** (0-2절).

### 🥁 드럼 탭

| 파일 | 줄 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|
| `app/(tabs)/drum/index.tsx` | **1333** | **세션 57** | **전수** | `ebc5926` | 7 / 2 |
| `components/game/InteractiveDrumSet.tsx` | **940** | **세션 57** | **전수** | `ebc5926` | 2 / **0** |
| `screens/DrumGameOverScreen.tsx` | **322** | 세션 56 · **60 재점검** | **전수** | `90076d4` | 2 / 2 |
| `app/(tabs)/drum/_layout.tsx` | 5 | **세션 50** | **전수** | `818bb9d` | 0 / 0 |

> 딸린 로직 둘도 함께 봤다 — `hooks/useDrumLogic.ts`(242) · `constants/drumLayouts.ts`(121).
> UI 파일이 아니라 아래 「한눈에」에는 안 센다 (세션 47 `constants/refriItems.ts`와 같다).
> **고친 다섯 중 둘(1·2번)이 `useDrumLogic.ts`에 있었다** — 화면 묶음은 `import`를 따라간다.

### 🎹 피아노·기타 (guitar 탭 · activity 탭)

**기타 화면은 세션 48, 피아노 화면은 세션 49에 나갔다. 이 묶음은 다 봤다.**
피아노는 `guitar` 탭이 아니라 **`app/(tabs)/activity/`**가 띄운다 — 세션 49에 알았다.

| 파일 | 줄 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|
| `screens/MusicTrainingScreen.tsx` | 1886 | **세션 49** | **전수** | `8fe2c23` | 4 / **0** |
| `app/(tabs)/guitar/_layout.tsx` | **593** | 세션 48 · **70 재점검** | **전수** | **`67cd697`** | 1 / **0** |
| `components/FallingNoteTrack.tsx` | 347 | **세션 49** | **전수** | `8fe2c23` | 0 / 0 |
| `components/FallingResultOverlays.tsx` | 242 | **세션 49** | **전수** | `8fe2c23` | 3 / **0** |
| `components/MiniKeyboardMap.tsx` | 191 | **세션 49** | **전수** | `8fe2c23` | 1 / **0** |
| `components/ParticleVisualizer.tsx` | 178 | **세션 49** | **전수** | `8fe2c23` | 0 / 0 |
| `components/instrument/InstrumentButton.tsx` | 156 | **세션 48** | **전수** | `a57c217` | 1 / **0** |
| `components/instrument/ScoreFeedback.tsx` | 81 | **세션 48** | **전수** | `a57c217` | 0 / 0 |
| `components/instrument/DifficultyRow.tsx` | 63 | **세션 48** | **전수** | `a57c217` | 0 / 0 |
| `components/instrument/InstrumentControlBar.tsx` | 58 | **세션 48** | **전수** | `a57c217` | 0 / 0 |
| `components/instrument/useInstrumentMetrics.ts` | 51 | **세션 48** | **전수** | `a57c217` | 0 / 0 |
| `components/LandscapeBackButton.tsx` | 44 | **세션 49** | **전수** | `8fe2c23` | 1 / **1** |
| `components/RippleLayer.tsx` | 31 | **세션 49** | **전수** | `8fe2c23` | 0 / 0 |
| `components/instrument/index.ts` | 9 | **세션 48** | **전수** | `a57c217` | 0 / 0 |
| `app/(tabs)/activity/index.tsx` | 5 | **세션 49** | **전수** | `8fe2c23` | 0 / 0 |
| `app/(tabs)/activity/_layout.tsx` | 5 | **세션 49** | **전수** | `8fe2c23` | 0 / 0 |

> **`components/instrument/`의 다섯은 피아노도 함께 쓴다.** 여기서 고치면
> `MusicTrainingScreen`도 같이 바뀐다 — 0-2절의 「[피아노 경로]」 줄이 그래서 있다.
> **세션 49에는 다시 훑지 않았다** — `git diff --name-only a57c217 HEAD -- components/instrument`가
> 비었다. 「다시 훑어야 하나」 판정이 실제로 한 묶음을 걸러낸 첫 사례다.
>
> **`app/(tabs)/activity/` 둘이 「앱 골격」에서 이리로 왔다.** 표에는 골격으로 적혀 있었지만
> `index.tsx`는 `<MusicTrainingScreen />` 한 줄이라 **피아노 화면의 일부**다.
> 화면 묶음은 `import`를 따라간다는 규칙대로 옮겼다.
>
> **표에 없던 파일 둘이 늘었다** — `useInstrumentMetrics.ts`(51)와 배럴 `index.ts`(9).
> 컴포넌트가 아니라 빠져 있었는데, 세션 48에 둘 다에서 발견이 나왔다
> (`clamp`가 아무 데서도 안 쓰이는데 `index.ts:9`가 다시 내보낸다).
> 안 봤다면 「안 봤다」로 보여야 하므로 **표에 넣는다** — 파일 수가 42 → 44가 됐다.
>
> **터치 수를 실측으로 고쳤다** (세션 47 `refri-test`와 같은 이유). `grep -c` 줄 수 3은
> 여는 태그 + 닫는 태그 + `import`였고, 소스에 쓰인 터치는 **각각 하나**다 —
> 다만 기타 화면이 **화면에 그리는** 터치는 프렛 24개 + `InstrumentButton` 6개다.

### 🎮 게임 (new 탭)

**이 묶음은 세션 45~46에 다 나갔다.** 점검은 세션 45(27건 보고), 수정은 세션 46(7건)이다 —
**두 세션에 걸쳐 있어 「마지막 점검」과 「점검 커밋」의 세션 번호가 다르다.**

| 파일 | 줄 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|
| `app/(tabs)/new/(games)/matchGamePG.tsx` | 720 | **세션 44** | **전수** | `2cceea3` | 15 / 7 |
| `app/(tabs)/new/(games)/matchGameAI.tsx` | **762** | 세션 44 · **69 재점검** | **전수** | **`85505aa`** | 15 / 7 |
| `app/(tabs)/new/(games)/orderGame.tsx` | 657 | **세션 45~46** | **전수** | `fac2130` | 5 / 2 |
| `app/(tabs)/new/(games)/matchGame.tsx` | **600** | 세션 45~46 · **59 재점검** | **전수** | `6d74f5a` | 11 / 8 |
| `app/(tabs)/new/index.tsx` | 479 | **세션 45~46** | **전수** | `fac2130` | 5 / 1 |
| `components/WaveRipple.tsx` | **121** | **세션 58** | **전수** | `65647b8` | 0 / 0 |
| `components/game/DraggableImage.tsx` | 103 | **세션 45~46** | **전수** | `fac2130` | 0 / 1 |
| `components/RiveAnimalGame.tsx` | **99** | 세션 58 · **59 재점검** | **전수** | `6d74f5a` | 0 / 0 |
| `app/(tabs)/new/(games)/_layout.tsx` | **63** | 세션 57 · **61 재점검** | **전수** | `27ba6db` | 0 / 0 |
| `components/GameExitButton.tsx` | **90** | **세션 61** | **전수** | `27ba6db` | **1 / 1** |
| `app/(tabs)/new/_layout.tsx` | 36 | **세션 57** | **전수** | `f2828a5` | 0 / 0 |

> **세션 59에 그 자리를 닫았다.** `matchGame`이 점검 커밋(`fac2130`)보다 새 파일이었고 —
> 「안 본 것」이 0이 된 뒤 `git diff`가 **처음으로 뭔가를 걸러낸 자리**다 —
> 바뀐 줄(트리거 호출부)과 그 호출 길만 봐서 1건을 고쳤다. 둘 다 점검 커밋이 `6d74f5a`로 갔다.
> **`RiveAnimalGame`도 같은 커밋에서 바뀌어 함께 갱신했다.**

### 🧊 냉장고 (refri-test 탭)

| 파일 | 줄 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|
| `app/(tabs)/refri-test/index.tsx` | **1383** | 세션 47 · 63 · 64 · 65 · 66 · **67 재점검** | **전수** | `b739b4b` | 4 / **0** |
| `app/(tabs)/refri-test/_layout.tsx` | 2 | **세션 47** | **전수** | `f981d38` | 0 / 0 |

### 🔗 여러 화면이 함께 쓰는 것

| 파일 | 줄 | 쓰는 곳 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|---|
| `components/MissionProgressIcon.tsx` | 308 | guitar · 피아노 · 게임 넷 | **세션 43** | **전수** | `4f3bf11` | 9 / 4 |
| `screens/DrumGameOverScreen.tsx` | **322** | learn · 드럼 | 세션 56 · **60 재점검** | **전수** | `90076d4` | 2 / 2 |

> **`MissionProgressIcon`을 고치면 다섯 화면이 함께 바뀐다.** 익힘모달의 짝이기도 하다.
>
> **`DrumGameOverScreen`의 점검 커밋이 `e9b2061` → `?` → `bfcaa90`으로 옮겨졌다.** 세션 56에
> 만점 콘페티를 넣고 57에 커밋해, 세션 58에 `?`를 채웠다. 전수는 세션 50에 했고, 세션 52 「나가기」 선택까지 본 뒤에
> 이번은 콘페티·`absoluteFill` 자리만 다시 봤다.

### 🧱 앱 골격

| 파일 | 줄 | 마지막 점검 | 범위 | 점검 커밋 | 터치/`Role` |
|---|---|---|---|---|---|
| `app/_layout.tsx` | **219** | **세션 51** | **전수** | `b11079b` | 1 / 1 |
| `app/(tabs)/_layout.tsx` | **241** | **세션 53** | **전수** | `4198874` | **1 / 1** |
| `app/+not-found.tsx` | 64 | **세션 57** | **전수** | `f2828a5` | **1** / **0** |
| `app/index.tsx` | 5 | **세션 57** | **전수** | `f2828a5` | 0 / 0 |

> `app/(tabs)/activity/` 둘은 **🎹 피아노 묶음으로 옮겼다** (세션 49). 골격이 아니라
> `MusicTrainingScreen`을 띄우는 자리다.
>
> **탭 골격의 「터치 1 / `Role` 1」은 실측이다.** `grep -c`는 3이지만(`import` + 여는 태그 +
> 닫는 태그) 실제 터치는 `AnimatedTabBarButton`의 `Pressable` **하나**이고, 그것이 탭마다 7번 그려진다.
> `accessibilityRole`은 이 파일에 없지만 `bottom-tabs`가 `role='tab'`·`aria-selected`를 **props로 넘겨**
> `filteredRestProps`로 그대로 붙는다 — 세션 53에 소스로 확인했다(`BottomTabItem.js:127-152`).

---

## 한눈에

| | 파일 | 줄 | 터치 | `Role` |
|---|---|---|---|---|
| **전수로 훑음** | **45** | **15,296** | 119 | 55 |
| **부분만 훑음** | **0** | **0** | 0 | 0 |
| **한 번도 안 봤다** | **0** | **0** | 0 | 0 |
| 합 | 45 | 15,296 | 119 | 55 (**46%**) |

> **「한 번도 안 봤다」 줄이 세션 58에 0이 됐다.** 마지막 둘(`WaveRipple`·`RiveAnimalGame`)이
> 나가면서 **이 표에 남은 줄은 「전수」 하나**다 — 세션 41에 다시 짤 때 4/44로 시작한 표다.
>
> **`Role` 51/116(44%)은 그대로다.** 세션 58에 고친 여섯에 터치가 없었다 —
> 「훑었다」가 「고쳤다」가 아닌 것처럼, **다 훑었다도 다 고쳤다가 아니다.**
>
> **세션 60에 116/51 → 117/52가 됐다.** learn 「그만하기」가 터치를 하나 더했고
> `accessibilityRole`·`Label`을 **함께 붙여** 비율(44%)은 그대로다.
> 줄 수는 세 파일이 130줄 늘어 **14,728 → 14,858**이다.
>
> **세션 61에 117/52 → 118/53, 파일이 44 → 45가 됐다.** 게임 넷 공용 「나가기」
> (`components/GameExitButton.tsx`)가 **파일째로 새로 생겨** 훑은 파일이 하나 늘었다.
> 여기도 터치와 `Role`·`Label`을 함께 붙여 비율이 44% → **45%**로 올랐다.
> 줄은 새 파일 90 + `(games)/_layout` 5 = **14,858 → 14,953**이다.
> **같은 세션의 learn 수정으로 24줄이 더 늘어 최종 14,977이다**(주석이 대부분이다) —
> 터치와 `Role`은 안 바뀌었다.
>
> **세션 62에 118/53 → 119/54가 됐다.** learn 액션줄의 왼쪽 토글이 터치 하나를 더했고
> `Role`·`Label`·`State`를 **함께 붙였다.** 같은 세션에 `WordGame`의 조작 둘
> (시작하기·다시 듣기)이 **`GestureDetector`에서 `TouchableOpacity`로 바뀌어**
> 0-2절 「조작 4개가 터치 요소가 아니다」(52)가 **둘로 줄었다** — `WordGame`의
> `grep -c`는 0 그대로다(그 둘이 파일 밖으로 나갔다). 줄은 learn +114 · `WordGame` −32로
> **14,977 → 15,059**다.
>
> **세션 48·49·50에 훑은 스무 파일이 더한 `Role`은 1개뿐**이고, 그 하나도 원래 있던
> `LandscapeBackButton`이다. 「훑었다」는 **봤다**는 뜻이지 고쳤다는 뜻이 아니다 —
> 세 세션의 접근성 발견 열둘(`InstrumentButton` · 프렛 24 · `ScoreFeedback` ·
> 건반 28 · 옥타브 2 · 미니맵 · 곡 슬롯 · 결과 버튼 3 · 드럼 마커 · 드럼 헤더·하단 버튼 ·
> 라이브리전 셋)은 **전부 0-2절에 있고 아직 안 골랐다.**
>
> **전수가 파일 45/45, 줄 14,977/14,977(100%)**이다. 세션 41에 이 표를
> 다시 짤 때는 4개였고, 세션 57까지 42개였다. **새 파일은 만든 자리에서 함께 훑어
> 100%를 지킨다** — 세션 61의 `GameExitButton`이 첫 사례다.
>
> **이 문단은 세션 57에 42/44가 된 뒤에도 37/44로 남아 있었다** — 표만 갱신하고
> 문단을 안 고친 것을 세션 58 시작에 대조로 잡았다. **「한눈에」는 갱신 절이라
> 낡으면 안 되는 자리다.**
>
> **「부분」 칸이 세션 54에 0이 됐고, 「한 번도 안 봤다」가 58에 0이 됐다.**
> 남은 물음은 「전수냐 미점검이냐」가 아니라 **「전수 뒤에 무엇이 바뀌었나」**뿐이고,
> 그건 **점검 커밋**이 답한다 — 세션 58에 `?` 아홉을 채워 **표에 `?`가 다시 0**이 됐다.
>
> **합이 168 → 161 → 157 → 142 → 129 → 127 → 124 → 122로 줄었다.** 훑은 파일은 `grep -c`의 줄 수(여는 태그 +
> 닫는 태그 + `import`)를 **실측으로 고치기** 때문이다 — 세션 47에 `refri-test`(11 → 4),
> 세션 48에 `guitar/_layout`(3 → 1)과 `InstrumentButton`(3 → 1), 세션 49에
> `MusicTrainingScreen`(11 → 4) · `FallingResultOverlays`(7 → 3) ·
> `MiniKeyboardMap`(3 → 1) · `LandscapeBackButton`(3 → 1), 세션 50에
> `drum/index`(14 → 7) · `InteractiveDrumSet`(5 → 2) · `DrumGameOverScreen`(5 → 2),
> 세션 51에 `app/_layout`(3 → 1), 세션 52에 `learn/index`(5 → 2),
> 세션 54에 `WordFlashcard`(7 → 3). 안 훑은 파일의 수는 여전히 줄 수다.
>
> **드럼의 7·2도 「소스에 쓰인 터치」다.** 실제로 그리는 것은 `drum/index`가 **16개**
> (하단 버튼 두 종류가 페이지 4벌 × 2 + 세그먼트 4 + 헤더 3 + 백드롭 1),
> `InteractiveDrumSet`이 페이지마다 **마커 2~5개**다 — 피아노 건반·기타 프렛과 같은 모양이다.
>
> **피아노의 4는 「소스에 쓰인 터치」다.** 화면에 실제로 그리는 것은 건반 27~28개 +
> 옥타브 버튼 2개 ≈ **30개**다 — `PianoKey` 하나가 `map`으로 늘어난다
> (기타 프렛 24개와 같은 모양).
>
> `DraggableImage`는 터치가 0인데 `Role`이 1이다 — `TouchableOpacity`가 아니라
> `PanResponder`를 붙인 `Animated.View`라 위 `grep`에 안 잡힌다.

---

## 세션 40 시범 점검 — 단어 카드 (7건)

**세션 36~39에 네 번 손본 파일인데 7건이 나왔다.** 그중 둘은 눈으로 보이지 않는 것이다
(`setFilteredPairs` updater 안의 부작용 · 학습완료 연타). 보고서는 `doc/conversation_40.md`에 있다.

## 세션 41 — 7건 수정 + 재점검 (새 4건)

7건을 다 고치고 규칙 5에 따라 같은 표로 다시 훑어 **4건**이 더 나왔다 —
모달 배경(닫기 역할인데 접근성 표시 없음) · `StyleSheet.absoluteFill` 자리의 인라인 절대배치 ·
고정 px 셋(`completeButton` 20 · `completionText` 10 · `completedCardImage` 8) ·
`resetAnimation` 주석이 코드와 다른 것.

**넷 다 세션 40에도 있던 것이다.** `git diff`로 확인했다 — 이번 수정으로 생긴 것은 없다.
즉 **지난번 「전수」가 놓친 것**이고, 큰 것 일곱 개에 가려 눈에 안 들어왔다.

> **한 번 훑었다고 끝이 아니다.** 「전수」는 **그 시점의 전수**이지 파일의 결론이 아니다.
> 큰 것을 걷어내면 그 아래가 보인다 — 그래서 고친 뒤 한 번 더 훑는다.

## 세션 41 — 이 표를 다시 짰다

전에는 **손으로 고른 14줄**이라 앱 42개 파일 중 **28개가 표에 없었다.**
없는 것은 「안 봤다」로도 안 보인다 — `InteractiveDrumSet`(885) · `guitar/_layout`(503) ·
`WordGame`(429) · `FallingNoteTrack`(347)이 그랬다.

바꾼 것 셋: **UI 파일 전수** · **화면 단위로 묶음**(`import`를 따라갔다) · **점검 커밋 기록**.

> **커밋을 적는 이유가 중복 방지다.** 전에는 「세션 41」만 있어서, 다시 만났을 때
> 「이미 봤으니 건너뛴다」인지 「그 뒤 바뀌었으니 다시 본다」인지 알 수 없었다.

## 세션 42 — 재점검 4건 수정 + 그 뒤 한 번 더 (새 3건)

넷을 사용자가 전부 골라 고쳤다 (`bae606b`). **1번은 보고했던 것보다 컸다** —
「배경에 `Role`·`Label`이 없다」로 적었는데, 그 배경 `TouchableOpacity`가 **시트를 자식으로 품고**
있었고 `TouchableOpacity`는 `accessible`을 끄지 않는 한 켜져 있다
(RN 0.86 `TouchableOpacity.js:303`). 즉 **시트 안이 통째로 한 덩어리**였고,
거기 `Role`을 달면 그 묶임이 굳는다. 절대배치 **형제**로 내렸다 — 미션모달이 쓰는 형태다.

> **보류 항목의 문구를 그대로 믿고 고치지 말 것.** 적을 때 본 것은 「없는 속성」이었고,
> 고치러 가서야 **왜 없었는지**(구조가 틀렸다)가 보였다.

규칙 5대로 한 번 더 훑어 **3건**이 나왔다. **고치지 않고 점검표 0-2절로 보냈다.**

| 자리 | 무엇이 |
|---|---|
| `elevation: 10` ×2 (`modalContent` · 하단 네비) · `elevation: 3` (`completionRestartButton`) | 옆 그림자는 토큰인데(`completeButtonElevation` · `navArrowButtonElevation` · `completedCardItemElevation`) 이 셋만 리터럴 |
| 완료 항목 슬래시의 인라인 `marginHorizontal: 6` | 320dp에서 안쪽 폭이 108로 빠듯한 자리인데 리터럴이고 인라인이다 |
| `measure` 콜백의 안 쓰는 인자 `bx` `by` `cx` `cy` | 위치 인자라 뺄 수 없지만 `tsc`가 4건 경고한다. `_bx` 꼴이면 조용해진다 |

> **터치/`Role` 칸의 22 / 9는 grep 줄 수라 주석의 「`TouchableOpacity`」도 센다.**
> 실제 터치 요소는 **9개이고 9개 전부** `Role`·`Label`을 갖췄다 (`disabled`인 넷은 `State`도).


## 세션 43 — 미션모달 전수 (7건 → 3건 수정 · 재점검 2건)

**사용처 6곳을 함께 봤다.** 표에는 「guitar · 게임 넷」으로 적혀 있었지만 실제로는
피아노(`MusicTrainingScreen`)까지 **여섯**이고, 그중 **악기 둘만 `style`을 넘긴다.**

나온 7건 중 사용자가 **접근성 셋**(1 · 1-2 · 7)을 골라 고쳤다 (`4f3bf11`).
나머지 넷(아이콘 색 대비 · `top: 50` · `ScrollView` 인라인 · 색 토큰 · 태블릿)은 0-2절로 갔다.

| 고친 것 | 무엇이었나 |
|---|---|
| 터치 4개에 `Role`·`Label` | **넷 다 하나도 없었다.** 우상단 아이콘은 이름 없는 버튼이었다 |
| 조건 카드에 달성 여부 | 별·체크가 **아이콘 모양·색에만** 있어 토크백에는 조건만 읽혔다 |
| `style?: any` → `StyleProp<ViewStyle>` | 넘기는 쪽이 `{top, right}`라 그대로 통과한다 |

> **배경(`Pressable`)은 구조가 이미 옳았다.** 점검표 3절이 이 파일을 「정해진 답」의 예로
> 드는 바로 그 형태(절대배치 **형제**)인데 **라벨만 없었다.** 세션 42의 flashcards와
> **반대 모양**이다 — 거기는 라벨을 달려다 구조가 틀린 걸 발견했고, 여기는 구조가 맞는데
> 라벨이 없었다. **「같은 갈래」가 늘 같은 원인은 아니다.**

**`top: 50`은 처음에 「상태바와 겹친다」로 보고했는데 사용자가 바로잡았다.**
게임은 `app/(tabs)/new/(games)/_layout.tsx`의 헤더가 `paddingTop: insets.top`을 이미 써서,
`top: 50`은 상태바가 아니라 **그 헤더 아래 기준**이다. 겹치는 게 아니라 **더 내려올 수** 있다.

> **인셋이 없어 보이면 그 화면의 `_layout`부터 본다.** 컴포넌트만 읽고 「인셋을 안 쓴다」고
> 단정하면 **부모가 이미 쓴 인셋을 못 본다.** 남는 문제는 겹침이 아니라
> **화면마다 기준이 다른 것**이고, 그건 기기에서 볼 일이다.

규칙 5대로 고친 뒤 한 번 더 훑어 **2건**이 나왔다 — 진행 항목의 「•」이 장식인데
`importantForAccessibility`가 없는 것 · `modalTitle`에 `accessibilityRole="header"`가 없는 것.
**고치지 않고 0-2절로 보냈다.**

---

## 세션 44 — 청능 훈련 쌍둥이 전수 (23건 → 5건 수정 · 재점검 4건)

**`matchGamePG`와 `matchGameAI`는 짝이라 함께 훑었다.** 1,437줄인데 `Role`이 양쪽 다 0개였다.
**짝을 나란히 놓으니 「한쪽에만 있는 것」이 문제로 드러났다** — 나온 23건 중 넷이 그랬다.

사용자가 **5건**(1 · 2 · 9 · 10 · 11)을 골라 고쳤다 (`2cceea3`).

| 고친 것 | 무엇이었나 |
|---|---|
| 터치 14개에 `Role`·`Label` | 두 파일 다 **0개**였다 |
| 게임 버튼에 `accessibilityState`·결과 라벨 | 정답·오답이 **색으로만** 구분돼 토크백엔 안 읽혔다 |
| AI: `GAME_START_REQUEST`를 재생 **뒤로** | 앞에 있으면 `await` 사이 렌더에서 **이전 라운드 정답으로 게임판이 보이고 그 창에 누른 것이 채점된다** |
| AI: 중복 채점 빗장 | **PG에는 있었다** (`if (state.userSelections[selectedName]) return state;`) |
| AI: `LOADING`을 `centered` 안으로 | PG는 감쌌고 AI는 안 감싸 스피너가 **좌상단**에 떴다 |

> **짝이 있는 화면은 나란히 훑는다.** 9·10·11은 「AI가 틀렸다」가 아니라
> **「PG에는 있는데 AI에는 없다」**로 드러났다. 한 파일만 봤으면 셋 다 정상으로 보였을 것이다.
> 색 리터럴(8번)도 같은 모양이다 — PG는 토큰, AI는 `#C8E6C9` 리터럴.

**사용자가 「그대로」를 고른 것이 여덟이다** (6 · 8 · 12 · 13 · 14 · 16 · 18 · 21 → 0절).
대비 4.36:1·색 리터럴·`elevation` 리터럴·이모지 라벨처럼 **지적은 맞지만 안 고치기로** 정한 것들이다.

규칙 5대로 고친 뒤 한 번 더 훑어 **4건**이 나왔다 — 그중 하나(AI의 통계 집계)는 **접근성이 아니라
데이터가 틀어지는 것**이다. 넷 다 **고치지 않고 0-2절로 보냈다.**

**❓ 3건은 사용자가 답해서 갈렸다** — 19·20은 「PG처럼」이라 고쳤고(`07dd68f`),
17(`useSyncGameData`의 `return;`)은 「그대로」라 0절로 갔다.
**재점검은 다시 돌리지 않았다** — 규칙 5대로 한 번뿐이고, 이미 위에서 돌렸다.

---

## 세션 47 — 냉장고 전수 (27건 → 8건 수정 · 재점검 4건)

**한 번도 안 본 화면 중 첫 번째였다.** `app/(tabs)/refri-test/index.tsx`(1,298 →
1,340) + `_layout.tsx`(2줄, `Slot`뿐) + `constants/refriItems.ts` + `LAYOUT`의
`refri*` 토큰까지 범위로 잡았다. 점검 커밋 `f981d38`.

**27건이 나왔고 사용자가 8건을 골랐다** — 1·2·3·4·8·12·18·19.
기준은 세션 45~46과 같다: **동작 버그와 이미 정해진 규칙만 고치고**,
계산으로 끝난 대비·토큰·치수는 미룬다.

| 갈래 | 무엇 |
|---|---|
| 동작 | 1·2 다시듣기가 이번 문항이 아닌 소리를 냈다 · 3 별 루프가 쌓였다 · 4 타이머 6곳 미정리 · 18 `isAnimating`이 state라 타이머가 옛 값을 봤다 |
| 정해진 규칙 | 8 완료 오버레이가 `Modal`이 아니었다(익힘모달과 같음) · 12 흰 바탕 초록 글자 · 19 디버그 로그 |

> **`await` 앞 상태 전환이 세 번째 화면에서 또 나왔다.** `matchGameAI`(44) ·
> `matchGame`(46) · `refri-test`(47). 이번에는 카드가 아니라 **「다시 듣기」**가
> 먼저 떴고, 그때 `currentQuiz`는 `MONO_ITEMS[0]`(아스파라거스)였다.
> **화면마다 켜지는 것이 다를 뿐 결함은 하나다.**

**「그대로」 3건은 사용자가 갈랐다** — 16(`REFRI_RIVE_AVAILABLE`) ·
21(deps `[]`) · 27(상자 음수 비율). 셋 다 0절로 올렸다.
16은 내가 「죽은 36줄이니 지우자」고 했는데 **`DEBUG_DROP`과 같은 스위치 갈래**라는
사용자 지적이 맞았다.

**재점검에서 4건이 더 나왔고 넷 다 이번 수정이 만든 것이다** —
`completeOverlay`가 `Modal` 안인데 `position:absolute`+`zIndex` 그대로 ·
`startGame`의 새 빗장이 state라 첫 프레임에 샌다 · `timerRefs`를 라운드마다
안 비운다 · `stopStarAnimation`이 deps에 없다.
**규칙 5대로 고치지 않고 0-2절로 보냈다.**

> **세션 41·42·44·46의 재점검은 「원래 있던 것」이 나왔는데 이번은 반대다.**
> 넷 다 **고치면서 새로 들인 것**이라, 다음 차례에 함께 보면 값이 싸다.

**터치 수를 실측으로 고쳤다.** 표에 있던 11은 `grep -c`의 줄 수(여는 태그 +
닫는 태그 + `import`)였고 실제 터치는 **4개**다. 「한눈에」 합이 168 → 161이 됐다.

**빠진 것 — 세션 45·46의 노트가 이 문서에 없다.** 표는 갱신돼 있지만 「그때 무엇이
나왔나」는 `handoff5.md`에만 있다. 채울지는 다음에 정한다.

## 세션 48 — 기타 화면 26건 (고침 5 · 0절 1 · 0-2절 20 + 재점검 4)

**guitar 묶음의 절반이 나갔다.** 훑은 것은 `app/(tabs)/guitar/_layout.tsx`(581)와
`components/instrument/` 여섯(418) — **999줄**이다. 피아노 쪽 2,773줄은 그대로 남았다.

고친 다섯은 **전부 동작**이다 (`a57c217`).

| 갈래 | 무엇 |
|---|---|
| 동작 | 1 **프렛에 없는 음이 출제됐다**(정답을 누를 수가 없다) · 2 연타가 한 문항에 여러 번 채점됐다 · 3 타이머 미정리 · 4 응답시간이 재생 지연을 포함했다 · 5 `progress`가 실제 진행과 어긋났다 |

> **1번이 이 화면의 본체였다.** 출제는 `guitarSounds` 28개에서 뽑는데 프렛보드는
> 그중 일부만 그린다 — 뽑힌 음이 프렛에 없으면 **맞힐 방법이 없다.**
> 난이도별 「보이는 음」으로 출제 집합을 좁혀서 풀었다.

**「그대로」 1건은 사용자가 갈랐다** — 22(3단계 `slice(0, 18)`). 고친 뒤
**3단계 후보가 2단계보다 적어졌는데**(15 vs 17), 뒤집힌 것은 **1번의 부작용**이지
새 설계가 아니다. 줄 단위로 다시 잡으면 코드는 깔끔해지지만 **커리큘럼을 바꾸는 일**이라
손대지 않기로 했다. 0절로 올렸다.

**남은 20건은 0-2절로 갔다** — 대비 6줄 · 접근성 4줄 · 기기 대기 4줄 · 죽은 코드와
구조 6줄이다. **접근성 셋은 공용 부품(`InstrumentButton`)이라 피아노까지 걸린다** —
고칠 때 두 화면을 함께 봐야 한다.

**재점검에서 4건이 나왔고 넷 다 이번 수정이 들여온 것이다** — `timerRefs`를
정답마다 안 비운다 · `playSound(...).then()`이 종료 뒤 도착해 `questionStartTimeRef`를
다시 찍는다 · `progress`를 state와 ref 두 곳에 든다 · `getVisibleNoteSet`이 호출마다
28개를 다시 필터한다. **규칙 5대로 고치지 않고 0-2절로 보냈다.**

> **세션 47과 같은 모양이다** — 재점검에서 나온 것이 「원래 있던 것」이 아니라
> **고치면서 들인 것**이었다 (41·42·44·46은 반대였다). 그중 `timerRefs`는
> **47의 `refri-test` 줄과 글자까지 같다** — 화면이 달라도 손버릇이 같다.

---

## 세션 49 — 피아노 전수 37건 (고침 5 + 부분 1 · 0-2절 32 + 재점검 6)

**guitar 묶음의 나머지 절반이 나갔다.** 범위는 `screens/MusicTrainingScreen.tsx`(1799 →
1886) · `FallingNoteTrack`(347) · `FallingResultOverlays`(227 → 242) ·
`MiniKeyboardMap`(191) · `ParticleVisualizer`(178) · `LandscapeBackButton`(44) ·
`RippleLayer`(31) · `app/(tabs)/activity/` 둘(10) · `musicTrainingHelpers.ts` — **2,827줄**이다.
점검 커밋 `8fe2c23`.

**`components/instrument/` 여섯과 `MissionProgressIcon`은 다시 훑지 않았다.**
`git diff --name-only a57c217 HEAD -- components/instrument`와 `4f3bf11` 쪽이 **둘 다 비었다** —
세션 41에 커밋 칸을 만든 뒤 **그 판정이 실제로 한 묶음을 걸러낸 첫 사례**다.

고친 다섯은 **전부 동작**이고, 사용자 기준은 세션 45~48과 같다.

| 갈래 | 무엇 |
|---|---|
| 동작 | 1 **응답시간을 새 문제마다 안 찍었다** · 2 정답 타이머 미정리 · 3 연타로 한 문제에 여러 번 채점 · 4 정답 처리가 렌더 클로저 값을 읽었다 · 5 `addStar`·`markAsCleared`가 정답마다 다시 불렸다 |
| 정해진 규칙 (부분) | 19 낙하 **리플레이·결과 둘만** `Modal`로. `missionOverlay`·`previewOverlay`는 **고르지 않아 그대로** |

> **세션 48 기타의 결함 넷이 피아노에도 그대로 있었다** — 응답시간(4번) · 타이머(3번) ·
> 연타(2번) · 클로저(5번). 같은 사람이 같은 골격을 두 번 쓴 자리라 **번호까지 겹친다.**
> **한쪽을 고쳤으면 짝을 바로 본다** — 기타를 훑은 세션 48에 이미 알 수 있었다.

**기타 1번(못 누르는 음 출제)은 피아노에 없었다.** 4·5단계 뷰포트 두 자리(0·14)의
합집합이 백건 30 · 흑건 22를 **전부 덮고**, 낙하 팔레트(`PENTA5`·`WHITE8`)도 전부 백건이라
레인과 건반이 어긋나지 않는다. **같은 갈래를 세었더니 이번엔 0건이었다** —
0건도 결과다.

**보고 5번은 내가 과장했다.** 「조건을 넘긴 뒤 정답마다 다시 부른다」는 맞지만
`StarContext.addStar`·`ClearContext.markAsCleared`가 **안에서 이미 막고 있어** 실제 동작은
전과 같다. 고른 뒤에야 Provider를 열어 봤다. **호출부만 보고 영향을 적지 말 것.**

**재점검에서 6건이 나왔고 여섯 다 이번 수정이 들여온 것이다** — `score`·`progress`를
state와 ref 두 곳에 든다 · 빗장 풀기가 여섯 자리로 흩어졌다 · `questionStartTime`이
여전히 state다 · `Modal` 안에 `position:absolute`+`zIndex`가 남았다 ·
`dismiss…`와 `close…`가 두 줄 겹친다 · 가로모드 `statusBarTranslucent`(📱).
**규칙 5대로 고치지 않고 0-2절로 보냈다.**

> **세션 47·48에 이어 세 번째로 「고치며 들인 것」이었다** (41·42·44·46은 반대였다).
> 그중 둘은 **앞 세션 0-2절에 이미 있는 줄과 같은 모양**이다 — 기타의 `progress` 이중 보유,
> 냉장고의 `Modal` 안 `absolute`. **미룬 항목이 다음 화면에서 다시 만들어진다.**

**표에서 파일 둘이 자리를 옮겼다.** `app/(tabs)/activity/`는 「앱 골격」에 있었지만
`index.tsx`가 `<MusicTrainingScreen />` 한 줄이라 **피아노 화면의 일부**다.
피아노가 `guitar` 탭이 아니라 `activity` 탭이라는 것도 여기서 알았다.

---

## 세션 50 — 드럼 전수 32건 (고침 5 · 0절 1 · 0-2절 26 + 재점검 4)

**사람이 손으로 만지는 마지막 화면이었다.** 범위는 `InteractiveDrumSet`(885 → 895) ·
`drum/index.tsx`(1222 → 1277) · `DrumGameOverScreen`(273) · `drum/_layout`(5) +
UI가 아닌 `hooks/useDrumLogic.ts`(218 → 242) · `constants/drumLayouts.ts`(121) — **2,724줄**이다.

**고친 다섯은 전부 동작이다.** 대비·접근성·치수는 세션 45~49와 같은 기준으로 미뤘다.

| | 무엇이 | 처방 |
|---|---|---|
| **1** | `useDrumLogic`의 다음 라운드 타이머 3개가 아무 데도 안 담겨, 「그만하기」·탭 이탈 뒤에도 **끝난 퀴즈가 혼자 다음 문제로 넘어갔다** | `roundTimersRef`(Set) + `scheduleRoundTimer`. 정답·오답·시간초과 세 갈래의 같은 꼬리를 `scheduleNextRound` 하나로 묶었다 |
| **2** | 점수를 `newScore = score + 1`로 **렌더 클로저에서** 더했다 | `scoreRef` + 유일한 쓰기 통로 `applyScore`. `onGameComplete`는 **터지는 시점의** `scoreRef`를 읽는다 |
| **3** | 카운트다운이 취소되지 않아 도중에 그만둬도 **접은 퀴즈가 `playing`으로** 들어갔다 | `quizRunIdRef` 표 + `quizWaitersRef`(끊을 때 `clearTimeout`과 함께 `resolve`). 접는 길 넷을 **`exitQuiz` 하나로** 모았다 |
| **4** | `Animated.loop` 둘에 정지 참조가 없어, 대기 시간이 지난 뒤 언마운트되면 루프가 **영영 돌았다** (FlatList에 네 벌 산다) | 루프를 바깥 변수에 담아 cleanup에서 `stop()` |
| **5** | 게임 종료 결과창이 조건부 `View`+`zIndex: 2000`이라 뒤로가기가 **탭을 나갔다** | `Modal`(`transparent`·`statusBarTranslucent`·`onRequestClose`). 뒤로가기는 「나가기」와 같은 길 |

> **1·2는 `useDrumLogic.ts`에 있었다.** 화면 이름이 붙은 파일이 아니라 훅이다 —
> **화면 묶음은 `import`를 따라가야 한다**(세션 49 `activity` 탭과 같은 교훈).
>
> **1·2·3·5는 세션 49 피아노의 2·4·2·19와 같은 결함이다.** 골격이 닮지도 않았는데
> 같은 손버릇이 나왔다. **타이머를 안 담고, 클로저로 더하고, 창을 `View`로 만드는 것**은
> 화면과 무관하게 반복된다.

**「그대로」로 정해진 것이 하나 늘었다** — 드럼 위 **마커를 눌러도 소리·제출이 없는 것**(6번).
**0-2절에는 30건이 갔다** — 안 고른 26(🔧 23 · ❓ 1 · 📱 2) + **재점검 4**.

### 이번에 걸리지 **않은** 것

**기타 1번(누를 자리 없는 음 출제)을 드럼에서 세었더니 0건이다.** `useDrumLogic`의
`availableInstruments`(2~5)가 `drumLayouts`의 `order`와 **네 페이지 모두 일치**한다.
채점 연타 빗장 `isAnsweringRef`도 **이미 있었다**(동작 2 통과) — 기타·피아노에는 없던 것이다.
`maxScore` 0 나눗셈은 `clampedMaxScore`가 막고, 오답 문구 `'정답!'` 판별도 정확하다.
`shadow*`·`Platform.OS === 'ios'`도 0건이다.

### 재점검 4건 — 넷 다 이번 수정이 들인 것

`score`를 state와 ref 두 곳에 든다 · 「시작 중」 표가 `isStartingQuizRef`와 `quizRunIdRef`
**둘이 됐다** · `stopPendingRounds`가 이름보다 많은 일을 한다 · 결과 `Modal`의
`statusBarTranslucent`(📱). **규칙 5대로 고치지 않고 0-2절로 보냈다.**

> **세션 47·48·49에 이어 네 번째로 「고치며 들인 것」이다.** 그리고 `score` 이중 보유는
> **기타(48)·피아노(49)에 이어 세 번째**다 — 0-2절이 손버릇 목록이라는 증거가 하나 더 늘었다.
>
> **다만 5번은 그 목록을 처음으로 피했다.** 세션 47·49가 「`Modal`로 감쌌는데 안에
> `absolute`가 남았다」를 두 번 0-2절에 올렸기 때문에, 이번에는 `gameOverOverlay`를
> **바꾸는 김에 `flex: 1`로** 고쳤다. **보류 목록을 읽고 시작하면 같은 줄을 안 만든다.**

**탭 이탈 동작이 바뀐다.** 전에는 퀴즈 모드가 유지된 채 라운드만 혼자 넘어갔다.
`stopAllSounds()`로 소리를 끊는 이상 퀴즈는 이어질 수 없어 **함께 접는 쪽**으로 정했다 —
안 그러면 3번을 고친 뒤 돌아왔을 때 **답을 못 내는 상태로 굳는다.**

---

## 세션 51 — 앱 뿌리 전수 18건 (고침 6 · 0절 1 · 0-2절 11 + 재점검 2)

**한 번도 안 본 것 중 가장 큰 파일이었다.** 범위는 `app/_layout.tsx` **하나**다 —
`import`가 가리키는 셋(`StarProvider`·`ClearProvider`·`AudioManagerProvider`)은
`context/`라 UI가 아니다. **묶음이 한 파일로 끝난 첫 점검**이다.

**이 파일은 두 겹이었다.** 1~134줄이 옛 `RootLayout` 전체의 주석 사본이고,
139줄부터가 현재 판이다. **349줄 중 134줄(38%)이 죽은 코드**였다.

| | 무엇이 | 처방 |
|---|---|---|
| **1** | 주석으로 남은 옛 `RootLayout` **134줄** | 지웠다 (219줄로). 0절의 `DEBUG_DROP`·`useSyncGameData`와 **다르다** — 저건 되살릴 스위치고 이건 대체된 사본이다 |
| **2** | 이름 `Modal`에 `onRequestClose`가 없어 **뒤로가기가 아무 반응도 없었다** | `onRequestClose={handleSaveName}`. **동작이 바뀐다** — 이름을 꼭 받는 관문이라 닫지 않고 **저장 시도로** 잇는다 |
| **3** | 터치 1개(「시작하기」)에 `Role`·`Label` 없음. 파일에 `accessibility` **0회** | `accessibilityRole`·`Label`·`Hint`. **첫 실행에 반드시 지나야 하는 관문**이었다 |
| **5** | 흰 글자 on `#007AFF` **4.02:1** (16px bold는 4.5 기준) | `#0069D9` **5.22:1** |
| **6** | placeholder `#aaa` on `#f5f5f5` **2.13:1** | `#666` **5.27:1** |
| **7** | 입력창 테두리 `#eee` vs 카드 `#fff` **1.16:1** (안쪽 `#f5f5f5`는 1.09:1이라 테두리가 유일한 경계) | `#8A8A8A` **3.45:1** (안쪽 대비도 3.17:1) |

> **`#767676`을 쓰려다 계산에서 걸렀다.** 흰 배경 4.54:1이라 습관처럼 고르는 값인데
> **`#f5f5f5` 위에서는 4.17:1**이다. 6번은 흰 배경이 아니라 회색 입력창 위였다 —
> **바탕을 확인하지 않고 「검증된 회색」을 옮기면 미달을 새로 만든다.**

> **2번은 「없으면 크래시」가 아니다.** RN 0.86 `ReactModalHostView.kt:291`에
> `checkNotNull(onRequestCloseListener)`가 있어 던질 것처럼 보이지만,
> `ReactModalHostManager.kt:96`의 `addEventEmitters`가 **prop과 무관하게 리스너를 늘 건다.**
> 이벤트는 JS까지 오고 `Modal.js:337`이 `undefined`를 넘겨 **거기서 버려진다.**
> **네이티브 소스를 끝까지 따라가야 「아무 일도 안 한다」와 「터진다」가 갈린다.**

### 이번에 걸리지 **않은** 것

`shadow*`·`Platform.OS === 'ios'` **0건**. 죽은 `import` 0건이고 `styles` 4개
(`container`·`background`·`imageContainer`·`image`)는 전부 쓰인다.
`modalTitle` **12.63:1** · `modalText` **5.74:1**로 글자 둘은 통과다.
동작 3(클로저로 더하기)·5(출제/시각)는 이 파일에 해당이 없다.

### 재점검 2건 — 둘 다 이번 수정이 들인 것

뒤로가기 연타로 `Alert`가 겹친다(2번이 `handleSaveName`에 진입점을 하나 더 냈다) ·
`TextInput`에 `accessibilityLabel`이 없다(3번이 버튼만 다뤘다).
**규칙 5대로 고치지 않고 0-2절로 보냈다.**

> **세션 47·48·49·50에 이어 다섯 번째로 「고치며 들인 것」이다.**
> 그리고 재점검 두 건이 **각각 고친 항목의 바로 옆자리**다 —
> 8번(연타 빗장)을 함께 안 골랐기에 2번이 그 구멍을 하나 더 열었다.
> **동작 수정은 진입점을 늘리므로, 빗장 줄과 짝지어 고르는 편이 맞다.**

### 이력이 틀렸던 것

표의 「터치 3」은 `grep -c`가 센 **줄 수**였다 — `import` 줄 + 여는 태그 + 닫는 태그다.
**실측은 터치 1개**다. 세션 47~50에 아홉 파일이 같은 이유로 줄었고 이번이 열 번째다.

---

## 세션 52 — learn 묶음 전수 24건 (고침 4 · 0-2절 20 + 재점검 4)

**「부분」으로 남아 있던 마지막 화면 묶음이다.** 범위는 UI 셋(`learn/index.tsx` 368 ·
`WordGame.tsx` 429 · `learn/_layout.tsx` 2)에 딸린 로직 셋(`useWordGameLogic` 161 ·
`useWordAudioPlayer` 246 · `useStopAudioOnBlur` 46) — **1,252줄**이다.
`DrumGameOverScreen`(273)은 세션 50 전수 뒤 안 바뀌어(`git diff 818bb9d HEAD`가 비었다) 건너뛰었다.

**고른 넷이 전부 동작 절이다.** 45~50에 이어 이번에도 사용자가 고른 것은 동작이었다.

| | 무엇이 | 처방 |
|---|---|---|
| **1** | 채점 빗장이 state(`gameState !== 'answered'`)라 **같은 프레임의 두 번째 탭이 통과**했다. 두 선택지를 동시에 눌러도 둘 다 채점됐고 600ms 타이머가 2개 걸려 라운드가 2칸 뛰었다 | `isGradingLockedRef` — `startNewRound`에서 푼다 (기타 48 · 피아노 49와 같은 처방) |
| **2** | `newScore = score + 1` 렌더 클로저 더하기. 1번과 겹치면 두 번 맞혀도 1점 | `scoreRef` **하나로만** 든다. 반환값 `score`는 읽는 곳이 0이라 함께 뺐다 |
| **3** | 600ms 타이머를 아무 데도 안 담아 종료·난이도 변경·언마운트 뒤에 도착했다 | `nextRoundTimerRef` + `clearNextRoundTimer` (언마운트 · `resetGame` · `resetGameWithoutStarting`) |
| **4** | 결과창이 조건부 `View` + `zIndex: 2000`이라 **뒤로가기가 탭을 나갔다** | `Modal` + `onRequestClose={handleGoHome}`, 절대배치를 걷고 `flex: 1` |

> **2번에서 처음으로 「state와 ref 두 곳」을 안 만들었다.** 기타(48) · 피아노(49) · 드럼(50)은
> 셋 다 `applyScore` 꼴로 고쳐 **0-2절에 같은 줄이 세 번** 올라갔다. 여기서는 점수를
> 그리는 곳이 없어 **ref 하나로 끝냈다** — 「렌더에서 안 읽으면 ref」를 끝까지 민 것이다.
> 세션 50이 `flex: 1`을 함께 고친 것과 같은 종류의 이득이고, **둘 다 0-2절을 읽고 시작해서** 나왔다.

> **4번은 「사본만 안 고친 자리」다.** 드럼과 learn이 `DrumGameOverScreen`을 함께 쓰고
> 감싸는 스타일 이름(`gameOverOverlay`)까지 같은데, 세션 50에는 드럼 파일만 봤다.
> **공용 컴포넌트를 고칠 때 「누가 더 쓰나」를 세면 같은 세션에 끝났다** —
> 이력의 「여러 화면이 함께 쓰는 것」 표가 그 답을 갖고 있었다.

### 이번에 걸리지 **않은** 것

`shadow*` · `Platform.OS === 'ios'` **0건**(6절). 난이도 버튼 폭은 `LAYOUT`이
화면폭에서 역산해 **320dp에서도 안 넘친다** — 세션 36에 이미 고친 자리다.
테두리는 늘 두고 색만 바꾼다(4절 통과). `useStopAudioOnBlur` 0건이고,
`useWordAudioPlayer`는 재생 ID · 백업 타이머 · 에러 시 캐시 축출까지 돼 있어 **0건**이다
(flashcards와 공용이라 다음에 그 묶음에서 다시 만난다).

### 재점검 4건 — 둘은 이번 수정이 들인 것, 둘은 그 옆자리

`statusBarTranslucent`가 인셋을 어떻게 덮나(📱, **드럼 50 재점검과 같은 줄**) ·
뒤로가기가 「나가기」로 이어졌는데 **그 「나가기」가 홈으로 안 간다**(21번을 안 골랐다) ·
빗장을 푸는 자리가 둘로 나뉘어 **마지막 라운드에서는 잠긴 채 끝난다** ·
라운드 타이머가 **탭 이탈에는 안 끊긴다**(탭은 언마운트되지 않는다).
**규칙 5대로 고치지 않고 0-2절로 보냈다.**

> **세션 47~51에 이어 여섯 번째로 「고치며 들인 것」이 나왔고, 51과 같은 모양이다** —
> 재점검 둘이 **각각 고친 항목의 바로 옆자리**다. 4번이 뒤로가기라는 진입점을 21번(안 고친 항목)에
> 이었고, 1번이 만든 빗장은 푸는 자리가 3번의 타이머와 갈렸다.
> **동작 수정은 진입점을 늘린다 — 세션 51의 문장이 그대로 다시 맞았다.**

### 점검 뒤 사용자가 하나 더 골랐다 — 「나가기」를 뺐다 (`e9b2061`)

21번(「`handleGoHome`이 홈으로 안 간다」)과 그 짝인 재점검 줄을 **0-2절에서 지웠다.**
learn 탭은 **전체가 퀴즈라 접고 돌아갈 화면이 없다** — 버튼이 하는 일이 「다시 하기」와 같았다.

**공용 컴포넌트라 버튼을 지우지 않고 선택으로 바꿨다.** `DrumGameOverScreen`의
`onGoHome`을 `?`로 두고 **안 넘기면 「나가기」를 그리지 않는다.** 드럼은 그대로 넘긴다 —
거기 「나가기」는 퀴즈를 접고 **연주로 돌아가는** 진짜 출구다. `app/(tabs)/drum/`은 안 건드렸다.
뒤로가기(`onRequestClose`)는 `handleRestartGame`으로 옮겼다 — **닫고 다시 하기**다.

> **같은 컴포넌트인데 두 호스트에서 버튼의 뜻이 달랐다.** 세션 52에 4번을 고칠 때는
> 「드럼과 같은 처방」으로 맞췄는데, 정작 **드럼에만 있는 것은 돌아갈 화면**이었다.
> 공용 부품은 골격을 맞추기 전에 **호스트마다 그 동작이 있는지**부터 본다.

### 이력이 틀렸던 것 둘

표의 `learn/index.tsx` 「터치 5」는 `grep -c`가 센 줄 수였다(`import` + 여는 태그 2 +
닫는 태그 2). **실측은 2개**다 — 세션 47~51에 열 파일이 같은 이유로 줄었고 **이번이 열한 번째**다.

**인계문 8의 「`Waveform`(140)도 이 묶음이 쓴다」도 틀렸다.** `Waveform`을 부르는 것은
`WordFlashcard`(flashcards)뿐이고 learn 묶음은 쓰지 않는다. **묶음은 `import`로 확인한다.**

---

## 세션 53 — 탭 골격 전수 14건 (고침 5 · 0절 2 · 0-2절 7 + 재점검 2)

`app/(tabs)/_layout.tsx` 212줄. **점검 커밋이 `?`라 「그 뒤 바뀌었나」를 기계로 못 갈랐고,
그래서 전수로 다시 훑었다.** 세션 26·34에 본 것은 화면 방향 한 자리뿐이었다.

**이 파일은 근거를 `node_modules`에서 읽어야 했다.** 화면 코드가 아니라 **라이브러리에
넘기는 설정**이라, 「무엇이 어떻게 되는가」가 `@react-navigation/bottom-tabs 7.8.4`
소스에 있었다. 발견 열넷 중 넷이 그렇게 나왔다.

| | 무엇이 | 처방 |
|---|---|---|
| **1** | `lockAsync`가 취소되지 않는데 effect에 cleanup이 없었다 | `lockRunIdRef` 표를 두고 끝날 때마다 대조 |
| **2** | `.catch(() => { })`가 방향 잠금 실패를 통째로 삼켰다 | 경로와 함께 `console.warn` |
| **3** | 튐이 `onPressOut`이라 **취소한 누름에도** 튀고, 연타하면 시퀀스가 겹쳤다 | `onPress`로 옮기고 `stopAnimation()` + `setValue(1)`, 언마운트 정리 |
| **7** | **탭 7개 전부 토크백이 읽을 것이 없었다** | `tabBarAccessibilityLabel` 7개 |
| **13** | JSX 속성 사이에 낀 학습용 주석 3덩어리 | 걷었다 |

> **7번이 이 세션에서 가장 큰 것이다.** 라벨이 일곱 다 `() => null`이고 아이콘은
> 글리프 `<Text>`뿐인데(`createIconSet.js:79`), `bottom-tabs`가 라벨로 접근성 이름을
> 만드는 길은 **`Platform.OS === 'ios'` 조건**이었다(`BottomTabBar.js:292`).
> **안드로이드 전용 앱(규칙 4)이라 그 길이 한 번도 안 돌았다** — iOS 분기를 안 넣는 것과
> **iOS 분기에만 있는 기능이 빠지는 것**은 다른 이야기다.
>
> **`role='tab'`·`aria-selected`는 원래 왔다.** `filteredRestProps`가 그대로 넘겨
> 「선택됨」은 읽혔는데 **무엇이 선택됐는지가 없었다.** 「접근성 0개」로 보이던 자리가
> 실은 「이름만 없는 자리」였다 — **`grep accessibility`가 0이어도 라이브러리가 넣는 것이 있다.**

### 「그대로」가 둘 나왔다

`speed: 200`(기본 12)과 `backBehavior="history"`(기본 `firstRoute`). **둘 다 기본값에서
벗어난 설정인데 사용자가 「그대로」를 골랐다** — 0절로 올렸다. 세션 48·50·51에 이어
네 번째로 0절이 늘었다.

### 이력이 틀렸던 것 둘

표의 「터치 3」은 `grep -c`가 센 줄 수였다(`import` + 여는 태그 + 닫는 태그).
**실측은 `Pressable` 하나**이고 그것이 탭마다 7번 그려진다 — 세션 47~52에 열한 파일이
같은 이유로 줄었고 **이번이 열두 번째**다.

**머리 문단의 「44개 · 14,683줄」도 틀렸다.** 세션 52 시점의 실제 합은 **14,633**이라
**50줄 어긋나 있었다.** 화면별 표의 파일별 줄 수를 더한 값이 원본이므로 머리를
**14,662**(탭 골격 212 → 241 반영)로 고쳤다.

---

## 세션 54 — 단어 카드 안 전수 17건 (고침 6 · 0-2절 11 + 재점검 1)

`components/game/WordFlashcard.tsx`(346 → **328**) + `Waveform.tsx`(140 → **80**) +
딸린 `waveformData.ts`(13). **점검 커밋 `dcbe37b`가 있고 그 뒤 두 파일 다 안 바뀌었는데도 훑었다** —
그때 범위가 「부분」(치수·재생 강조)이라 **건너뛰기 판정에 걸리지 않는다.**
「안 바뀜」은 **전수를 한 뒤에만** 건너뛸 근거가 된다.

**고른 여섯 중 넷이 죽은 코드였다.** 45~53에는 사용자가 고른 것이 거의 늘 동작이었는데
여기는 동작이 **0건**이었다 — 그래서 고를 것이 죽은 코드 쪽으로 몰렸다.

| | 무엇이 | 처방 |
|---|---|---|
| **1** | `minimal` prop을 **아무도 안 넘긴다.** 유일한 사용처가 `wordPair`만 준다 | prop·설명 3줄·VS 분기 둘·`vsContainerSpacer`·`vsSpacerMinWidth`를 걷었다 |
| **2** | `Waveform`의 `isPlaying`·`fallbackProgress`·그 `useEffect`가 죽었다 — 호출자가 늘 `progress`를 넘겨 `if (progressProp) return;`에서 매번 되돌아갔다 | `progress`를 필수로. import 넷을 걷었다 |
| **3** | **Canvas가 담는 상자보다 21px 컸다** — `waveformHeight`(60) vs `waveformContainerHeight`(39) − padding 20 | 자리를 차지하던 쪽(39)을 남겨 **한 토큰으로 합쳤다.** `AMPLITUDE_RATIO` 0.35 → 0.5로 칠하는 높이를 맞췄다 |
| **5** | `getImmutableWaveformData`를 렌더마다 불러 새 배열을 줬다 → `useDerivedValue` 의존성이 매 렌더 바뀌어 **mapper가 정지·재시작**됐다 | `useMemo` |
| **6** | `index <= visibleBars` — 진행도 0에서도 첫 막대가 서 있었다 | `<` |
| **16** | `Waveform` 140줄 중 **61줄이 주석**이고 `progress` 하나에 설명 블록이 3개. `minimal`에는 `better name? …?` 질문이 코드에 남아 있었다 | 뜻이 남는 것만 두고 걷었다. 매직 넘버 둘은 이름 있는 상수로 |

### 동작 0건 — **소스를 보고 없앤 것이 둘이다**

`playAllTimerRef`가 언마운트에서 정리되나: `useStopAudioOnBlur`는 `useFocusEffect`로 걸려 있고
그 훅은 **언마운트 시에도 클린업을 부른다**(`@react-navigation/core/src/useFocusEffect.tsx:104-111`).
「전체 듣기」 연타: `useWordAudioPlayer`의 `playbackIdRef`가 앞 체인의 `onComplete`를 무효화해
**타이머가 겹치지 않는다.** 둘 다 코드만 보면 결함처럼 읽히는 자리였다.

### 재점검 1건

3번을 고치며 그려지는 파형이 **33.6 → 31.2px**로 2.4px 줄었다(411dp). 계수로 맞췄지만
같아 보이는지는 눈으로 봐야 한다 — 0-2절에 📱로 올렸다.

### 0-2절에 올린 것 중 하나는 **점검표 자신을 가리킨다**

`playAllButtonDisabled`가 2절의 「정해진 답」(`grayLight` + `textSecondary` + `elevation:0`)
그대로인데 **그 조합이 4.35:1**이다. 한 파일의 문제가 아니라 **처방을 다시 봐야 하는 줄**이라
그렇게 적었다.

---

## 세션 56 — 만점 콘페티 재적용 (`DrumGameOverScreen` 전수 0건)

세션 49~50에 넣었다가 다시 클론한 뒤 빠진 콘페티를 `doc/confetti-재적용.md` 순서로 되살렸다.
`confetti.riv`는 `res/raw`에 있고(아트보드 `Confetti` · SM `State Machine 1` · 타임라인 `Burst`),
코드는 만점일 때만 `PerfectConfetti`를 깐다. 컨테이너 `absoluteFill`, 콘페티 `elevation: 8` ·
`pointerEvents="none"`.

전수 다시 돌렸다. **새 발견 0건.** 0-2절의 대비·게이지 라이브리전은 그대로라 다시 안 올렸다.
점검 커밋은 **미커밋이라 `?`**.

같은 세션에 듣기연습 첫 문제 안내를 `drum/index.tsx` 헤더 아래에 넣었다. 전수 다시 돌렸다. **새 발견 0건.**
0-2절 드럼 항목은 다시 안 올렸다. 점검 커밋은 여전히 `?`.

---

## 세션 57 — 듣기연습 진입 시 캐릭터 초기화 (드럼 두 파일 전수, 새 발견 1건)

연주모드에서 마지막에 친 악기 위에 캐릭터가 앉은 채로 듣기연습에 들어가, 첫 문제가 그 악기면
**정답 자리에 이미 서 있던 것**을 고쳤다. 라운드 사이에는 `moveToNeutralPosition`이 비켜 주는데
**진입 첫 문제에만 그 처리가 없었다.** 고른 것은 「초기 위치로 리셋 + 선택 해제」(중립 이동·마커만
끄기 두 안 중에서). 마운트 때 쓰던 초기 좌표 계산을 `computeInitialCharacterPosition`으로 빼서
`InteractiveDrumSet.resetToInitialPosition` → `HorizontalDrumScroller.resetCharacterToInitial` →
`handleStartQuiz`가 카운트다운 전에 부른다. `setValue`가 아니라 `spring`이라 카운트다운 동안
돌아가는 움직임이 보인다.

두 파일 다 전수 다시 돌렸다. **새 발견 1건** — 선택 해제가 부모의 악기명 레이블
기억(`instrumentByPage`)까지 비운다. **사용자가 그 자리에서 「그대로」를 골라 0절로 올렸다** —
「이름은 캐릭터가 올라간 악기만 가리킨다. 퀴즈를 나가면 캐릭터가 악기 위에 없으니 레이블이
없는 게 맞다.」 **부작용이 아니라 레이블의 정의였다.** 0-2절 드럼 항목 스물 남짓은 다시 안 올렸다.
**0-2절 「캐릭터 초기 위치의 기준이 존재하지 않는 버튼이다」(세션 50)의 무게가 커졌다** —
마운트 때 한 번 쓰던 좌표를 이제 **퀴즈 시작마다** 쓴다. 그 줄은 그대로 두되 여기에 적어 둔다.
점검 커밋은 **미커밋이라 `?`**.

---

## 세션 57(2) — 골격 여섯 전수 11건 (고침 0 · 0-2절 11)

`+not-found`(64) · `app/index`(5) · `flashcards/_layout`(5) · `new/_layout`(36) ·
`new/(games)/_layout`(58) — 합 **168줄**. **한 번도 안 본 것이 이걸로 다섯 나갔다.**

**가장 큰 것은 파일 하나가 아니라 한 쌍이다** — `new/_layout`이 빈 헤더 `View`에
`paddingTop: insets.top`을 주는데 `new/index:117`이 또 더해 **상단 인셋이 두 번**이다.
같은 구조인 `(games)` 쪽은 화면이 안 더해 정상이라, **부모를 안 열면 갈리지 않는 자리**였다.

**동작 절 0건** — 타이머·`await`·채점 빗장·닫는 창이 여섯 파일에 아예 없다.
세션 54에 이어 두 번째다. 고를 것이 접근성·대비·죽은 코드로 몰렸다.

`app/index.tsx`는 `Redirect` 한 줄이라 **0건**이다. 그래도 표에 넣는다 —
「안 봤다」로 보이면 안 되기 때문이다(이 문서를 만든 이유).

**터치 수를 정정했다** — `+not-found`는 `grep -c`가 3이었지만 **실측 1개**다
(import + 여는 태그 + 닫는 태그). 세션 47~54의 열두 번과 같은 정정이고,
이걸로 **「한눈에」의 터치 합이 118 → 116**이 됐다.

**「한 번도 안 봤다」가 7 → 2가 됐다** — 남은 것은 `WaveRipple`(119) ·
`RiveAnimalGame`(72)뿐이고 **둘 다 터치 0 · `Role` 0**이다.
점검 커밋은 **미커밋이라 `?`**.

---

## 세션 58 — `WaveRipple` · `RiveAnimalGame` 전수 10건 (고침 6 · 0-2절 6)

**마지막 둘이다.** 합 191줄(→ 206줄)이고 **터치 0 · `Role` 0**이라 순서에서 늘 뒤로 갔던 것들인데,
**사용자에게 닿는 결함은 여기서 나왔다.**

**모션이 조용히 빠지는 자리** — 카드를 누르면 그때 Rive가 마운트되고 `matchGame`이
정답 120ms / 오답 50ms 뒤 `fireState`를 **한 번** 쐈다. 상태머신이 그 전에 안 붙으면
`riveRef.current?.fireState(...)`가 **아무 일도 안 하고 에러도 안 낸다.** 느린 기기일수록 빠진다.
`RiveAnimalGame`이 `pendingTriggers`에 담았다가 `onPlay`에서 쏘게 고쳤고,
호출 쪽 지연은 **마운트 한 틱**(`setTimeout(trigger, 0)`)만 남겼다.

**주석이 없는 안전장치를 말하고 있었다** — `matchGame:268`이 「ref가 늦게 잡히는 기기 대비
**1회 재시도를 둡니다**」였는데 **재시도 코드는 없었다.** 시간 하나로 때우고 있었다.

**죽은 공개 API** — `setAnimalIndex`가 인터페이스·구현·`useImperativeHandle` 세 자리에 있었지만
**부르는 곳이 저장소에 0개**다. 동물 지정은 `initialAnimalIndex` → `onPlay` 한 길만 쓴다.

**주석이 코드와 어긋나 있었다** — `WaveRipple`의 stagger가 「30프레임 = 1000ms」였는데
실제는 `duration/count` = **375ms**다. 주석을 믿고 1000ms로 고치면 8개가 3주기로 겹쳐
**원이 3개로 보인다.** 값이 아니라 **주석이 다음 사람을 틀리게 하는** 자리였다.

**동작 절 1건** — 세션 54·57(2)의 **0건 두 번** 뒤에 다시 나왔다. 다만 6종류(타이머·채점 빗장·
클로저·`await` 앞 `set*`·출제/시각·닫는 창) 중 어느 것도 아니고, **「비동기로 붙는 네이티브를
시간으로 기다린다」**는 새 모양이다.

**터치 수 정정 없음 — 세션 47 이래 처음이다.** 두 파일에 `TouchableOpacity`·`Pressable`이
아예 없어 `grep -c`가 0이고 실측도 0이었다. **「한눈에」의 116/51은 그대로다.**

**`?` 아홉을 채웠다** — 세션 56·57이 커밋을 미뤄 생긴 것이고, 커밋이 난 뒤 처음 도는 점검이
이 세션이었다. `bfcaa90`(`DrumGameOverScreen`) · `ebc5926`(`drum/index`·`InteractiveDrumSet`) ·
`f2828a5`(골격 여섯). **표에 `?`가 다시 0이다.**

**「한 번도 안 봤다」가 2 → 0.** 대신 **`matchGame`이 점검 커밋보다 새 파일**이 됐다 —
이 표가 다음에 답할 물음이 「안 본 것」에서 **「바뀐 것」**으로 넘어간 첫 줄이다.

---

## 세션 59 — `matchGame` 재점검 1건 (고침 1) · **범위를 틀렸다가 되돌렸다**

**전수가 아니라 diff가 입력인 첫 점검이었는데, 파일 전체를 다시 돌렸다.**
`fac2130` 뒤로 바뀐 것은 세션 58의 트리거 호출부 한 줄(+주석 셋)뿐인데
일곱 갈래를 처음부터 돌려 11건을 보고했다. **사용자가 범위를 되돌렸다** —
그중 이번 범위는 **1건**이고, 나머지 열은 「옛 전수가 안 적은 자리」라
**0-2절에도 올리지 않고 버렸다.** 잘못된 전수가 보류 목록이 되면
다음 세션이 그것을 근거로 삼는다.

> 규칙 원문은 사용자가 [`.claude/commands/점검.md`](../.claude/commands/점검.md) 0번과
> 이 문서 「다시 훑어야 하나」에 적었다 — **바뀐 줄과 그 호출 길만** 본다.

**고친 1건 — 복구 타이머가 세션 58이 살린 모션을 다시 잘랐다.**
58이 트리거를 `onPlay`까지 담아 두게 고쳤는데, 카드를 이미지로 되돌리는 1500ms는
여전히 **누른 시각**부터였다. 상태머신이 T ms 뒤에 붙으면 모션은 **1500−T**만 돈다.
`RiveAnimalGame`이 **쏜 순간**(`onFired`)을 알려 주고 거기서 다시 1500ms를 잰다.
누를 때 거는 타이머는 **끝내 못 쏠 때의 바닥**으로만 남겼다 — 그 기기의 동작은 오늘과 같다.

> **고친 자리가 고친 자리를 만든다.** 58 이전에는 트리거가 조용히 사라져
> 「잘린 모션」이 **있을 수 없었다.** 모션을 살린 커밋이 곧 이 결함을 열었다 —
> 그래서 `git diff`가 가리킨 자리가 정확히 맞았다.

**완료 신호는 열지 않았다.** 「모션이 끝났다」를 받으려면 `RiveAnimalGame`에 새 API가
필요하고 그건 이번 범위가 아니다 (사용자 판단).

**재점검(규칙 5) 0건.** 새로 쓴 길만 다시 봤다 — 늦게 온 알림은 `hasRestored`가 막고,
바닥 타이머를 걷은 뒤 남는 id는 `clearTimeout`이 이미 끝낸 것이라 아무 일도 안 한다.

**실기기에서 갈렸다 — 세션 58·59치가 한 번에 나갔다.** 「오답이 바로 붙는 것(58) — 좋다」 ·
「첫 칸이 끝까지 도는 것(59) — 문제 없다」. **둘 다 계산으로는 못 끝내는 체감 항목**이었고
같은 커밋 줄기라 기기 하나로 함께 확인됐다 (인계문 10 「다음 작업」 3번).

**터치/`Role`은 안 건드렸다.** `grep -c` 11이지만 실측은 5개(전부 `Role`·`Label` 있음,
나머지 셋은 `header`)다 — 다만 **그 실측은 범위 밖 전수에서 나온 값**이고
이번 diff로 터치가 바뀐 것도 아니라 표와 「한눈에」를 그대로 뒀다.

---

## 세션 60 — learn 「그만하기」 (기능 작업 · 점검 5건 · 고침 1)

**점검이 아니라 기능 작업이었다.** 설계 원문은 사용자가 쓴
[`doc/learn-그만하기.md`](./learn-그만하기.md)이고, 「그대로 구현해」가 지시였다.
손댄 곳은 `learn/index.tsx` · `WordGame.tsx` · `useWordGameLogic.ts` ·
`DrumGameOverScreen.tsx` + `constants/layout.ts`(토큰 하나)다.

**규칙 5로 훑어 5건을 보고했고 사용자가 2번 하나를 골랐다.**
설계 문서의 표는 푼 수를 셀 때 `answered`도 이번 라운드로 셌는데,
그 상태는 **선택지가 떠 있고 아직 안 고른** 자리라 3문제를 풀고 4번 문제에서 접으면
`3/4`가 됐다. **채점이 끝난 `waitingForNextRound`만** 세도록 한 줄을 고쳤다.

> **설계 문서와 코드가 갈린 자리다.** 문서의 표는 그대로이고
> 고른 것은 사용자다 — 문서를 고칠지는 사용자 몫으로 남겨 뒀다.

**나머지 넷은 0-2절로 갔다** — 그만하기 줄이 세로 60px를 먹는 것(📱) ·
한 문제도 안 풀고 접으면 `0/0`인 것(❓) · 조기 종료가 채점 빗장을 안 거는 것(🔧) ·
`stopSound()` 프라미스를 안 기다리는 것(🔧).

**설계에 없던 것 하나를 함께 막았다** — `0/0`이면 `score === maxScore`가 참이라
**콘페티가 깔리고 「완벽해요!」**가 떴다. `DrumGameOverScreen`의 만점 판정에
`maxScore > 0`을 더했다. **조기 종료가 연 자리**라 이번 범위로 봤다.

**재점검(규칙 5) 0건.** 고친 것이 삼항 한 줄이라 새로 열린 길이 없다.

**터치가 116 → 117이 됐다.** 새 터치(「그만하기」)에 `Role`·`Label`을 함께 붙였다 —
`WordGame`의 조작 넷이 `GestureDetector`라 터치로 안 잡히는 것과 갈린다 (0-2절).

## 세션 61 — 게임 넷 공용 「나가기」 (기능 작업 · 점검 5건 · 고침 0)

**기능 작업이다.** 지시는 「네 화면에 각각 넣지 말고 `(games)/_layout.tsx`에
**나가기 버튼 하나**를 올려라」였다. 새 파일 `components/GameExitButton.tsx`(90) +
`(games)/_layout.tsx`(58 → 63) + `constants/layout.ts` 토큰 7개다.
**게임 네 화면은 한 줄도 안 건드렸다** — 그게 이 방식을 고른 이유다.

> **기존 부품을 쓰지 않은 것도 판단이다.** `LandscapeBackButton`은 fallback이
> `/(tabs)/drum`이라 게임에서 히스토리가 없으면 **목록이 아니라 드럼으로 떨어지고**,
> 탭바가 숨겨진 **가로** 화면용이라 반투명 흰 판 + 흰 아이콘이다. 몸이 `Ionicons`
> 하나뿐이라 글자를 넣으려면 어차피 갈라진다. **근거는 커밋 `27ba6db` 본문이 갖는다.**

**규칙 5로 훑어 5건을 보고했고 사용자가 「지금은 고치지 마」를 골랐다 — 다섯 다 0-2절로 갔다.**
1~4는 📱(통계 「홈으로」 320dp 14dp 겹침 · `orderGame` 제출 411dp 4dp ·
드래그 `elevation: 1000` · `matchGame` 격자 모서리), 5는 🔧(`GameHeader`가 본문 안).

> **5번은 세션 57의 판정을 뒤집는다.** 그때 0-2절에 「`new/_layout.tsx:23`은 인라인
> `header`인데 옆 파일 `(games)/_layout.tsx`는 `GameHeader`로 빼 뒀다 — 짝이 어긋난다」고
> 적었다. 이름은 뺐지만 **자리가 컴포넌트 본문 안**이라 렌더마다 새 타입이 되는 것은 같다.
> **짝이 어긋난 게 아니라 둘 다 같은 결함이었다.**

**만들면서 고친 것이 하나 있다 — 점검 발견이 아니다.** 테두리를 `borderGray`(#BDBDBD)로
잡았더니 흰 판·`#f5f5f5` 양쪽에서 **1.88:1**이었다. 판이 흰색이면 경계가 유일한 구분인데
안 보인다 — `textSecondary`(5.2~5.7:1)로 바꿨다. 「밝은 배경에서 보이게」가 **지시**였으므로
규칙 5의 「발견은 고치지 않는다」가 아니라 그 지시의 이행으로 봤다.

**재점검(규칙 5) 0건.** 고친 것이 테두리 색 한 줄이라 새로 열린 길이 없다.

**파일이 44 → 45, 터치 117 → 118, `Role` 52 → 53이 됐다.**
새 파일을 **만든 자리에서 함께 훑어** 「한 번도 안 봤다」를 0으로 지켰다.
`accessibilityRole`·`Label`을 함께 붙여 비율은 44% → **45%**다.

## 세션 61(2) — learn 「다시 듣기」가 탭바에 가리던 것 (고침 · 점검 5건 · 고침 0)

**설계 원문은 사용자가 쓴 [`doc/learn-다시듣기-탭바.md`](./learn-다시듣기-탭바.md)다.**
문서에 「구현은 사용자가 시킨 뒤에만 한다」고 적혀 있어, 지시 없이 파일만 열렸을 때는
**고치지 않고 문서를 코드와 대조**했다. 세션 60에서 설계 표의 괄호 하나가 틀려
`3/4`가 뜰 뻔한 자리라 순서를 그렇게 잡았다.

**대조에서 넷이 갈렸고, 그중 둘이 방향을 바꿨다.**

| 갈린 것 | 무엇 |
|---|---|
| **「남은 칸을 본다」를 할 수단이 없었다** | `WordGame` 뿌리 `container`에 `flex: 1`이 없고 화면에 `ScrollView`도 없다. 남은 칸을 아는 경로가 **아예 없었다** — 문서는 원인만 짚고 얻는 방법은 안 적었다 |
| **두 원인이 이미 0-2절에 있었다** | 세션 52의 「탭바가 이미 먹은 자리를 화면이 또 뺀다」가 **이미 「위 `minHeight 400` 넘침에 그대로 얹힌다」고 적고 있었다.** 근거도 세션 53에 확정(`BottomTabBar.js:248`)이라 갈래가 📱이 아니라 🔧였다 — 문서 5번의 「측정 없이 지우지 않는다」와 갈린다 |
| 셋째 `minHeight`가 빠졌다 | `playingContainer`의 300(「듣는 중」). 「손대는 파일」 표에 없었다 |
| `startOffsetY` 상한 | 문서는 「+12~22」인데 태블릿은 28이다 |

**사용자가 둘을 골랐다 — 「64는 되돌리고, 남은 칸은 `flex: 1`로 보라. 실측은 나중에.」**
그대로 넣었다. `minHeight` 셋(`400`·`300`·`contentMinHeight`)을 걷고 `flex: 1` 사슬을
이었으며, 「다시 듣기」의 `translateY`(내림)를 `marginBottom`(올림)으로 뒤집었다.
`contentMinHeight`는 읽는 곳이 없어져 **타입·함수에서 함께 뺐다** (5절).

> **0-2절에서 한 줄이 나갔다.** 세션 52의 「탭바가 이미 먹은 자리를 화면이 또 뺀다」는
> **고쳤으므로 지웠다** — 항목이 나가는 두 길 중 하나다. 같은 절의 「`WordGame` 치수가
> 전부 리터럴」에서 `minHeight:400/300`을, 세션 60의 「그만하기 줄 60px」에서
> 「`minHeight: 400`이라 이미 아슬」을 **근거가 없어졌으므로 고쳐 적었다.**
> **0-2절은 갱신형이다** — 고친 뒤에 낡은 근거를 남기면 다음 점검이 그것을 믿는다.

**규칙 5로 훑어 5건을 보고했고 사용자가 「0-2절에 올려」를 골랐다 — 다섯 다 보류다.**
📱 둘(되돌린 64px가 통째로 버튼 위 여백이 된다 · 320×569는 여전히 ~57px 넘친다),
🔧 둘(하한이 하나도 없어졌다 · `replayOffsetY`와 `startOffsetY`가 이제 같은 일이다),
❓ 하나(`playingContainer`도 바꾼 것이 범위 안인가).

> **1번은 이번 수정의 대가다.** 64를 되돌리면 그 64가 어딘가로 간다 —
> `marginTop:'auto'` 구조에서는 **전부 버튼 위 여백**이 된다. 설계 문서 2번의
> 「보통 폰에서 간격이 크게 안 바뀌게」와 어긋난 것을 **알고 올렸다.**
> 상한(`maxHeight`)을 두면 잡히지만 방금 걷어낸 「창과 무관한 고정값」을 다시
> 들이는 일이다. **실측(`onLayout`)을 「나중에」로 미룬 자리가 정확히 여기다.**

> **3번은 맞바꾼 것이다.** 하한을 걷어 넘침은 풀렸지만 **크기 보장도 함께 사라졌다.**
> 전에는 보장하는 대신 넘쳤고, 지금은 반대다. 세션 58~60의 「기능이 다음 결함을 연다」와
> 같은 모양인데, 여기서는 **고침 자체가 반대쪽 결함을 열었다.**

**`flashcards/index.tsx`는 주석 한 덩어리만 고쳤다.** 「learn이 `insets.bottom +
tabBarHeight`를 쓰는 것과 같은 방식」이 낡았다 — 그쪽 `tabBarHeight`는 시스템 탭바가
아니라 **그 화면이 직접 그리는 하단 네비**(`navBar`의 `height`)다. 코드는 안 바뀌었지만
점검 커밋은 `326320a`로 옮겼다. **다음에 `git diff`가 이 파일을 걸러도 주석뿐임을
여기서 알 수 있어야 한다.**

**재점검(규칙 5) 0건.** 고친 것이 `flex` 사슬과 여백 방향이라, 새로 열린 길은
위 5건에 이미 다 들어 있다.

**터치·`Role`은 안 바뀌었다.** 줄만 24 늘어 **14,953 → 14,977**이다.

## 세션 62 — learn 액션줄 (중단된 구현 이어받기 · 점검 6건 · 고침 0)

**시작이 「@doc/handoff11.md 중단된 작업이 있어 이어서 해줘」였다.** 인계문에는 이 일이
**없었다** — 열려 있는 것은 실기기 확인뿐이었다. 찾은 자리는 **커밋 `cd0a017`(사용자)**이고,
거기 새 설계 [`doc/learn-액션줄.md`](./learn-액션줄.md)와 `constants/layout.ts` 토큰 여섯이
들어와 있었다. 그 토큰이 쓰는 **`scaleActionByWidth`가 정의되지 않아 `tsc`가 6건 났다** —
저장소가 컴파일되지 않는 상태였다.

> **「중단된 작업」은 인계문이 아니라 `git log`와 `tsc`가 답했다.** 인계문은
> **내가 쓴 것까지만** 안다. 사용자가 그 뒤에 커밋한 것은 거기 없다 —
> **이어받기는 `git log -1`부터**다.

**방향이 세션 61에서 뒤집혔다.** 61은 「바닥에 두고 칸을 맞춘다」였고 62는
**「바닥에서 뺀다」**다. 시작·다시 듣기를 난이도 아래 **액션줄**로 올려 탭바 근처에서 치웠다.

| 파일 | 무엇을 |
|---|---|
| `app/(tabs)/learn/index.tsx` | `quitRow` → **액션줄**(`space-between`). 왼쪽은 **칸 하나**이고 상태가 라벨·아이콘·동작을 바꾼다. 왼쪽이 비면 **빈 `View`**를 남긴다 |
| `components/game/WordGame.tsx` | 바닥 버튼 둘 · 래퍼 둘 · 초록 알약 스타일 셋 제거. `WordGameRef`에 `start`·`replay`. `onProgressChange`에 `isPlaying` |
| `constants/layout.ts` | `scaleActionByWidth(하한, 상한, 폭비율)` 정의. `startOffsetY`·`replayOffsetY`를 타입·함수에서 제거 |
| `constants/colors.ts` | `playingBlue`(#4da8de) — `WordGame`에 리터럴이던 값 그대로다 |

**본문은 한 줄도 안 옮겼다.** `handleStartGame`·`handleReplaySound`는 `WordGame`에 그대로
있고 부모는 ref로 부른다 — 세션 60의 「버튼은 부모, 하는 일은 자식」과 같은 골격이다.
`numberOfLines={1}`은 점검 발견이 아니라 **설계의 「한 줄이 두 줄로 접히지 않게 한다」**를
지킨 것이다(`GameExitButton`과 같은 처리).

### 0-2절이 두 번째로 줄었다 — 이번엔 **두 줄**

| 지운 줄 | 왜 |
|---|---|
| 「되돌린 64px가 통째로 버튼 위 여백이 된다」 (61) | `marginTop:'auto'`가 사라졌다. **61이 만든 대가를 62가 걷었다** |
| 「`replayOffsetY`와 `startOffsetY`가 이제 같은 일이다」 (61) | 두 값이 **없어졌다** |
| ~~「`gameContentArea`의 `minHeight: 400` 고정」 (52)~~ | **61에 `flex: 1`이 되며 근거가 사라졌는데 줄이 남아 있었다.** 61이 같은 절의 다른 두 줄은 고치면서 이 줄을 못 봤다 |

**넷은 근거만 고쳤다** — 흰 글자 대비(52)와 선택지 색 셋(52)은 **자리가 `WordGame`에서
`learn/index.tsx`로 옮겨졌고 리터럴이 토큰이 됐다**(색·대비는 그대로), 「조작 4개」(52)는
**둘로 줄었고**, 「320×569」(61)와 「액션줄 60px」(60)는 **수치가 달라졌다.**

> **0-2절을 고치는 데 이번 세션 것보다 지난 세션 것이 많았다.** 코드가 움직이면
> 그 코드를 근거로 적은 줄이 전부 낡는다 — **고친 파일이 아니라 「그 파일을 근거로 삼은 줄」**을 찾는다.

### 점검 6건 — **전부 0-2절(보류)**

🔧 넷(부모 상태가 한 프레임 늦다 · 로딩 중에도 「시작하기」가 눌린다 ·
`contentBottomPadding`이 할 일이 거의 없다 · 「그만하기」 테두리 1.23:1) ·
🔧+계산 하나(비활성 초록 `opacity: 0.6` ≈1.6:1) · 📱 하나(320dp에서 줄이 꽉 찬다).

> **첫 줄이 이 구조의 값이다.** 버튼을 부모로 올리면 **상태가 알림을 거쳐 오므로 한 프레임 늦다.**
> 세션 60에 「버튼은 부모, 접기는 자식」을 고를 때는 안 보였던 대가다 —
> 그때는 「그만하기」 하나뿐이라 늦어도 표가 안 났다. **왼쪽 토글은 상태마다 바뀌므로 표가 난다.**

> **여섯째 줄은 세션 61과 갈린 자리다.** 흰 알약의 테두리를 게임 「나가기」에서는
> `textSecondary`(5.2:1)로 올렸는데, learn 「그만하기」는 `COLORS.border`(1.23:1) 그대로다.
> **같은 모양의 부품이 두 기준을 갖고 있다.**

**재점검(규칙 5)은 따로 돌지 않았다.** 고친 것이 없다 — 여섯 다 보류라
새로 열린 길은 그 여섯에 이미 들어 있다.

**터치·`Role`이 118/53 → 119/54가 됐다.** 줄은 **14,977 → 15,059**다.

## 세션 63 — 냉장고 다시 듣기 위치 (실기기 겹침 · 점검 0건)

실기기(Note10+)에서 플로팅 「다시 듣기」가 **2행 가운데 카드**와 겹쳤다.
설계는 [`doc/냉장고-다시듣기-위치.md`](./냉장고-다시듣기-위치.md).

| 파일 | 무엇을 |
|---|---|
| `app/(tabs)/refri-test/index.tsx` | `bottom: insets.bottom + 20` → `LAYOUT.refriFloatingReplayBottom` |
| `constants/layout.ts` | `refriFloatingReplayBottom` — `(선반 아래 패딩 − 버튼) / 2`. 폰 18 · 태블릿 12 |

시작 버튼은 안 만졌다. `refriBottomInsetOffset`은 그쪽이 아직 쓴다.
0-2절 「`insets.bottom`을 또 더한다」는 **시작만 남았다**고 근거를 고쳤다.

바뀐 줄만 봤다. **새것 0건.** 터치·`Role`·한눈에 줄 수는 그대로다.
~~**점검 커밋은 코드 커밋 뒤에 `f981d38`에서 옮긴다.**~~ → **`406cbb2`로 옮겼다.**

### 63(2) — 교차검증에서 **기준이 둘인 것**이 나왔다 (`406cbb2`)

사용자가 **「교차검증만 해봐」**를 시켰다. 고치지 않고 계산만 했더니
**버튼의 `bottom`은 화면 바닥 기준인데 88(`refriTrayPaddingBottom`)은 선반 안쪽 값**이었다.
둘은 **선반 바닥이 화면 바닥에 닿을 때만** 같은 자리인데, 세로 스택(게이지·냉장고·선반)에
`flex: 1`이 하나도 없어 **남는 높이가 선반 아래에 남는다.**

| 기기(인셋 가정) | 스택 / 남은 칸 | 선반 아래 여유 |
|---|---|---|
| 411×868 (34/24) | 739 / 746 | **+7** — 실기기에서 맞아 보인 이유 |
| 411×868 (34/0) | 739 / 770 | +31 |
| 360×800 (24/24) | 712 / 688 | −24 |
| 320×569 (24/0) | 577 / 481 | **−96** |
| 800×1280 태블릿 | 980 / 1192 | **+212** |

**사용자가 「선반 안으로 옮겨서 고쳐줘」를 골랐다.** 버튼을 `answersContainer`의
**자식**으로 옮겼다 — `bottom`이 그 88 칸을 바로 가리키므로 스택이 화면을 채우든 남든
같은 자리다. 세로 자리를 정하는 값은 그대로(`refriFloatingReplayBottom`)이고 **기준만** 바뀌었다.

> **패딩이 절대배치를 밀지 않는다는 것을 소스로 확인했다.**
> `AbsoluteLayout.cpp`는 `측정높이 − 자식 − 테두리 − 마진 − bottom`이라
> **패딩을 빼지 않는다.** 그래서 선반 바깥 바닥에서 18(태블릿 12)이고 88 칸 안이다.
> 「그럴 것이다」로 두면 반대 동작에서 버튼이 카드 위로 올라간다.

**점검 3건 — 전부 0-2절.** 📱 하나(선반이 넘치면 버튼도 함께 넘친다) ·
🔧 둘(zIndex 두 값의 비교가 뜻을 잃었다 · 88이 리터럴이라 태블릿에서 위아래 12뿐).

> **1번은 맞바꾼 것이다.** 전에는 화면 기준이라 짧은 폰에서도 **보이긴 했다**(대신 카드와 겹쳤다).
> 지금은 선반을 따라가므로 **선반이 넘치면 버튼도 넘는다.** 세션 61의
> 「하한을 걷는 것은 맞바꾸는 것이다」와 같은 모양이다.

줄이 **1,340 → 1,352**(주석이다). 「한눈에」는 15,059 → **15,071**이다.

## 세션 64 — 냉장고 짧은 폰 세로 (남은 칸 · 점검 0건)

320×569가 계산상 31px 넘쳤다(0-2절). 게이지·선반은 크기를 지키고,
냉장고만 **남은 칸**을 쓰게 했다. 상한은 화면의 40%라 Note10+·태블릿은 그대로다.

| 파일 | 무엇을 |
|---|---|
| `app/(tabs)/refri-test/index.tsx` | `refriContainer` `height: 40%` → `flex: 1` + `maxHeight: 40%`. 자식은 `height: '100%'`. 선반 `flexShrink: 0` |
| `constants/layout.ts` | `refriSceneHeightRatio` 주석 — 고정 높이가 아니라 상한 |

0-2절 「320×569 31px 넘침」은 **지웠다.** 「선반이 넘치면 버튼도」는 근거만 고쳤다
(그 기기는 이제 냉장고가 줄어서 들어온다).

바뀐 줄만 봤다. **새것 0건.** 터치·`Role`은 그대로다.
~~**점검 커밋은 코드 커밋 뒤에 `406cbb2`에서 옮긴다.**~~ → **`b739b4b`로 옮겼다.**

## 세션 65 — 냉장고·배경을 칸에 맞춰 통째로 줄임 (점검 0건)

64는 칸만 줄여 짧은 폰에서 그림이 잘릴 수 있었다. 배경·냉장고·상자를
Rive 설계 크기로 묶고, 칸에 `contain`처럼 **한 배율**로 줄인다.

바뀐 줄만 봤다. **새것 0건.**
~~**점검 커밋은 코드 커밋 뒤에 `406cbb2`에서 옮긴다.**~~ → **`b739b4b`로 옮겼다.**

## 세션 66 — 냉장고 배율을 짧은 폰에만 (점검 0건)

65가 칸에 항상 맞춰 이 기기(Note10+)까지 줄였다. 칸이 40% 상한이면 배율 1,
상한보다 짧을 때만 contain.

바뀐 줄만 봤다. **새것 0건.**

## 세션 67 — 냉장고 짧은 폰 세로 (코드 · 점검 1건 → 0-2)

64~66 노트만 있고 코드는 없었다. 이번에 넣었다.
게이지·선반은 `flexShrink: 0`, 냉장고만 `flex: 1` + `maxHeight` 화면 40%.
칸이 상한보다 짧을 때만 배경·냉장고·상자를 한 배율로 줄인다.
확인 기기·태블릿은 상한을 그대로 쓴다.

| 파일 | 무엇을 |
|---|---|
| `app/(tabs)/refri-test/index.tsx` | `refriContainer` 고정 40% → `flex: 1` + `maxHeight`. 선반 `flexShrink: 0`. 짧은 칸만 `overflow` + `scale` |
| `constants/layout.ts` | `refriSceneHeightRatio` 주석 — 고정이 아니라 상한 |

0-2절 「320×569 31px 넘침」은 **지웠다.** 「선반이 넘치면 버튼도」는 근거만 고쳤다
(그 두 크기는 이제 냉장고가 줄어서 들어온다).

바뀐 줄만 봤다. **새것 1건** — 짧은 폰 첫 프레임이 상한으로 그려진다. 재점검에서
나와 **0-2절로** 보냈다. 터치·`Role`은 그대로다.
줄이 **1,352 → 1,383**. 「한눈에」는 15,071 → **15,102**.
~~**점검 커밋은 코드 커밋 뒤에 `406cbb2`에서 옮긴다.**~~ → **`b739b4b`로 옮겼다.**

## 세션 68 — learn 진행 게이지 (바뀐 줄 · 점검 1건 → 0-2)

냉장고 게이지를 learn 제목 아래로 옮겼다. 연습(5문항)은 한 색, 도전(10문항)만
70·90·100에서 색이 바뀌고 100%에서 깜빡인다. 세로 자리는 제목 마진·난이도
아래 마진에서 가져 순증이 없게 했다.

| 파일 | 무엇을 |
|---|---|
| `app/(tabs)/learn/index.tsx` | 푼 수/`maxRounds` 게이지. `RNAnimated.loop`는 결과·블러·언마운트에서 끊음 |
| `constants/layout.ts` | `learnGauge*` · `learnDifficultyContainerMarginBottom` 30→16 |

바뀐 줄만 봤다. **새것 1건** — 게이지 색 5개가 리터럴(냉장고와 같은 값).
0-2절로 보냈다. 터치 4 / `Role` 4→**5** (`progressbar`).
줄이 **574 → 745**. 「한눈에」는 15,102 → **15,273**.
**점검 커밋은 `123a389`.**

### 68(2) — 게이지 duration 220→450 + cubic inOut (점검 0건)

연습 한 칸 20%가 220ms라 툭 끊겼다. 다음 문항 타이머(600ms)보다 짧게 두었다.
처음엔 `out`이었는데 1~2번째가 튀어서 `inOut`으로 바꿨다.
바뀐 줄만 봤다. **새것 0건.** 0-2절 게이지 리터럴은 다시 안 물음.

## 세션 69 — learn 라운드 타이머가 탭 이탈에도 끊기게

답을 고른 뒤 600ms 타이머가 탭을 나가도 살아 있어 라운드가 넘어가고,
마지막이면 다른 탭에서 결과 창이 떴다. 소리는 끊고 타이머는 안 끊던 자리.

| 파일 | 무엇을 |
|---|---|
| `components/game/WordGame.tsx` | `useStopAudioOnBlur`에서 `clearNextRoundTimer`. 피드백 대기 중이면 `resetGame` |
| `hooks/useWordGameLogic.ts` | `clearNextRoundTimer`를 내보냄. `resetGame`이 피드백도 끈다 |

바뀐 줄만 봤다. **새것 1건** — 도전에서 나가면 `resetGame`이 두 번 돈다.
0-2절로 보냈다. 세션 52 0-2 「탭 이탈에 타이머가 안 끊긴다」는 **고쳐서 지웠다.**
줄이 **449 → 455**(`useWordGameLogic`은 225 → 228). 「한눈에」는 15,277 → **15,283**.
~~**점검 커밋은 아직이다** (미커밋). `WordGame` 칸은 `e1c74a8` 그대로.~~ → **`b4f7f1c`로 옮겼다.**

### 69(2) — matchGameAI 통계를 누른 동물만 (점검 0건)

`SELECT_ANSWER`가 남은 정답 전부에 `total`/`correct`를 붙이던 것을 PG처럼
누른 동물 하나만 센다. Q테이블·승패·난이도·목숨은 그대로다.
0-2절 「통계가 남은 정답 전부」는 **고쳐서 지웠다.**
바뀐 줄만 봤다. **새것 0건.** 줄이 **761 → 762**. 「한눈에」는 15,283 → **15,284**.
~~**점검 커밋은 아직이다** (미커밋). `matchGameAI` 칸은 `07dd68f` 그대로.~~ → **`85505aa`로 옮겼다.**

## 세션 70 — 기타 오답 −1 (바뀐 줄 · 점검 0건)

피아노처럼 오답에서 점수를 깎는다. 정답 `score + 1` 클로저도 `scoreRef`+`applyScore`로 맞췄다.

| 파일 | 무엇을 |
|---|---|
| `app/(tabs)/guitar/_layout.tsx` | 시작·정답·오답이 `applyScore`만 탄다. 오답은 `Math.max(0, −1)` |

바뀐 줄만 봤다. **새것 0건.** 재점검에서 `score` 이중 보관이 나와 0-2절 `progress` 줄에 합쳤다.
줄이 **581 → 593**. 「한눈에」는 15,284 → **15,296**.
~~**점검 커밋은 아직이다** (미커밋). `guitar/_layout` 칸은 `a57c217` 그대로.~~ → **`67cd697`로 옮겼다.**

## 다음 점검 후보

**비었다 — 세션 58에 `WaveRipple`·`RiveAnimalGame`이 나가면서 44개를 다 훑었다.**

| 순서 | 대상 | 왜 |
|---|---|---|
| — | 없음 | 「한 번도 안 봤다」가 **0**이다 |

> **이제 물음이 바뀐다.** 「무엇을 안 봤나」가 아니라 **「전수 뒤에 무엇이 바뀌었나」**이고,
> 답하는 것은 **점검 커밋**이다. 세션 58에 `?` 아홉을 채웠으니 판정이 기계로 돈다:
>
> ```
> git diff --name-only <점검 커밋> HEAD -- <파일>
> ```
>
> ~~**지금 걸리는 것은 `matchGame.tsx` 하나다** (점검 커밋 `fac2130` 뒤로 세션 58이 만졌다).~~
> → **세션 59에 봤다.** `matchGame`·`RiveAnimalGame` 둘 다 점검 커밋이 `6d74f5a`다.
> **세션 60에 learn 셋(`learn/index` · `WordGame` · `DrumGameOverScreen`)을 만졌고
> 점검 커밋이 `90076d4`로, 61에 `326320a`·`27ba6db`로, 62에 learn 둘이 `e1c74a8`로,
> **68에 `learn/index`가 `123a389`로** 갱신됐다.
> 새로 만질 화면이 없으면 그 목록이 곧 후보다 —
> 그리고 걸린 파일은 **전수가 아니라 바뀐 줄만** 본다 (위 「다시 훑어야 하나」).

> **세션 43에 `MissionProgressIcon`, 44에 쌍둥이 둘, 45~46에 남은 셋, 47에 `refri-test`,
> 48에 기타 화면, 49에 피아노, 50에 드럼 묶음, 51에 앱 뿌리, 52에 learn 묶음,
> 53에 탭 골격, 54에 단어 카드 안(`WordFlashcard`+`Waveform`), 57에 골격 여섯,
> 58에 `WaveRipple`·`RiveAnimalGame`이 나갔다. 이걸로 44개가 다 나갔다.**
>
> **마지막 둘에는 터치가 0개였다.** 44%(51/116)라는 접근성 비율은 세션 57 이후
> 움직이지 않았다 — 남아 있던 것이 **터치가 없는 애니메이션 부품**이었기 때문이다.
>
> **「부분」은 54에 0, 「한 번도 안 봤다」는 58에 0이 됐다.**
> 세션 49에 점검 커밋이 처음으로 한 묶음을 걸러냈지만 **`?`는 걸러 주지 못했다.**
> 세션 56·57이 커밋을 세 세션 미루며 `?`가 아홉으로 늘었고, **58에 다 채웠다** —
> `bfcaa90`(콘페티) · `ebc5926`(드럼 둘) · `f2828a5`(골격 여섯).
> **커밋을 미루면 다음 점검의 입력이 사라진다**는 것이 그 아홉 줄이다.
>
> **세션 53에 탭바 인셋 세 줄의 근거가 확정됐다.** `BottomTabBar.js:248`이
> `position: isTabBarHidden ? 'absolute' : undefined`라 **탭바가 보일 때는 절대배치가 아니다** —
> 냉장고(47)·드럼(50)·learn(52)이 `insets.bottom`을 또 더하는 것은 중복이 맞다.
> **다만 갈래는 그대로다** — 냉장고·드럼은 「빼면 버튼이 탭바에 붙어 보이나」가 남아 📱,
> learn(탭바 높이 64까지 더함)은 🔧다. **근거가 확정돼도 눈으로 볼 것은 남는다.**
