# 점검표 — 화면 하나를 훑는 순서

**이 표는 상상해서 만든 것이 아니다.** 세션 34~39에서 **실제로 지적받아 고친 것**을
갈래로 묶었다. 사용자가 자리를 짚어 줘야만 문제가 드러나던 것을, 자리를 안 짚어도
훑을 수 있게 만든 것이 목적이다.

- 쓰는 법: **`/점검 <경로>`** (`.claude/commands/점검.md`), 또는 화면을 만질 때 자동 (`CLAUDE.md` 규칙 5)
- **발견은 고치지 않는다.** 목록으로 보고하고, 고칠 것은 사용자가 고른다
- 어디를 봤는지는 [`점검-이력.md`](./점검-이력.md)에 남긴다
- 새 갈래가 나오면 **여기에 더한다.** 인계문 주의사항이 원문이고, 이 표는 그 색인이다
- **동작 절(6종류)** 은 세션 44~49에서 게임·냉장고·기타·피아노에 같은 결함이 반복돼 더했다.
  1~7절 번호는 그대로 둔다. 훑을 때는 **동작 → 1~7** 순이다.

---

## 0. 시작 전 — 오검 목록부터 읽는다

**이 절을 건너뛰면 매번 같은 것을 지적하고, 사용자가 매번 다시 설명해야 한다.**
아래는 **문제처럼 보이지만 그대로 두기로 결정된 것**이다. 점검에서 빼거나,
「알고 있는 항목」으로만 적는다.

| 자리 | 왜 그대로 두나 | 정한 때 |
|---|---|---|
| `components/game/WordGame.tsx` — `contentTopPadding * 0.4` | **사용자가 직접 넣은 값.** 실기기 확인됨 | 세션 36 |
| `components/game/WordFlashcard.tsx` — 「전체 듣기」가 카드 **아래**, 카드 테두리 초록 2px | **사용자 편집** | 세션 37 |
| `app/(tabs)/flashcards/index.tsx` — 하단 네비 `bottom: insets.bottom - flashcardsBottomOffset` | 인셋 0인 기기에서 가려지는 걸 **알면서** 「그대로 둔다」를 골랐다. 손으로 맞춘 값 | 세션 36 |
| 테스트 스위치 셋 (정답 힌트 · 초기화 버튼 2종) | **지우지 않는다** | 세션 29 |
| `WordFlashcard`의 `audioPlayer.progress` | 진행 바가 없어져 죽은 듯 보이지만 **`Waveform`이 쓴다** | 세션 35 |
| `flashcards`의 `panY` | 스와이프는 지웠지만 **학습완료 날아가기가 쓴다** | 세션 38 |
| `CACHE_LIMIT = 12` (`MusicTrainingScreen` · `guitar/_layout`) | 한도 40 − 배경 상주 26 − 여유 2에서 나온 **근거 있는 값** | 세션 20~27 |
| 탭 이탈 시 `remove()` | `pause()`는 AudioTrack을 붙잡아 **다른 탭까지 무음**이 된다. 재진입 지연은 대가로 받아들였다 | 세션 20~27 |
| 화면 방향 — `app/(tabs)/_layout.tsx`가 **유일한 소유자** | 화면별 `lockAsync`를 넣으면 경합이 재발한다. `app.json`은 출고 설정일 뿐 | 세션 26 · 34 |
| **가로 대응**을 새로 넣는 것 | 드럼·learn은 `PORTRAIT_UP` 고정. 안 쓰는 분기만 는다 | 세션 34 |
| `learn`의 `gameSection` 배경색 없음 | 「흰 카드로 묶기」를 **고르지 않았다** — 배경 이미지를 살리는 쪽 | 세션 35 |
| 드럼 탭을 다른 탭과 같은 골격으로 | **안 한다** | 세션 27 |
| 미션 성공 오버레이가 세션당 한 번 · 누적 정답 3회에 메시지 없음 | **의도된 동작** | 세션 28~33 |
| `matchGamePG`·`matchGameAI` — `loadingText` `textLoading`(#64748b) on `#f5f5f5` **4.36:1** | 본문 4.5에 0.14 모자란다. 「그대로」를 골랐다 | 세션 44 |
| `matchGameAI` — 정답·오답·비활성 색 6줄이 리터럴(`#C8E6C9` `#7cbd7e` `#FFCDD2` `#F44336` `#F5F5F5` `#BDBDBD`) | **PG는 같은 자리를 `COLORS` 토큰으로 쓴다.** 짝이 어긋난 것을 알고 「그대로」를 골랐다 | 세션 44 |
| 게임 두 파일 — `elevation` 리터럴 12개 | `LAYOUT`에 토큰이 있지만 안 바꾸기로 했다 | 세션 44 |
| 게임 4파일 — `WaveRipple color="#79A1FF"` | `COLORS`에 없는 리터럴이고 **`WaveRipple` 기본값과 같은 값**이라 지워도 되지만 그대로 둔다 | 세션 44 |
| 게임 4파일 — `styles.waveAnimation`의 `width`/`height` | `WaveRipple`이 `size`로 이미 넣는 값과 중복(죽은 스타일)이지만 그대로 둔다 | 세션 44 |
| `matchGamePG:225`·`matchGameAI:220` — `console.log("🚀 [의료 데이터 전송]", payload)` | 라운드마다 누적 통계를 찍지만 **남긴다** | 세션 44 |
| 게임 화면의 이모지 라벨 (🎯🎮🔥📊🏠▶️ + 등급 5) | flashcards는 세션 39에 Ionicons로 바꿨지만 **게임은 이모지 그대로** | 세션 44 |
| `hooks/useSyncGameData.ts:44` — `'error' is of type 'unknown'` | **저장소 전체 tsc 오류가 이것 하나뿐**인데 그대로 둔다 | 세션 44 |
| `hooks/useSyncGameData.ts:10` — `syncData`가 첫 줄에서 `return;` | 아래 `fetch` 40줄(`http://192.168.0.54:1357/sync`)이 전부 안 돈다. **꺼둔 스위치이고 지우지 않는다** — 세션 29의 테스트 스위치 셋과 같은 갈래 | 세션 44 |
| `orderGame:25` — `DEBUG_DROP = false`와 딸린 경로(`debugText` 스타일 · `#666` 리터럴 · 표시 블록) | 5절 「꺼둔 채 남은 경로는 남기지 않는다」에 걸리지만 **꺼둔 스위치라 지우지 않는다** — 세션 29의 테스트 스위치 셋과 같은 갈래. **세션 46의 `console.log` 제거에서도 이 블록만 남겼다** | 세션 45 |
| `orderGame` — 알림 6곳이 시스템 `Alert.alert` (시작·오류·빈칸·성공·오답) | `matchGame`은 같은 자리를 `Modal` 3개로 만들었지만 **짝을 맞추지 않는다** | 세션 45 |
| `refri-test` — `REFRI_RIVE_AVAILABLE = true`와 딸린 폴백 2블록 36줄 | 5절 「꺼둔 채 남은 경로는 남기지 않는다」에 걸리지만 **`DEBUG_DROP`·`useSyncGameData`와 같은 스위치 갈래**라 지우지 않는다 | 세션 47 |
| `refri-test` — `pickRandomAndPlay`의 `useCallback` deps가 `[]` | 안에서 쓰는 셋이 다 ref 기반이다. **바로 위 `setRiveDoor` 주석과 같은 stale 우회**라 그대로 둔다 | 세션 47 |
| `refri-test` — 상자 `refriCrateWrapperMarginBottomRatio: -0.053` · `…TopRatio: -0.077` | 음수 비율의 근거를 모르지만 **손으로 맞춘 값**이다. flashcards 바닥 오프셋과 같은 갈래 | 세션 47 |
| 기타 — 3단계가 `allNotesList.slice(0, 18)`이라 **2단계보다 후보가 적다** (15 vs 17) · 음역도 E2~A3로 좁다 | 후보 수가 뒤집힌 건 **1번(못 누르는 음 빼기)의 부작용**이지 새 설계가 아니다. 줄 단위(아래 4줄 16개)로 다시 잡으면 코드는 깔끔해지지만 **커리큘럼을 바꾸는 일**이라 손대지 않는다. 「반음이 처음 들어오니 음역을 좁혔다」로 읽으면 앞뒤가 맞는다 | 세션 48 |
| `InteractiveDrumSet` — 드럼 위 마커를 눌러도 **소리·하이라이트·정답 제출이 없다** | `onPress`가 `setCurrentInstrument`만 해서 악기명 라벨만 바뀐다. `currentInstrumentIndex`도 안 따라가 **그다음 ▶는 옛 자리에서 이어진다.** 32건 중 이것만 「그대로」를 골랐다 | 세션 50 |

> 여기 없는 「이상해 보이는 값」은 **먼저 물어본다.** 손으로 맞춘 값일 수 있다.

**이 절도 갱신형이다 — 쌓기만 하지 않는다.** 0절은 「**지금** 지적하지 말 것」이라
현재 상태를 답한다. 아래 둘 중 하나면 **그 줄을 지운다.**

- **결정이 뒤집혔다** (사용자가 「이제 고치자」고 했다)
- **근거가 사라졌다** — 그 코드·값이 이미 없어졌다

> 지울 때도 근거는 남는다. **원문은 인계문**에 있고 0절은 색인일 뿐이다.
> 세션 38의 `panY`처럼 「죽은 줄 알았는데 쓰인다」는 항목은
> **그 쓰임이 없어지면 0절에서도 나가야 한다.**

---

## 0-2. 보류 — 보고했으나 **아직 안 정한 것**

0절과 다르다. 0절은 「**그대로 두기로 결정**된 것」이고, 여기는
「**보고했는데 고칠지 말지 아직 안 고른 것**」이다. **둘 다 다시 지적하지 않는다** —
같은 것을 또 올리면 사용자가 매번 다시 판단해야 한다.

**「왜 보류」 칸을 본다.** 📱는 **실기기 확인 때 묶어서** 꺼낼 줄이고,
🔧는 **기기 없이도 끝나는데 그냥 안 고른** 줄이다. 섞이면 둘 다 안 나온다.

| 자리 | 무엇이 | 왜 보류 | 보고한 때 |
|---|---|---|---|
| `flashcards/index.tsx` — `modalContent`·하단 네비 `elevation: 10` · `completionRestartButton` `elevation: 3` | 옆 그림자는 토큰인데(`completeButtonElevation` · `navArrowButtonElevation` · `completedCardItemElevation`) 이 셋만 리터럴 | 🔧 안 골랐다 | 세션 42 |
| `flashcards/index.tsx` — 완료 항목 슬래시의 인라인 `marginHorizontal: 6` | 320dp에서 안쪽 폭 108로 빠듯한 자리인데 리터럴이고 인라인이다 | 🔧 안 골랐다 | 세션 42 |
| `flashcards/index.tsx` — `measure` 콜백의 `bx` `by` `cx` `cy` | 위치 인자라 뺄 수 없지만 `tsc`가 4건 경고한다. `_bx` 꼴이면 조용해진다 | 🔧 안 골랐다 | 세션 42 |
| `MissionProgressIcon.tsx` — `ICON_COLOR` 셋이 **게임 4화면에서 3:1 미달** | `#f5f5f5` 기준 cleared `#FFD700` **1.29:1** · none `#a0a0a0` 2.40:1 · partial `#CD7F32` 2.88:1. orderGame(`#fef3c7`)에선 금색 1.26:1. 피아노(`#000`)·기타(`#1a120b`)에서는 셋 다 통과 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 43 |
| `MissionProgressIcon.tsx` — `iconContainer` `top: 50` | 악기 둘은 `useInstrumentMetrics().missionIconStyle`(인셋 기준)을 넘기고 **게임 넷은 안 넘겨** 헤더 아래 50이다. **겹침이 아니라 기준이 다른 것** — 게임은 `(games)/_layout.tsx` 헤더가 `paddingTop: insets.top`을 이미 쓴다 | 📱 기기 대기 | 세션 43 |
| `MissionProgressIcon.tsx` — `ScrollView`의 인라인 `maxHeight: 220` · `paddingTop: 10` | 높이가 화면과 무관한 고정값이고, padding이 `contentContainerStyle`이 아니다. 모달에 `maxHeight`가 없어 폰트 배율을 키우면 **가로 화면(피아노·기타)** 에서 넘친다 | 📱 기기 대기 | 세션 43 |
| `MissionProgressIcon.tsx` — **`COLORS`를 한 번도 안 쓴다** (리터럴 20개+) | `#FFD700`→`gold` · `'white'`→`white` · `#E2E8F0`→`border`는 **토큰이 이미 있다.** 스크림은 `rgba(0,0,0,0.6)`인데 토큰 `overlayModal`은 0.5라 **모달마다 어둡기가 다르다** | 🔧 안 골랐다 | 세션 43 |
| `MissionProgressIcon.tsx` — 치수가 전부 리터럴 (규칙 3) | `LAYOUT`의 `isTablet ? x : y` · `spacing*`을 안 써서 **태블릿에서 안 커진다.** 반대쪽 320dp에서는 `width:'75%'`로 `progressCard` 안쪽이 152px다 | 📱 기기 대기 — 태블릿이 없다 | 세션 43 |
| `MissionProgressIcon.tsx` — 진행 항목의 「•」 · `modalTitle` | **재점검에서 나왔다.** 불릿이 장식인데 `importantForAccessibility`가 없어 따로 초점을 받고, 제목에 `accessibilityRole="header"`가 없다 | 🔧 안 골랐다 | 세션 43 (재점검) |
| `matchGamePG`·`matchGameAI` — `gradeLabel`의 등급 색 5개 | 흰 카드 위 14px인데 **1.25~2.85:1** (완벽 2.78 · 우수 1.93 · 보통 1.50 · 연습필요 **1.25** · 미도전 2.85). 본문 4.5 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 44 |
| 게임 두 파일 — `disabledButtonText` | `#999` + 버튼 `opacity: 0.6` 합성 = `#bebebe` on `#f5f5f5` **1.70:1**. 2절에 정해진 답(`grayLight` 배경 + `textSecondary` + `elevation:0`)이 있다 | 🔧 안 골랐다 | 세션 44 |
| 게임 두 파일 — `noDataText`·`overallStatsDetail` | `textLight`(#999)가 흰 카드 위 **2.85:1** | 🔧 안 골랐다 | 세션 44 |
| 게임 두 파일 — 홈 버튼 3개의 흰 글자 | blue **3.29** · orange **2.31** · successGreen **3.13**. 16px bold는 큰 글씨가 아니라 4.5가 기준이다. **토큰 자체의 문제라 다른 화면에도 번진다** | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 44 |
| 게임 두 파일 — 디버그 블록 + 주석 처리된 원본 `for` 루프 | `PG:271-287` · `AI:270-286`. 5절 「꺼둔 채 남은 경로는 남기지 않는다」에 걸린다 | 🔧 안 골랐다 | 세션 44 |
| 게임 두 파일 — `statusText` | 「난이도 \| 남은 기회 \| 점수」가 한 줄. 320dp·큰 폰트 배율에서 줄바꿈 | 📱 기기 대기 | 세션 44 |
| 게임 두 파일 — 40% 폭 「홈으로」 | `numberOfLines={1}` + `flexShrink: 0`이라 폰트 배율을 키우면 잘린다 | 📱 기기 대기 | 세션 44 |
| **`matchGameAI:110-118` — 통계가 「남은 정답 전부」에 붙는다** | **재점검에서 나왔다.** 한 번 누를 때마다 아직 안 고른 정답 **전부**의 `total`이 +1 되고 `correct`도 함께 움직인다. 누르지도 않은 동물의 정확도가 바뀌어 **약점 정렬·전송 데이터가 실제와 달라진다.** PG는 누른 것 하나만 센다 | 🔧 안 골랐다 — **접근성이 아니라 데이터다** | 세션 44 (재점검) |
| 게임 두 파일 — `mainTitle`·`title`에 `accessibilityRole="header"` 없음 | **재점검에서 나왔다.** `MissionProgressIcon`의 `modalTitle`과 같은 갈래 | 🔧 안 골랐다 | 세션 44 (재점검) |
| 게임 두 파일 — `statusText`에 `accessibilityLiveRegion` 없음 | **재점검에서 나왔다.** 남은 기회·점수가 바뀌어도 토크백이 읽지 않는다 | 🔧 안 골랐다 | 세션 44 (재점검) |
| `matchGameAI` — `startGame`의 `useCallback` deps에 `state.qTable` | **재점검에서 나왔다.** 함수 안에서 안 쓰는데 deps에 있어 **한 수 둘 때마다 함수가 새로 만들어진다** | 🔧 안 골랐다 | 세션 44 (재점검) |
| `matchGame` — `disabledButton`의 `borderWidth: 3` (기본 2) | 4절 「테두리는 늘 두고 색만 바꾼다」. 맞히면 카드 안쪽이 1px 줄어 내용이 흔들린다 | 🔧 안 골랐다 | 세션 45 |
| `matchGame` — 흰 글자 on `primary`(#f59e0b) **2.15:1** | 「게임시작」 16px bold와 모달 버튼 셋 다. 본문 4.5·큰글씨 3.0 양쪽 미달 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 45 |
| `matchGame` — `modalTitle` `primaryDark`(#d97706) on `backgroundWarm`(#fffbeb) **3.05:1** | 폰은 18px bold라 큰 글씨(18.66px)에 못 미쳐 4.5가 기준 — 미달. 태블릿 22px는 통과 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 45 |
| `matchGame` — `errorButton`에 「테스트: 3 → 2」·「테스트: 0.9 → 1」 주석 | 값이 이미 바뀐 뒤라 주석만 남았다 | 🔧 안 골랐다 | 세션 45 |
| `matchGame` — 치수 리터럴 8개 | `paddingHorizontal:10`·`paddingTop:16`·`paddingBottom:36`·`maxWidth:460`·`marginTop:6` 등. `LAYOUT.spacing*`이 있는데 안 써서 태블릿에서 안 커진다 (규칙 3) | 🔧 안 골랐다 | 세션 45 |
| `orderGame` — 드롭존이 담는 상자보다 크다 | 드롭존은 `orderGameCardSize`(태블릿 **120**) + `margin` 12×2 = **144**인데 `dropZoneContainer`는 **고정 120**. 태블릿에서 24px 넘쳐 「정답 제출」과 겹친다. 폰(≤454dp)은 안 넘친다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 45 |
| `orderGame` — 흰 글자 on `green`(#10b981) **2.54:1** · `greenBright`(#50C878) **2.13:1** | 16px bold 둘 다 미달 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 45 |
| `orderGame` — 「놓는곳」 `textPlaceholder`(#aaa) on `backgroundLight`(#f0f0f0) **2.02:1** | 인라인 스타일이고 `fontSize`가 없어 RN 기본 14px다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 45 |
| `orderGame` — `dropZonesLayout` state를 읽는 곳이 `DEBUG_DROP` 블록뿐 | 판정은 `dropZonesLayoutRef`가 한다 — 측정할 때마다 쓰이지 않는 리렌더가 돈다. 0절의 `DEBUG_DROP`을 남기기로 해서 **죽은 state가 아니게 됐다** | 🔧 안 골랐다 | 세션 45 |
| `orderGame` — 드롭존 `onLayout`의 `setTimeout(…,100)` 3개가 정리되지 않는다 | `matchGame`은 `timerRefs`로 모아 정리한다 | 🔧 안 골랐다 | 세션 45 |
| `orderGame` — 정답 비교가 `correctSoundNames.length`만 돈다 | 사운드가 2개만 로드되면(로드 실패 허용) **3번째 칸은 무엇을 넣어도 정답**이다. 비교도 `!=`(느슨한 비교)다. 세션 46에 로그를 지우며 같은 줄의 `===` 한 줄이 빠졌지만 **이 두 결함은 그대로다** | 🔧 안 골랐다 | 세션 45 |
| `orderGame` — `getRandomElements`가 컴포넌트 **안**이다 | 렌더마다 새로 만들어진다. `matchGame`은 파일 최상단에 뒀다 | 🔧 안 골랐다 | 세션 45 |
| `orderGame` — `ScrollView`의 `style`·`contentContainerStyle`이 인라인 | 매 렌더 새 객체이고, `contentContainerStyle`에 바닥 여백이 없다 (4절) | 🔧 안 골랐다 | 세션 45 |
| `new/index` — 카드 `width:128` + `columnGap:38`, 바깥 `marginHorizontal:18` | **320dp에서 안쪽 폭 284 < 2열 294**라 카드 4개가 **1열로 쌓인다.** 360dp부터 2열 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 45 |
| `new/index` — `COLORS`를 **한 번도 안 쓴다** | 색 리터럴 20개+ (`#352B46` `#FFF7E8` `#FFD54F` `#5EE7A8` `#1F1A32` `#FFB4C8` + `rgba()` 9개) | 🔧 안 골랐다 | 세션 45 |
| `new/index` — 치수도 전부 리터럴 | `marginBottom:72`·`width:128`·`104`·`columnGap:38`·`top:84`. `LAYOUT.isTablet`을 안 써서 **태블릿에서 카드가 안 커진다** (규칙 3) | 🔧 안 골랐다 | 세션 45 |
| `new/index` — `GestureHandlerRootView`로 두 번 감싼다 | 이 화면엔 제스처가 없다. `orderGame`의 드래그도 `PanResponder`(RN 코어)다. drum·flashcards·learn 셋은 실제로 쓴다 | 🔧 안 골랐다 | 세션 45 |
| `new/index` — 부제가 60ms마다 한 글자씩 자란다 | 토크백이 **자라는 중인 잘린 문장**을 읽고, 완성 문장(`SUBTITLE_TEXT`)은 어디에도 라벨로 없다 | 🔧 안 골랐다 | 세션 45 |
| **`matchGame` — 로딩 글자가 배경 사진 위에 놓인다** | **세션 46 재점검에서 나왔다.** 1번을 고쳐 `loadingContainer`가 **이제 실제로 뜬다.** `textLoading`(#64748b)인데 뒤가 `38.jpg` 사진이라 **대비를 계산할 수 없다** — 쌍둥이 둘은 `#f5f5f5` 위 4.36:1로 0절에 있지만 여기는 근거가 다르다 | 📱 기기 대기 — 사진 위라 계산이 안 된다 | 세션 46 (재점검) |
| `matchGame` — 로딩 문구에 `accessibilityLiveRegion` 없음 | **세션 46 재점검에서 나왔다.** 1번을 고쳐 「소리를 재생하고 있습니다」가 실제로 뜨는데, 토크백은 화면이 바뀐 것을 안 읽는다. 게임 두 파일의 `statusText` 줄과 같은 갈래 | 🔧 안 골랐다 | 세션 46 (재점검) |
| `DraggableImage` — `imagebutton`인데 눌러도 하는 일이 없다 | **세션 46 재점검에서 나왔다.** 21번으로 라벨·역할은 붙었지만 이동은 `PanResponder`뿐이라 **토크백으로 카드를 옮길 수단이 여전히 없다.** `accessibilityActions`로 「N번째 자리로 보내기」를 주는 것이 답인데 기능 추가라 범위 밖이었다 | 🔧 안 골랐다 | 세션 46 (재점검) |
| `new/index` — `[DEV]` 전체 초기화 버튼에 `Role`·`Label` 없음 | **세션 46 재점검에서 나왔다.** 27번은 게임 카드 4개만 다뤘다. 0절의 「테스트 스위치는 지우지 않는다」에 걸리는 버튼이라 **남는 이상 라벨은 필요하다** | 🔧 안 골랐다 | 세션 46 (재점검) |
| `refri-test` — 터치 4개에 `Role`·`Label`·`State`가 **하나도 없다** | 그중 **다시듣기(아이콘만)·시작(SVG만)은 읽을 텍스트가 아예 0**이라 토크백이 아무것도 못 읽는다. 이 파일 `accessibility` 등장 0회 | 🔧 안 골랐다 — 게임 화면들과 같이 미뤘다 | 세션 47 |
| `refri-test` — 정답 카드가 `disabled={isAnswerLocked}`인데 표시가 없다 | `accessibilityState`도 시각 반응도 없다 (4절). 비활성 답은 2절에 정해져 있다 | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — 게이지에 `accessibilityValue`·`accessibilityLiveRegion` 없음 | 진행이 바뀌어도 토크백이 안 읽는다. 게임 두 파일 `statusText` 줄과 같은 갈래 | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — 화면이 `insets.bottom`을 더하는데 **탭바가 이미 먹었다** | `(tabs)/_layout.tsx:99`가 `height: 64 + insets.bottom`이고 탭바는 `position:absolute`가 아니다 (bottom-tabs 7.8.4 `BottomTabBar.js:248`) — 화면 영역은 탭바 위에서 끝난다. 플로팅 다시듣기(962)·시작 버튼(1036) 둘 다 | 📱 기기 대기 — 빼면 버튼이 탭바에 붙을 수 있다 | 세션 47 |
| `refri-test` — `completeButton` 흰 글자 on `#7cbd7e` **2.23:1** | 16px bold라 4.5 기준. `#7cbd7e`는 `COLORS.success` 값이고 **토큰 자체의 문제라 다른 화면에도 번진다** | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 47 |
| `refri-test` — `answerCard` 테두리 `#BDBDBD` **1.88:1** | 흰 카드를 트레이와 가르는 유일한 경계인데 비텍스트 3:1 미달 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 47 |
| `refri-test` — 게이지 채움 4색 vs 트랙 `#E0E6ED` | `#66BB6A` **1.88** · `#FB8C00` **1.89** · `#43A047` **2.63** · `#F44336` **2.93**. 넷 다 비텍스트 3:1 미달 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 47 |
| `refri-test` — `COLORS`를 **한 곳에서만 쓴다** | 세션 47에 `completeTitle`만 토큰으로 바꿨다. 나머지 색 리터럴 19개는 그대로고, 스크림이 `rgba(0,0,0,0.7)`인데 토큰 `overlayModal`은 0.5라 **모달마다 어둡기가 다르다** | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — `fadeAnim`이 만들어진 뒤 한 번도 안 바뀐다 | `opacity: fadeAnim`은 늘 1이다 (5절 「읽는 곳 없는 state」의 사촌) | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — `CRATE_IMAGE = require(...)`가 컴포넌트 **안**이다 | 렌더마다 돈다. 0절로 간 `orderGame`의 `getRandomElements`와 같은 갈래 | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — 상자 임계값 `[25,50,75,100]`·렌더 순서 `[3,2,1,0]` | 상자 수와 **손으로 맞춰져** 있다. 배열이 바뀌면 둘 다 따라 고쳐야 한다 (1절 진행바 눈금과 같은 갈래) | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — 안 쓰는 `refri*` `LAYOUT` 토큰 6개 | `refriControlBtnGap`·`refriControlBtnPaddingV`·`refriControlRowGap`·`refriControlRowMaxWidth`·`refriStatusBadgeBorderRadius`·`refriStatusBadgeTop` | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — 죽은 주석 4곳 | 「버튼 애니메이션 제거」 2줄 · 상자 「오른쪽 아래/위」 2줄(**오른쪽 스택은 없다**) · 빈 `//` | 🔧 안 골랐다 | 세션 47 |
| `refri-test` — `gaugeContainer.marginTop: 40` | 리터럴이고, 폰에서 `gaugeSection` 안쪽 높이 **44를 10px 넘긴다** (60 − paddingV 16 vs 트랙 14 + 40). 태블릿(80)은 안 넘친다 | 📱 기기 대기 — 손으로 맞춘 값일 수 있다 | 세션 47 |
| `refri-test` — `refriGaugeMilestoneSize`가 폰 **2px** | 14px 트랙 위의 2×2 점이라 사실상 안 보인다 | 📱 기기 대기 | 세션 47 |
| `refri-test` — 320×569에서 세로가 **31px 넘친다** | 인셋 24 + 게이지 80 + 냉장고 208 + 트레이 288 = 600 > 569. 트레이가 눌린다 | 📱 기기 대기 — 작은 폰이 없다 | 세션 47 |
| `refri-test` — 플로팅 버튼 `#F0EDE8` on `#F5F7FA` | 거의 같은 색이라 경계가 `elevation: 4`뿐이다 | 📱 기기 대기 — 그림자는 계산이 안 된다 | 세션 47 |
| `refri-test` — `completeOverlay`가 `Modal` 안인데 `position:absolute`+`zIndex` 그대로다 | **세션 47 재점검에서 나왔다.** 8번을 고쳐 `Modal`로 감쌌으니 `flex: 1`이 맞고, `refriZIndexCompleteOverlay` 토큰은 **이제 뜻이 없다** (바로 위 「안 쓰는 토큰」 줄과 한 묶음) | 🔧 안 골랐다 | 세션 47 (재점검) |
| `refri-test` — `startGame`의 `isRoundLoading` 빗장이 첫 프레임에 샌다 | **세션 47 재점검에서 나왔다.** state라 시작 버튼이 사라지기 전(약 16ms) 두 번째 탭은 여전히 `false`를 본다. **ref였다면 확실하다** — 18번에서 `isAnimating`에 한 것과 같은 처방 | 🔧 안 골랐다 | 세션 47 (재점검) |
| `refri-test` — `timerRefs`가 한 게임(31라운드) 동안 안 비워진다 | **세션 47 재점검에서 나왔다.** `resetGame`·언마운트에서만 비운다. `matchGame`은 **라운드마다** 비운다(`matchGame.tsx:228-229`). ~90개라 무해하지만 짝이 어긋난다 | 🔧 안 골랐다 | 세션 47 (재점검) |
| `refri-test` — `stopStarAnimation`이 `startStarAnimation`의 deps에 없다 | **세션 47 재점검에서 나왔다.** 매 렌더 새 함수라 넣으면 `useCallback`이 무의미해진다. 0절로 간 `pickRandomAndPlay` deps `[]`와 같은 갈래 | 🔧 안 골랐다 | 세션 47 (재점검) |
| **`InstrumentButton` — `accessibilityRole`·`Label`·`State`가 하나도 없다** | 공용이라 **기타 6개(난이도 4 + 다시듣기 + 시작/종료) + 피아노 전부**가 함께 걸린다. 특히 난이도는 선택 여부가 **색으로만** 갈려 토크백엔 넷이 똑같이 읽힌다 | 🔧 안 골랐다 | 세션 48 |
| 기타 — 프렛 24개에 `Role`·`State` 없음 | `disabled={!isVisible}`인데 `accessibilityState`가 없어 잠긴 프렛도 같게 읽힌다 | 🔧 안 골랐다 | 세션 48 |
| **기타 — 프렛이 `onPressIn`만 써서 토크백으로는 안 눌린다** | 토크백 활성화는 `ACTION_CLICK`(→`onPress`)으로 들어오는데 그 경로가 없다. `onPress`를 그냥 더하면 **일반 터치에서 두 번 난다** — `onAccessibilityAction` 쪽이 답이다 | 🔧 안 골랐다 | 세션 48 |
| `ScoreFeedback` — `accessibilityLiveRegion` 없음 | 점수·정답/오답이 바뀌어도 토크백이 안 읽는다. 게임 두 파일 `statusText` 줄과 같은 갈래 | 🔧 안 골랐다 | 세션 48 |
| 기타 — 흰 글자 on `SEMANTIC.repeat` `#34C759` **2.22:1** | 「다시 듣기」. 11~14px bold라 4.5 기준 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 48 |
| 기타 — 흰 글자 on `guitar.accent` `#d4a373` **2.26:1** | 「훈련 시작」 **그리고 선택된 난이도**. 토큰 자체의 문제라 기타 화면 전체에 번진다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 48 |
| 기타 — 흰 글자 on `SEMANTIC.stop` `#FF3B30` **3.55:1** | 「훈련 종료」. 14px bold는 큰 글씨(18.66px)가 아니라 4.5가 기준 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 48 |
| **`InstrumentButton` — 훈련 중 선택된 난이도 1.69:1** | `disabled:{opacity:0.45}`가 배경·글자를 함께 흐리게 해서, 비선택(3.75)보다 **선택된 쪽이 더 안 읽힌다**. 2절의 정해진 답(`grayLight` 배경 + `textSecondary`)이 어두운 테마엔 그대로 안 맞는다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 48 |
| 기타 — 프렛 면 `#2d2016` vs 화면 `#1a120b` **1.17:1** · 테두리 `#5f4339` **2.07:1** | 비텍스트 3:1 미달. 프렛 상자를 배경에서 가르는 건 `elevation: 2`뿐이다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 48 |
| [피아노 경로] `InstrumentButton` — `action` 기본 `#007BFF` **3.98:1** · 꺼진 토글 `rgba(255,255,255,0.45)` on `#3a3a3a` **3.70:1** | 공용 부품이지만 실제로 타는 것은 피아노다. 피아노 전수 때 함께 본다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 48 |
| 기타 — `activeNotes` state가 죽었다 | 설정도 읽기도 없다. 피아노에는 실제 쓰임이 있어(`MusicTrainingScreen:1201,1270`) 복사된 흔적이다 | 🔧 안 골랐다 | 세션 48 |
| `useInstrumentMetrics` — `clamp`가 저장소 어디서도 안 쓰인다 | 쓰이는 건 `clampRound`뿐. `index.ts:9`가 다시 내보낸다 | 🔧 안 골랐다 | 세션 48 |
| `InstrumentButton` — 기본색 셋이 **피아노 색 리터럴** | `action '#007BFF'`(=`piano.accent`) · `difficulty '#555555'`(=`piano.idle`) · `toggleActive borderColor '#007BFF'`. 공용 부품이 한 악기 색을 기본값으로 든다 | 🔧 안 골랐다 | 세션 48 |
| 기타 — `midgroundLayer`의 `zIndex: 2`가 가리키는 것이 없다 | 기타엔 배경 레이어가 없다(피아노에서 온 값). 미션 아이콘은 `zIndex:100`이라 무관 | 🔧 안 골랐다 | 세션 48 |
| 기타 — `GUITAR_LEVELS`가 `label: name` | 두 칸이 늘 같은 값이다 | 🔧 안 골랐다 | 세션 48 |
| **`app/(tabs)/guitar/`에 `index.tsx`가 없고 `_layout.tsx`가 화면 본문이다** | 다른 탭은 전부 `_layout` = `Slot` + `index.tsx`(피아노 `activity`가 바로 옆 예다). 지금은 돌지만 **`<Slot/>`이 없어** 나중에 `guitar/index.tsx`를 만들면 그 화면이 절대 안 뜬다 | ❓ → 🔧 안 골랐다 | 세션 48 |
| 기타 — 작은 폰 가로(569×320)에서 **난이도 4개가 담는 칸을 넘는다** | `difficultySection` 231px vs 버튼 4개 ≈ 260px. RN은 `flexShrink` 기본이 0이라 줄지 않고 밀려 나간다. Note10+(868)는 369 vs 299로 여유 | 📱 기기 대기 — 글꼴 실측이 필요하다 | 세션 48 |
| 기타 — 오답 안내가 잘린다 | `feedbackLines={1}`인데 `infoSection`이 Note10+에서 185px, 「틀렸습니다! 다시 들어보세요.」는 ≈205px. **자르는 건 의도**(`ScoreFeedback` 주석)지만 문구가 그 폭을 전제로 하지 않는다 | 📱 기기 대기 | 세션 48 |
| 기타 — 비활성 프렛이 `opacity: 0.15`인데 `elevation: 2`가 남는다 | 안드로이드 그림자가 알파를 따라가는지는 기기라야 안다. 안 따라가면 잠긴 프렛 자리에 **그림자만** 뜬다 | 📱 기기 대기 — 그림자는 계산이 안 된다 | 세션 48 |
| 기타 — 태블릿 가로(1280×800)에서 줄 높이 113px에 글자 16px | `fit`의 상한(fretFont 16 · stringNameFont 14)에 걸려 더 안 커진다. **상한 자체는 의도**(`clamp` 주석)지만 태블릿에서 작아 보일 폭이다 | 📱 기기 대기 — 태블릿이 없다 | 세션 48 |
| **기타 — `timerRefs`가 한 훈련 동안 안 비워진다** | **세션 48 재점검에서 나왔다.** `clearPendingTimers`는 시작·종료·언마운트에서만 돈다 — 이미 끝난 타이머 id가 정답마다 쌓인다. `matchGame`은 라운드마다 비운다. **세션 47 `refri-test` 줄과 똑같다** | 🔧 안 골랐다 | 세션 48 (재점검) |
| **기타 — `playSound(...).then()`은 취소되지 않는다** | **세션 48 재점검에서 나왔다.** 「훈련 종료」가 `questionStartTimeRef`를 `null`로 되돌린 직후 앞서 시작된 재생의 `.then()`이 도착해 **다시 `Date.now()`를 찍는다.** 다음 시작이 새로 찍어 피해는 없지만 초기화가 뒤집힌다 — 세션 47의 「`await` 뒤 프라미스는 `timerRefs`에 못 넣는다」와 같은 자리 | 🔧 안 골랐다 | 세션 48 (재점검) |
| 기타 — `progress`를 state와 ref **두 곳에** 든다 | **세션 48 재점검에서 나왔다.** 5번을 고치며 들였다. 지금은 늘 함께 세팅하지만 한쪽만 고치면 조용히 어긋난다 (세션 45 `dropZonesLayout`/`Ref`와 같은 모양) | 🔧 안 골랐다 | 세션 48 (재점검) |
| 기타 — `getVisibleNoteSet`이 호출마다 `onFretboard`를 새로 만들고 28개를 필터한다 | **세션 48 재점검에서 나왔다.** 1번을 고치며 들였다. 결과가 난이도별로 고정이라 **모듈 상수 4개면 끝난다.** 0-2절의 `getRandomElements`·`CRATE_IMAGE`와 같은 갈래 | 🔧 안 골랐다 | 세션 48 (재점검) |
| **피아노 — `activeNotes` state가 죽었다** | `renderPianoViewportRow`가 세 번째 인자로 받기만 하고 **본문에서 한 번도 안 쓴다.** 그런데 건반마다 `setActiveNotes`가 누름·뗌 두 번 돌아 1,799줄 화면이 통째로 리렌더된다. **세션 48에 「기타는 죽었고 피아노엔 쓰임이 있다」고 적은 판정이 틀렸다** — `MusicTrainingScreen:1201,1270`은 넘기는 자리일 뿐이다 | 🔧 안 골랐다 | 세션 49 |
| **피아노 — 자동 포커싱 훅 전체가 죽었다** | `useAutoFocusViewport({ currentNote: null … })`이라 늘 첫 줄에서 반환한다(주석도 「완전히 비활성화」). 훅 33줄(`86-118`)과 `musicTrainingHelpers.ts:47`의 `computeFocusStart`(파라미터 `currentStart`·`viewportSize`를 안 쓰고 상수 0/14/16을 돌려준다)가 함께 죽어 있다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `FallingNoteTrack`의 통합 분기가 죽었다 | `viewportStartIdx`·`whiteIdxMap` prop을 아무도 안 넘겨 `isIntegrated`가 늘 거짓이다. `getLaneX`의 그 갈래가 안 돈다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `MiniKeyboardMap:41-60`의 `else` 분기가 죽었다 | 경계 7/15 · `setViewportStartIdx(16)`. 미니맵은 `!isFixedViewport`(4·5단계)에서만 뜨고 그때 `viewportSize`는 **늘 16**이다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `data/songs.ts:296` `songsByScale`이 안 쓰인다 | 피아노는 같은 일을 `songs.filter(...)`로 **두 곳에서 따로** 한다 (`MusicTrainingScreen:374, 630`) | 🔧 안 골랐다 | 세션 49 |
| 피아노 — 죽은 선언 둘 | `useWindowDimensions`를 import하고 안 쓴다(치수는 `useInstrumentMetrics`가 준다) · `isMounted`를 세우고 지우기만 하고 읽는 곳이 없다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `clampConsole`이 `clamp`의 복사본이다 | `useInstrumentMetrics`의 `clamp`와 같은 함수를 `MusicTrainingScreen:1092`가 다시 만든다. **0-2절의 「`clamp`가 어디서도 안 쓰인다」(세션 48)는 이것 때문**이다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `console.log("🚀 [의료 데이터 전송] music:", …)` | 0절은 `matchGamePG`·`matchGameAI`의 같은 줄만 「남긴다」로 정했고 **피아노 줄은 정해진 적이 없다** (세션 47은 냉장고에서 지웠다) | 🔧 안 골랐다 | 세션 49 |
| **피아노 — `FallingResultOverlays`의 `bottom: 96`이 고정이다** | 이 화면 제어반은 낙하 모드에서 **76**이고 인셋이 더 붙는다. 같은 화면의 `missionOverlay`·`previewOverlay`는 「고정값은 어긋난다」는 주석과 함께 `overlayFrameStyle`(인셋 + 제어반)을 쓴다 — **넷 중 둘만 그렇게 돼 있다** | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `standardWhiteKeyHeight`의 `− 90` | 90은 옥타브 줄(52) + 여백(38)을 손으로 더한 값인데 **그 줄은 1·2·3단계에서 아예 안 뜬다** — 그때도 빼서 건반이 64px 짧다. 뜰 때 남는 여유는 **2px**다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `consoleWidth = (usableWidth − 100) × (2.4/5.4)` | `5.4`는 `infoSection 1.2 + songSlotSection 1.8 + consoleSection 2.4`, `100`은 뒤로가기 36+4 · `marginHorizontal 8×2` 둘 · 제어반 `paddingHorizontal 14×2`를 손으로 더한 값이다. **스타일 쪽 flex나 여백을 고치면 여기가 조용히 어긋난다** | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `MiniKeyboardMap`의 `KEY_SCALE = 36 / 28` | 36은 옥타브 버튼 높이인데 그 버튼은 내용(아이콘 24 + padding 6×2)으로 정해진다. 폰트 배율이 커지면 버튼만 커지고 미니맵은 그대로다. 전체 폭 231px도 고정이라 태블릿에서 안 커진다 (규칙 3) | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `FallingNoteTrack`의 판정선 식이 두 곳이다 | `Math.max(40, trackHeight − 24)`가 `59`와 `80`에 똑같이 있다. 한쪽만 고치면 그려지는 선과 부모에게 통보하는 좌표가 갈린다 | 🔧 안 골랐다 | 세션 49 |
| **피아노 — `missionOverlay`·`previewOverlay`가 `Modal`이 아니다** | 세션 49에 **리플레이·결과 둘만** `Modal`로 바꿨다(`8fe2c23`, 사용자가 그 둘만 골랐다). 남은 둘은 조건부 `View` + `zIndex`(90 · 95)라 여전히 뒤로가기를 못 받는다 | 🔧 안 골랐다 | 세션 49 |
| **피아노 — 건반이 `onPressIn`만 써서 토크백으로는 안 눌린다** | 토크백 활성화는 `ACTION_CLICK`(→`onPress`)으로 들어오는데 그 경로가 없다. **0-2절의 「기타 프렛」 줄과 같은 결함인데 그 줄은 기타 파일만 가리킨다** — 고칠 때 둘을 함께 본다 | 🔧 안 골랐다 | 세션 49 |
| **피아노 — 건반 27~28개에 `Role`·`Label`·`State`가 없다** | `disabled={!isVisible}`인데 표시도 없다. 이 묶음(2,827줄)에서 `accessibility*`가 나오는 파일은 **`LandscapeBackButton` 하나뿐**이다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — 옥타브 버튼 2개에 `Role` 없음 | `disabled={viewportStartIdx === 0 / 14}`인데 `accessibilityState`도 없다 | 🔧 안 골랐다 | 세션 49 |
| **피아노 — 미니맵이 토크백으로 조작 불가다** | `Role`·`Label`이 없고 조작이 **`locationX`(누른 좌표)**라 토크백으로는 어디를 눌러도 같은 곳이다. 게다가 `TouchableOpacity`가 켜져 있어 **자식(백건 30 + 흑건 22 + 하이라이트 + 도트)이 한 덩어리**로 묶인다 (세션 42 자리) | 🔧 안 골랐다 | 세션 49 |
| **피아노 — 곡 슬롯이 `PanResponder` 스와이프뿐이다** | 터치 요소가 아니라 토크백에 아무것도 안 읽히고 **곡을 바꿀 수단이 없다.** 0-2절 `DraggableImage` 줄과 같은 갈래 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `FallingResultOverlays`의 버튼 3개에 `Role`·`Label` 없음 | 리플레이 · 결과 보기 · 확인 | 🔧 안 골랐다 | 세션 49 |
| **피아노 — `octaveBtnHighlight` 흰 글자 on `#00e5ff` 1.54:1** | `PIANO.highlight`. 14px bold라 4.5 기준 — **강조하려고 켜는 색인데 가장 안 읽힌다** | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 49 |
| 피아노 — 흰 글자 on `#007BFF` **3.98:1** 두 곳 | `octaveBtnText`(14 bold) · `FallingResultOverlays`의 `resultCloseButton`(14 bold). 0-2절의 「[피아노 경로] `InstrumentButton` action」과 **같은 색인데 다른 파일**이다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 49 |
| 피아노 — `FallingResultOverlays` 대비 다섯 자리 | `#E6A800` on `#F4F7FB` **1.96:1** 셋(`replayIconText` 14 bold · `resultStatPerfect` 22 · 상자 테두리) · `#64748B` **4.43:1** 둘(`replaySubText` 14 bold · `resultStatLabel` 12 bold) | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 49 |
| 피아노 — `songSlotPeek` `rgba(255,255,255,0.35)` ≈ **3.2:1** | 프레임 SVG 바탕 `#101010`~`#1a1a1a` 기준. 12px bold라 4.5 기준 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 49 |
| **피아노 — `PIANO.keys.white`·`keys.black` 토큰이 죽었다** | `PianoKey`의 `animatedStyle`이 배경을 리터럴(`#111111`/`#2a2a2a`/`#ffffff`/`#ececec`)로 덮어쓴다. 토큰은 `black`(#000)인데 실제로 칠해지는 건 `#111111`이다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — 색 리터럴 | `FallingResultOverlays`는 토큰을 **한 번도 안 쓴다**(리터럴 20개+, `#007BFF`는 `piano.accent`와 같은 값). `MusicTrainingScreen`도 `#000`·`#fff`·`#555`·`rgba()` 여섯이 리터럴 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — 주석이 코드와 다른 곳 셋 | 「백건 18px, 흑건 14px」인데 코드는 `isBlack ? 9 : 12`(**숫자도 방향도 반대**) · 「14개의 백건」인데 실제는 8/15/16 · 「3단계 및 4단계에서 16건반」인데 5단계도 16이다 | 🔧 안 골랐다 | 세션 49 |
| 피아노 — `KEYBOARD_ENABLED = false` · `VISUALIZER_MODE = 'particle'` | 꺼진 경로가 함께 남는다 — 키 라벨 두 스타일 + `noteToKeyMap`/`keyToNoteMap`(`constants/music` 15줄) · `RippleLayer` 31줄 전체. **0절의 `DEBUG_DROP`·`REFRI_RIVE_AVAILABLE`과 같은 스위치 갈래로 보이는데 0절에 없다** | ❓ → 답 없음 · 🔧 안 골랐다 | 세션 49 |
| 피아노 — 옥타브 가이드가 4단계에만 있다 | 5단계(전문 · 흑건 22개)도 뷰포트 밖 음이 나오는데 화살표가 안 깜빡인다. 의도인지 모른다 | ❓ → 답 없음 | 세션 49 |
| 피아노 — 작은 폰 가로에서 **난이도 5개가 담는 칸을 넘는다** | 569×320에서 `fallingPickerSection` ≈215px vs 버튼 5개 ≈310px. Note10+(868)는 343 vs 310으로 아슬하게 든다. **0-2절 기타(4개 · 231 vs 260)와 같은 자리인데 피아노가 하나 더 많다** | 📱 기기 대기 — 글꼴 실측이 필요하다 | 세션 49 |
| 피아노 — 태블릿 가로에서 **백건 높이가 600px**이 된다 | 1280×800에서 `usableHeight − 200`. 상한이 없다 — 기타는 `fit`으로 막았다 | 📱 기기 대기 — 태블릿이 없다 | 세션 49 |
| 피아노 — 제어반 왼쪽이 거의 딱 찬다 | `feedbackLines` 제한이 없는 건 의도인데, 「난이도를 선택하고 [문제 재생]을 눌러 시작하세요!」가 215px 칸에서 2줄이 되고 점수+피드백+힌트 합이 제어반 안쪽 **94px에 거의 딱 찬다.** 폰트 배율을 키우면 넘친다 | 📱 기기 대기 | 세션 49 |
| **피아노 — `score`·`progress`를 state와 ref 두 곳에 든다** | **세션 49 재점검에서 나왔다.** 4번을 고치며 들였다. 쓰는 자리를 `applyScore`/`applyProgress` 둘로 좁혔지만 초기값은 여전히 따로 선언된다. **0-2절의 기타 `progress` 줄(세션 48 재점검)과 같은 모양을 피아노에도 들였다** | 🔧 안 골랐다 | 세션 49 (재점검) |
| 피아노 — 빗장 풀기 + 타이머 비우기가 **여섯 자리**로 흩어졌다 | **세션 49 재점검에서 나왔다.** `playNextQuestion` · `startTraining` · `stopTraining` · `handleDifficultyChange` · `startFallingNoteMode` · `handleResetProgress`. 하나라도 빠뜨리면 채점이 영영 막힌다 — 한 함수로 묶여야 한다 | 🔧 안 골랐다 | 세션 49 (재점검) |
| 피아노 — `questionStartTime`이 여전히 state다 | **세션 49 재점검에서 나왔다.** 1번을 고치며 출제마다 `setQuestionStartTime`이 돈다. **렌더에서 안 읽으므로 ref여야 한다** — 세션 47 `isAnimating` · 세션 48·49의 채점 빗장과 같은 갈래인데 여기만 안 바꿨다 | 🔧 안 골랐다 | 세션 49 (재점검) |
| 피아노 — `Modal` 안인데 `position:absolute`+`bottom:96`+`zIndex:100`이 그대로다 | **세션 49 재점검에서 나왔다.** 19번(부분)을 고치며 남았다. **세션 47 `completeOverlay` 줄과 글자까지 같다.** 위 「`bottom: 96`이 고정이다」와 한 묶음 — 그걸 안 골라서 손대지 않았다 | 🔧 안 골랐다 | 세션 49 (재점검) |
| 피아노 — `dismissFallingReplayPrompt`와 `closeFallingResult`가 겹친다 | **세션 49 재점검에서 나왔다.** 19번을 고치며 만들었다. 세 줄 중 둘(`setLastFallingResult(null)` · `setCurrentSong(null)`)이 같아 한쪽만 고치면 갈라진다 | 🔧 안 골랐다 | 세션 49 (재점검) |
| 피아노 — 가로모드에서 `statusBarTranslucent` Modal이 인셋을 어떻게 덮나 | **세션 49 재점검에서 나왔다.** 이 화면은 가로라 상태바가 옆에 붙는다. Modal이 노치 밖까지 덮는지는 눈으로 봐야 안다 | 📱 기기 대기 | 세션 49 (재점검) |
| **드럼 — 캐릭터 크기 계수가 두 곳에서 다르다** | `InteractiveDrumSet:304`은 `drumSetSize * 0.15`, 화면크기 변경 처리(`262`)는 `* 0.1`. 회전·분할화면 뒤 경계(`maxX`/`maxY`)가 실제 캐릭터보다 **크게** 잡혀 캐릭터가 세트 밖으로 나간다 | 🔧 안 골랐다 | 세션 50 |
| **드럼 — 캐릭터 초기 위치의 기준이 존재하지 않는 버튼이다** | `InteractiveDrumSet:176-190`의 주석·계산은 `right: 0.3` · `bottom: 50`인데 실제 스타일은 `right: 0.1`(801) · `bottom: 10`(838)이고, 그 버튼은 **늘 꺼진다**(아래 줄). 진짜 버튼은 부모가 화면 하단에 그린다 | 🔧 안 골랐다 | 세션 50 |
| **드럼 — `▶️1` 순환 버튼이 죽었다** | 유일한 호출자가 `hideCycleButton`을 늘 켠다(`drum/index.tsx:1211`). `controlButtonsContainer`·`cycleButton`·`cycleButtonText`도 함께 죽었다. 라벨은 `▶️` + **군더더기 `1`**(바이트 확인) | 🔧 안 골랐다 | 세션 50 |
| 드럼 — `InteractiveDrumSet`의 죽은 선언 넷 | `DEFAULT_DRUM_ORDER`(39) · `isGameMode`(83·받기만) · `hideCharacterAndButtons`(88·넘기는 곳 없음) · `layout` prop(103·늘 `numInstruments`로 온다) | 🔧 안 골랐다 | 세션 50 |
| **드럼 — 하이라이트 애니값 4개가 아무 데도 안 붙는다** | `triggerHighlight`는 하이하이트·심벌의 `Scale`·`Shake`를 세팅하는데 두 `Animated.Image`(694-707)는 `opacity`만 받는다 — **스네어·킥·톰만 흔들린다** | 🔧 안 골랐다 | 세션 50 |
| 드럼 — `drum/index.tsx`의 죽은 state·구조분해 여섯 | `isGameStarted`(105·쓰기 3회 읽기 0) · `isSoundTestExpanded`(106) · `isGameMode`(108·주석도 「레거시」) · `score`·`answerHistory`·`startNewRound`(243·245·249) | 🔧 안 골랐다 | 세션 50 |
| **드럼 — `useDrumLogic`의 죽은 경로 셋** | `choices`(라운드마다 오답 선택지를 만드는데 **선택지 UI가 없다**) · `resetGameWithoutStarting`·`setAnswered`(부르는 곳 0) · `handleAnswer(null)` **시간 초과 경로**(제한시간 타이머가 없어 절대 안 온다) | 🔧 안 골랐다 | 세션 50 |
| 드럼 — `colorImage`·`scrollToPage`가 죽었다 | `drumLayouts.ts`의 네 레이아웃이 `image`와 **같은 값**으로 들고 있는데 읽는 곳이 없다 · `scrollToPage`(`drum/index.tsx:1152`)도 ref에 노출만 하고 부르는 곳이 없다 | 🔧 안 골랐다 | 세션 50 |
| **드럼 — `InteractiveDrumSet`에 `accessibility*`가 0회** | 마커 2~5개는 선택 여부가 **색으로만** 갈리고(`#7cbd7e`/`#FF9800`) 내용은 🎵 이모지뿐이라 토크백이 읽을 것이 없다 | 🔧 안 골랐다 | 세션 50 |
| **드럼 — 헤더 셋·하단 버튼 둘에 `Role`·`Label` 없음** | `drum/index.tsx` 378·386·412·516·558. **◀▶는 SVG뿐이라 읽을 글자가 0**이다. 이 파일의 접근성은 `QuestionCountSelector`(1014-1035)에만 있다 | 🔧 안 골랐다 | 세션 50 |
| 드럼 — 바뀌는 값에 `accessibilityLiveRegion`·`accessibilityValue` 없음 | 모드 배지(433) · `{round}/{questionCount} 회`(443) · `DrumGameOverScreen:127`의 Rive 게이지. 게임 두 파일 `statusText` 줄과 같은 갈래 | 🔧 안 골랐다 | 세션 50 |
| **`DrumGameOverScreen` — 흰 글자 on `COLORS.success` 2.23:1** | `247`·`262`. 20px bold라 3:1 기준. 「나가기」 아웃라인 **테두리도 같은 색 2.23:1**(254)로 비텍스트 3:1 미달 — 흰 카드 위 흰 버튼이라 그 테두리가 유일한 경계다. **`refri-test` `completeButton` 줄(세션 47)과 같은 토큰** | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 50 |
| 드럼 — 흰 아이콘·글자 on `#FF9800` **2.16:1** | `drum/index.tsx:843`·`857`·`875`. 하단 ◀▶ 드럼스틱(SVG `#ffffff`)과 퀴즈 ↻(26px bold) 둘 다 3:1 미달 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 50 |
| 드럼 — 흰 글자 on `#fd7d7d` **2.50:1** | `735`·`741`(「듣기연습」 14px bold) · `923`·`936`(세그먼트 선택 칸). 4.5 기준 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 50 |
| 드럼 — 헤더 중앙 **모드 배지 두 색 다 미달** | 연주 `blue` on `blueLight` **2.88:1**(708) · 퀴즈 `primaryDark` on `backgroundWarning` **3.05:1**(712). 18px bold라 큰 글씨(18.66)에 못 미쳐 4.5가 기준. 퀴즈 쪽은 **0-2절 `matchGame` `modalTitle`과 같은 색 쌍**이다 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 50 |
| 드럼 — 「그만하기」 인라인 `#999` on 흰 헤더 **2.85:1** | `drum/index.tsx:418`, 16px bold. 게임 두 파일 `noDataText` 줄과 같은 갈래 | 🔧 안 골랐다 — **계산으로 끝났다** | 세션 50 |
| **드럼 — 탭바가 이미 먹은 `insets.bottom`을 화면이 또 더한다** | `358`(`contentWrapper`) · `512`(하단 오버레이). 드럼 탭은 `tabBarStyle`을 안 끈다(`(tabs)/_layout.tsx:112`) — **세션 47 `refri-test`와 같은 자리**다. 802번 줄 주석 「zIndex로 탭 바 위에 그리기」도 **틀렸다**: 탭바는 `absolute`가 아니라 형제 밖이라 `zIndex 9999`로 못 덮는다 | 📱 기기 대기 — 빼면 버튼이 탭바에 붙을 수 있다 | 세션 50 |
| 드럼 — 설정 드롭다운이 조건부 `View`다 | `drum/index.tsx:449-457`. 백드롭이 **패널 아래만** 덮어(`top: headerHeight + PANEL`) 헤더 쪽 터치로는 안 닫히고, 뒤로가기도 못 받는다 | 🔧 안 골랐다 | 세션 50 |
| 드럼 — 고정 px 셋 | `sectionDrum.marginTop: 150`(652) · `drumOverlayFixed.height: 160`(808) · `countdownOverlayUp.paddingBottom: 300`(956). 320×569 폰에서 세로 569 중 **150+160이 먼저 나가고**, 카운트다운은 300만큼 위로 밀려 헤더에 물린다 (규칙 3) | 🔧 안 골랐다 | 세션 50 |
| 드럼 — `fixedBtnScale`이 **모듈 로드 시** `Dimensions.get`한 값이다 | `drum/index.tsx:18-25`. 태블릿·분할화면에서 다시 안 잰다 — 같은 파일이 아래에서는 `useWindowDimensions`를 쓴다(92) | 🔧 안 골랐다 | 세션 50 |
| **드럼 — `console.log`/`warn` 16개** | `drum/index` 10 · `useDrumLogic` 3 · `InteractiveDrumSet` 3(warn). 세션 46에 게임 화면에서 걷어낸 갈래인데 드럼은 그대로다. 0절이 「남긴다」로 정한 건 **`matchGamePG`·`matchGameAI`의 의료 데이터 전송 줄뿐**이다 | 🔧 안 골랐다 | 세션 50 |
| 드럼 — `DRUM_POSITIONS`를 매 렌더 `Object.fromEntries`로 새로 만든다 | `InteractiveDrumSet:126-130`. 0-2절의 `getRandomElements`·`CRATE_IMAGE`와 같은 갈래 | 🔧 안 골랐다 | 세션 50 |
| 드럼 — 화면크기 리스너 deps가 `[characterPosition]`이다 | `InteractiveDrumSet:256-294`. **캐릭터가 움직일 때마다 구독을 떼었다 붙인다.** 안에서 쓰는 `insets`는 deps에 없다 | 🔧 안 골랐다 | 세션 50 |
| 드럼 — `InteractiveDrumSet`이 `COLORS`를 **한 번도 안 쓴다** | `#7cbd7e`(=`success`) · `#FF9800` · `#ecc681` · `'white'`가 리터럴이다 | 🔧 안 골랐다 | 세션 50 |
| **드럼 — 그림과 마커의 기준이 다르다** | `drumSetContainer`에 `paddingBottom: 70`(827)이 있어 드럼 그림은 안쪽 상자(높이 `drumSetSize − 70`)에 `contain`으로 그려지는데, 마커·캐릭터 좌표(733-746)는 `drumSetSize` **전체**를 곱한다. `drumLayouts`의 비율은 540² 캔버스 실측값이다 | ❓ → 좌표를 눈으로 맞춰 흡수한 것인지 답 없음 | 세션 50 |
| 드럼 — `fixedButtonBottomOffset`이 음수까지 내려간다 | `drum/index.tsx:157`, `Math.max(-70, …)`. 배경 레터박스 여백에서 나온 값이라 기기 비율이 다르면 하단 버튼이 탭바에 물리는지 눈으로 봐야 안다 | 📱 기기 대기 | 세션 50 |
| **드럼 — `score`를 state와 ref 두 곳에 든다** | **세션 50 재점검에서 나왔다.** 2번을 고치며 들였다. 쓰는 통로를 `applyScore` 하나로 좁혔지만 초기값은 따로 선언된다. **0-2절의 기타 `progress`(세션 48) · 피아노 `score`·`progress`(세션 49)와 같은 모양을 드럼에도 들였다 — 세 번째다** | 🔧 안 골랐다 | 세션 50 (재점검) |
| 드럼 — 「시작 중」을 뜻하는 표가 둘이다 | **세션 50 재점검에서 나왔다.** 3번을 고치며 `quizRunIdRef`를 더해 `isStartingQuizRef`와 나란히 산다. 지금은 `exitQuiz`가 둘을 함께 만지지만 한쪽만 고치면 조용히 어긋난다 | 🔧 안 골랐다 | 세션 50 (재점검) |
| 드럼 — `stopPendingRounds`가 이름보다 많은 일을 한다 | **세션 50 재점검에서 나왔다.** 1번을 고치며 만들었다. 타이머만 끊는 이름인데 `showFeedback`도 끄고 답안 빗장도 푼다 | 🔧 안 골랐다 | 세션 50 (재점검) |
| 드럼 — 결과 `Modal`의 `statusBarTranslucent`가 인셋을 어떻게 덮나 | **세션 50 재점검에서 나왔다.** 5번을 고치며 켰다. **세션 49 피아노 재점검 줄과 같은 것인데 그쪽은 가로, 드럼은 세로**라 상태바가 위에 붙는다 | 📱 기기 대기 | 세션 50 (재점검) |

**항목이 여기서 나가는 길은 둘뿐이다.**

- **고쳤다** → 줄을 지운다 (근거는 인계문에 남는다)
- **그대로 두기로 정했다** → **0절로 올린다** (이유와 정한 때를 적는다)

> **여기 있는 것은 다시 묻지 않는다.** 점검이 끝날 때 묻는 것은
> **그 파일에서 새로 나온 것**뿐이다. 이 절을 비우는 일은 실기기 확인 때
> **📱 줄을 묶어서** 따로 한다.
>
> 세션 42까지는 「점검이 끝날 때마다 **이 중 고칠 것 있나를 묻는다**」였다.
> **세션 43에 뒤집었다** — 이미 「나중에」로 정해 둔 것을 점검마다 다시 꺼내면
> 사용자가 **같은 판단을 매번 다시** 해야 한다. 그건 보류가 아니라 미결이다.

---

## 동작 — 타이머 · 채점 · 출제 · 닫는 창 (6종류)

**1~7절에 없던 갈래다.** flashcards 치수를 다른 탭에 들이대는 것이 아니라,
훈련·게임 화면을 읽을 때 **같은 버릇이 다시 나오는지** 본다.
실기기 정상 경로에서는 잘 안 티 난다.

| # | 볼 것 | 실제로 걸린 것 |
|---|---|---|
| **1** | **`await` 앞에서 `set*`가 하나라도 있나** | 그 사이 렌더가 나간다. `matchGameAI`는 `PLAYING`을 재생 앞에 넣어 **이전 라운드**로 채점됐고, `matchGame`은 재생 중 카드가 눌렸고, 냉장고는 「다시 듣기」가 **안 나온 문항** 소리를 냈다 |
| **2** | **정답 직후·이미 고른 뒤에 채점 빗장이 있나** | 다음 문제가 되기 전에 같은 답이 또 들어간다. `matchGameAI`는 PG에 있던 중복 채점 빗장이 없었고, 기타·피아노는 정답 뒤 1~1.2초 **같은 칸 연타로 클리어**됐다. 막는 것은 **채점뿐**이고 소리는 낸다 — 렌더에서 안 읽으면 **ref** |
| **3** | **점수·누적을 `score + 1` · `{...progress}`처럼 렌더 클로저로 더하나** | 한 프레임에 입력이 겹치면 하나만 오른다. 기타·피아노 `progress` 스프레드, 피아노는 오답만 `prev =>`였다. 별·클리어 조건이 **매 정답마다** 다시 참이면 호출이 반복된다. 냉장고 `isAnimating`이 state라 타이머가 **누른 시점의 옛 값**을 봤다 |
| **4** | **`setTimeout` / `Animated.loop`를 담는 곳이 있나** (원형은 `matchGame.tsx` `timerRefs`) | 종료·언마운트 뒤에 콜백이 살아남는다. 냉장고 타이머 6곳·별 루프 쌓임, 기타·피아노는 정답 뒤 출제가 **훈련 종료 뒤**에도 울렸다. **`await` 뒤 프라미스는 `timerRefs`에 넣지 않는다** — 취소하면 안 풀린다. `.then()`도 취소되지 않는다 |
| **5** | **출제 후보·시작 시각이 화면에 고를 수 있는 것과 같나** | 기타는 소리 28 vs 프렛 24라 **누를 자리 없는 문제**가 나왔다. 시각을 `.then()` 안에만 찍으면 비어 **17억 초**가 서버로 갔다. 피아노는 새 문제에서 시각을 안 찍어 첫 오답 전까지 **0초**, 이후는 **그 오답 시각**이 기준이었다. 그리는 쪽을 상한으로 두고 출제를 **파생**시킨다 |
| **6** | **닫아야 하는 판이 `Modal` + `onRequestClose`인가** | 조건부 `View`+`zIndex`면 뒤로가기가 **탭을 나간다.** 익힘모달(세션 39)·냉장고 완료(47)가 그 자리. **안내(카운트·미션 성공처럼 잠깐 뜨고 사라지는 것)는 창이 아니다** — 리플레이·결과처럼 확인이 필요한 것만 4절을 적용한다 |

```
grep -n "await \|setTimeout\|setInterval\|Animated.loop\|\.then(" <파일>
grep -n "setScore\|setProgress\|addStar\|markAsCleared\|currentNote" <파일>
```

> **스위치로 막아 둔 전송 경로는 지우지 않는다.** `useSyncGameData`의 첫 줄 `return;`과
> `console.log("🚀 [의료 데이터 전송]")` · 페이로드 조립은 **나중에 `return`만 걷으면** 쓴다 (0절).

---

## 1. 기준이 틀린 값 — 가장 자주 나왔다

| 볼 것 | 실제로 걸린 것 |
|---|---|
| **개수를 손으로 박았나** | 진행바 눈금 `[0,1,2,3,4,5]` — 카드가 20장이어도 6개였다 (세션 38) |
| **고정 px가 반응형 자리에 있나** | 게이지 `220×40` · 난이도 버튼 `140/160`(360dp에서 40px 넘침) · 카드 밀어내기 `150+70`(640dp에서 48px 잠김) |
| **퍼센트가 무엇의 퍼센트인가** | 레일·마커·눈금이 **기준 셋**이었다. `railPercent()` 하나로 묶었다 (세션 38) |
| **중심 보정이 값에 드러나나** | 마커 `marginLeft: -1` → **`-마커폭/2`** |
| **상자보다 큰 내용** | 마커 상자 32px에 아이콘 36px + `overflow:'hidden'` → 좌우 2px씩 잘림 |
| **개수 0일 때** | 완료화면에서 `1 / 0`이 떴다 |

**확인법** — StyleSheet에서 숫자 리터럴을 훑고, 그 값이 **데이터 개수·화면 폭 중 무엇을 따라야 하는지** 묻는다.

```
grep -n "width: [0-9]\|height: [0-9]\|\[0, 1, 2" <파일>
```

> **값 하나만 보고 단정하지 말 것.** 세션 38에서 「마커가 90%까지만 간다」고 보고했는데
> 그 90은 **레일 폭과 같은 수**였다. 세션 39에서는 「단어 카 드」로 보고 자간인 줄 알았는데
> 실제 문자열은 「단어 카드」였다. **한글·이모지가 섞이면 `od -c`로 바이트를 본다.**

## 2. 대비 — 눈으로는 안 보이고 계산해야 걸린다

| 볼 것 | 실제로 걸린 것 |
|---|---|
| **회색 배경 + 흰 글자** | 「재생 중...」 `borderGray`+`opacity:0.6`+흰 글자 = **1.5:1** · 학습완료 버튼도 같음 |
| **흰 배경 + 옅은 회색** | 되돌리기 아이콘 `textLight`(#999) = **2.8:1** |
| **흰 배경 + 원색** | 등급 색 `#FFD700`·`#7cbd7e` = **2.2:1** (큰 글씨 기준 3:1 미달) |

기준: 본문 **4.5:1**, 큰 글씨/아이콘 **3:1**.
정해진 답: 비활성은 `grayLight` 배경 + `textSecondary` 글자 + `elevation: 0`.
**흰 바탕 초록 글자는 `COLORS.successOnWhite`** — 배경·테두리는 `COLORS.success` 그대로다.

```
grep -rn "#999\|#BDBDBD\|textLight\|borderGray\|opacity: 0\." <파일>
```

## 3. 접근성 속성 — 이 저장소에서 가장 크게 비어 있다

터치 요소에 `accessibilityRole` · `accessibilityLabel` · `accessibilityState`가 있는가.
장식 아이콘에는 **`importantForAccessibility="no"`**.

```
for f in $(grep -rl "TouchableOpacity\|Pressable" app components); do \
  echo "$(grep -c 'TouchableOpacity\|Pressable' $f)	$(grep -c accessibilityRole $f)	$f"; done | sort -rn
```

**수치는 여기 적지 않는다** — 적으면 낡는다. 실측은 [`점검-이력.md`](./점검-이력.md)가 갖는다
(파일마다 「터치/`Role`」 칸, 그리고 「한눈에」 표).

> 훑은 자리에만 접근성이 있다 — **비율과 어느 파일인지는 이력의 「한눈에」가 갖는다.**
> (여기 숫자를 적으면 세션마다 낡는다. 세션 40에 적은 「나머지는 전부 0」이 실제로 틀린 말이 됐다)

**터치를 감싸는 것이 자식을 삼키지 않는지도 본다.** `TouchableOpacity`·`Pressable`은
`accessible`을 끄지 않는 한 **켜져 있고**(RN 0.86 `TouchableOpacity.js:303` —
`accessible={this.props.accessible !== false}`), 켜진 요소의 **자식은 한 덩어리로 묶여**
토크백 초점이 따로 가지 않는다. 모달 배경으로 시트를 감싼 자리가 그랬다 (세션 42).

| 걸리는 모양 | 정해진 답 |
|---|---|
| 배경 `Touchable`이 **시트를 자식으로** 감쌌다 | 배경을 **절대배치 형제**로 내린다 (`components/MissionProgressIcon.tsx`가 그 형태) |
| 눌리지 않는데 `Touchable`로 감쌌다 | 그냥 `View`로. `e.stopPropagation()`은 **RN에서 하는 일이 없다** |

```
grep -n "TouchableOpacity" <파일> | 그 줄의 자식이 화면 한 덩어리인지 본다
```

## 4. RN 골격 — 안드로이드에서만 티가 난다

| 볼 것 | 실제로 걸린 것 |
|---|---|
| 모달이 **`Modal`**인가 | 조건부 `View`+`zIndex`라 **뒤로가기로 안 닫혔다** (익힘모달, 세션 39). **닫아야 하는 창만** — 동작 6. 카운트·미션 성공 같은 안내는 해당 없음 |
| `onRequestClose` 있나 | 위와 한 쌍 |
| `ScrollView` padding이 **`contentContainerStyle`**인가 | `style`에 있어 **마지막 항목 아래 여백이 없었다** |
| `maxHeight` 안의 스크롤 상자에 **`flexShrink: 1`** | 0이라 내용이 잘리고 스크롤도 안 됐다 |
| 바닥에 붙는 것에 **`insets.bottom`** | 「전체 다시 하기」가 제스처바에 물렸다 |
| `disabled`인데 **시각 반응만 나는** 버튼 | 학습완료·화살표·「전체 듣기」 |
| 테두리를 상태로 켜나 | **`borderWidth`는 늘 두고 색만 바꾼다.** 아니면 크기가 흔들린다 |

## 5. 죽은 것 · 이름 충돌

| 볼 것 | 실제로 걸린 것 |
|---|---|
| 읽는 곳 없는 state | `completionMessage` |
| 설정 없는 ref의 cleanup | `completionTimeoutRef` |
| 자식이 하나인데 `gap` | `playAllButton`의 `gap: 8` |
| **꺼둔 채 남은 경로** | `PanGestureHandler enabled={false}` (세션 38에 지웠다) |
| **한 스타일 키를 두 화면이 공유** | `completionRestartButton` — 익힘모달과 완료화면이 같이 썼다 |
| **파일이 달라 이름만 같은 키** | `modalContent` — 익힘모달 ≠ 미션모달 |

> **지우기 전에 쓰는 곳을 센다.** `audioPlayer.progress`·`panY`는 죽어 보였지만 쓰인다.
> 반대로 **꺼둔 코드는 다음 세션이 또 판단해야 하므로** 남기지 않는다.

## 6. 플랫폼 규칙 (`CLAUDE.md` 규칙 4)

```
grep -rn "shadowColor\|shadowOffset\|shadowOpacity\|shadowRadius\|Platform.OS === 'ios'" app components hooks constants
```
**2026-09-04 재확인 0건**(세션 48에 기타, 49에 피아노, **50에 드럼 묶음**까지 포함해 다시 돌렸다). 그림자는 `elevation`으로만 낸다.

## 7. 반응형 (`CLAUDE.md` 규칙 3)

숫자를 **320 / 360 / 411(실기기) / 600~800**에 넣어 본다.
버튼줄 합이 안쪽 폭을 넘는지, 세로 여유가 음수가 되는지.
근거 수치는 `handoff3.md` 세션 35~37, `handoff4.md` 세션 38~39 표에 모여 있고,
**「기기가 생기면 볼 것」 목록은 현재 인계문**(`handoff8.md`) 「다음 작업」 3번이 갖는다
(세션 49~50치는 `handoff7.md`, 42~48치는 `handoff6.md`의 같은 자리에 남아 있다).

---

## 못 잡는 것 — 이 표의 한계

**코드에서 읽히는 것만 잡는다.** 아래는 여전히 실기기다.

- 실제 렌더링(글자 잘림 · 겹침) · 기기별 인셋 · 소리 · 체감 속도 · 애니메이션 느낌
- **「빠짐없이」는 약속하지 않는다.** 이 표의 약속은 **반복된 것을 놓치지 않는다**이다
