# 오디오 무음 — 원인 분석과 수정 방향

작성: 2026-08-16 (세션 15)
대상: 🎹 피아노 · 🎸 기타 무음, 그리고 같은 뿌리에서 나온 🎧 단어 무음
검증: 실기기 logcat (SC-01M / Android 12 / com.vlondy.threadsclone)

---

## 0. 한 줄 요약

**플레이어를 "가벼운 객체"로 보고 개수를 늘렸는데, 실제로는 안드로이드가 앱당 약 40개로 제한하는 희소 자원(AudioTrack)이었습니다.**

피아노는 탭에 들어가는 순간 52개를 만듭니다. 한도를 넘긴 플레이어는 만들어지자마자 죽고,
그 뒤 `play()`는 아무 일도 하지 않습니다. **에러는 JS에 보이지 않습니다.**

---

## 1. 증거 — logcat

추측이 아니라 실기기에서 직접 잡은 로그입니다.

```
E AF::Track:     Track(3341): no more tracks available
E AudioFlinger:  createTrack_l() initCheck failed -12; no control block?
E AudioTrack:    createTrack_l(-1): AudioFlinger could not create track, status: -12
E AudioTrack-JNI: Error -12 initializing AudioTrack
E MediaCodecAudioRenderer: Caused by: java.lang.UnsupportedOperationException: Cannot create AudioTrack
E ExoPlayerImplInternal: Playback error
    androidx.media3.exoplayer.ExoPlaybackException: MediaCodecAudioRenderer error,
    format=Format(1, null, audio/mp4, audio/mp4a-latm, mp4a.40.2, ...)
```

- `no more tracks available` → **AudioFlinger가 이 앱에 더 이상 오디오 트랙을 안 준다**
- `status: -12` → `NO_MEMORY`. 메모리가 아니라 **개수 한도**입니다
- `audio/mp4a-latm` → **피아노·기타의 `.m4a`** 가 실패한 것입니다 (32건)
- `audio/wav` → 드럼·단어의 `.wav` 도 같이 실패 (2건)

### 타임라인

| 시각 | 플레이어 생성 | 관측 |
|---|---:|---|
| 19:10 | 80개 | **첫 `no more tracks` 에러** |
| 19:17 | 85개 | |
| 19:20 | **52개** | 피아노 탭 프리로드 (숫자가 정확히 일치) |
| 19:21 | 63개 | |
| 19:21:52 ~ 19:22:10 | — | 이 18초 동안 AudioTrack 실패 **741건** |
| 19:22:03~05 | — | `[Touch debug] F4 / E4 / D4 / C4` — **터치는 들어오는데 무음** |
| 19:29:14 | — | `🔊 Playing word: 탈` — **단어 탭도 같은 이유로 무음** |

에러가 터진 시각(19:21:52)이 건반을 누른 시각(19:22:03)보다 **11초 앞섭니다.**
건반을 누른 게 원인이 아니라, **탭에 들어간 순간 이미 죽어 있었습니다.**

버퍼 전체에서 `no more tracks available` **348건**.

---

## 2. 메커니즘 — 왜 이렇게 되나

### `createAudioPlayer` 하나가 잡는 것

`createAudioPlayer()` 한 번은 JS 객체 하나가 아닙니다.

```
createAudioPlayer(음원)
  └─ ExoPlayer 인스턴스           (expo-audio AudioPlayer.kt:36-56)
  └─ MediaSession 등록            (AudioPlayer.kt:67)
  └─ setMediaSource() → prepare() (AudioPlayer.kt:93-98)
       └─ MediaCodec 오디오 디코더
       └─ ★ AudioTrack ★          ← 시스템 전체에서 개수가 제한된 자원
  └─ 상태 갱신 코루틴 (500ms 주기, 영구)  (BaseAudioPlayer.kt:52-69)
```

### 자원을 잡는 시점이 "재생"이 아니라 "생성"

이게 가장 반직관적인 부분입니다.

`prepare()`는 재생 여부와 무관하게 렌더러를 켜고 버퍼를 채웁니다.
그 과정에서 `DefaultAudioSink`가 **AudioTrack을 미리 할당합니다.**

> **만들어만 두고 한 번도 안 울린 플레이어도 자리를 차지하고 있습니다.**

logcat이 이걸 증명합니다 — 터치 11초 전, 프리로드 시점에 에러가 났습니다.

### 한도

AOSP `AudioFlinger`의 UID당 트랙 상한은 **40개** 수준입니다 (기기·OS별로 다를 수 있음).
넘으면 `createTrack_l()`이 `-12`로 실패하고, ExoPlayer는 그 플레이어를 에러 상태로 떨굽니다.

---

## 3. 간과한 것 — 7가지

이번 문제의 핵심입니다. 하나하나가 독립적인 판단 착오였습니다.

### ① 플레이어를 "메모리 문제"로만 봤다

세션 6~7에서 프리로드를 결정할 때 검토한 축은 **메모리(파일 크기)** 였습니다.
"기타 28 + 피아노 52 + 드럼 5 = 85개가 남는다, 사용자는 메모리 문제 없다고 판단" (`handoff.md:909-911`).

메모리는 실제로 문제가 아니었습니다. **문제는 메모리가 아니라 개수였습니다.**
음원 파일이 아무리 작아도 AudioTrack은 파일 크기와 무관하게 1개씩 소모됩니다.

> 봐야 했던 축: **동시 개수 한도**

### ② 예산이 "화면별"이 아니라 "앱 전체"라는 걸 놓쳤다

피아노 52개를 정할 때 피아노 화면만 봤습니다.
그런데 **탭은 언마운트되지 않고, 캐시도 비우지 않기로 했으므로** 모든 탭의 플레이어가 동시에 살아 있습니다.

logcat에서 피아노 때문에 **단어 탭 "탈"이 무음**이 된 게 그 결과입니다.
서로 다른 화면이 같은 예산을 갉아먹는 구조인데, 화면 단위로만 설계했습니다.

### ③ 상한값(15)을 "근거 없음"과 "불필요"로 혼동했다

`CACHE_LIMIT = 15`를 지울 때의 기록:

> "**기타 `CACHE_LIMIT = 15`는 실측값이 아니다.**" → 세션 7에서 삭제 (`handoff.md:902`)
> "왜 이전에 안 했나: 의도는 있었습니다. **음이 많아서 LRU 15로 제한**한 것이고,
> 파일 크기·기기 한도를 재고 고른 숫자는 문서에 없습니다." (`handoff.md:1157`)

**"기기 한도를 재고 고른 숫자가 아니다"는 관찰은 정확했습니다.**
거기서 내린 결론이 틀렸습니다. "숫자에 근거가 없다 → 한도 자체가 없다"로 넘어갔습니다.

실제로는 **한도는 실재했고, 15가 우연히 그 아래였을 뿐**입니다.
근거 없는 숫자를 만났을 때 할 일은 삭제가 아니라 **측정**이었습니다.

### ④ 오디오 에러가 JS에 안 보이는 구조를 몰랐다

expo-audio는 재생 에러를 **이벤트로만** 보냅니다.

```kotlin
// expo-audio/android/.../AudioPlayer.kt:158
override fun onPlayerError(error: PlaybackException) {
  sendStatusUpdate(mapOf("error" to error.message))   // playbackStatusUpdate 이벤트
}
```

- 어느 화면도 이 이벤트를 **구독하지 않습니다**
- `play()`는 `Function`이라 **예외를 던지지 않습니다** (`AudioModule.kt:467`)
- `seekTo()`도 실패하지 않습니다

결과: `playSound`의 `try/catch`는 **절대 안 걸립니다.**

> **에러 없이, 로그 없이, 조용히 무음.**
> 세션 15에서 "재생 실패 로그는 없다"를 "재생은 성공했다"로 읽은 근거가 이겁니다.

### ⑤ 최적화 대상을 "지연" 하나로 잡았다

세션 6~8의 모든 결정이 **첫 소리 지연**을 향해 있었습니다. 프리로드, 캐시 비우지 않기, LRU 삭제 전부 같은 방향입니다.

지연은 실제 문제였고 개선도 됐습니다. 하지만 **한 축만 밀면 다른 축이 무너집니다.**
용량(개수)이라는 축을 아무도 안 봤습니다.

### ⑥ 실기기 로그를 안 봤다

세션 15에서 세 번 오진했습니다. 전부 **코드만 읽고 추론**한 결과입니다.

| 회차 | 진단 | 실제 |
|---|---|---|
| 1 | `currentTime > 0`일 때만 `seekTo` → pause 후 무음 | ❌ 무관 |
| 2 | 첫 진입 시 로드 미완료 | ❌ 무관 |
| 3 | 블러가 미로드 플레이어까지 `pause()` | ❌ 무관 |

`adb logcat` 한 번으로 3분 만에 확정됐습니다.
**네이티브 자원 문제는 JS 코드를 아무리 읽어도 안 보입니다.**

### ⑦ "탭은 언마운트되지 않는다"의 대가를 절반만 계산했다

이 사실 자체는 정확히 파악하고 문서화까지 했습니다 (`useStopAudioOnBlur.ts:7-9`).
그래서 "블러 때 정지"라는 대응까지 나왔습니다.

하지만 **정지는 소리만 멈추지 자원은 안 돌려줍니다.**
"언마운트 안 됨 → 소리가 계속 남" 은 봤는데, "언마운트 안 됨 → **자원이 계속 쌓임**" 은 못 봤습니다.

---

## 4. 현재 앱의 플레이어 재고

예산 계산에 반드시 필요한 표입니다.

| 화면 | 방식 | 상주 개수 | 언제 사라지나 |
|---|---|---:|---|
| 🎹 피아노 | 탭 포커스 시 전체 프리로드 | **52** | 안 사라짐 |
| 🎸 기타 | 탭 포커스 시 전체 프리로드 | **28** | 안 사라짐 |
| 🐾 동물게임 (AI/PG) | `GameAudioManager` 싱글턴 | **12** | **앱 종료까지** |
| 🎧📖 단어 | 모듈 단위 LRU | **8** | 앱 종료까지 |
| 🥁 드럼 | `AudioManager` 풀 | **5** | 앱 종료까지 |
| ❄️ 냉장고 | 플레이어 1개 재사용 | **1** | — |
| 🎮 matchGame / orderGame | 라운드마다 3개 생성·해제 | 3 (일시) | 라운드 종료 시 해제 ✅ |
| | | | |
| **합계 (최악)** | | **109** | 한도 ≈ 40 |

### ⚠️ 여기서 중요한 정정

대화 중에 제가 **"캐시를 비우면 기타 28은 그대로 둬도 된다"** 고 말했는데, **틀렸습니다.**

피아노·기타를 뺀 **배경 상주분이 이미 26개**입니다 (동물 12 + 단어 8 + 드럼 5 + 냉장고 1).
이건 탭을 비워도 안 사라집니다.

```
한도 40 − 배경 상주 26 = 실제 쓸 수 있는 예산 ≈ 14개
```

**기타 28도 혼자서 안 들어갑니다.** 피아노 52는 말할 것도 없습니다.
"탭 이탈 시 캐시 비움"만으로는 부족하고, **상한 자체가 14 근처여야 합니다.**

이 숫자가 뒤의 두 방향을 고르는 기준입니다.

---

## 5. 언제 들어왔나

`b48fec8` (2026-08-15 23:32, "doc_78_주석지우기전guitar")

```diff
- const CACHE_LIMIT = 15;
- const recentlyUsedNotes = useRef<Note[]>([]);

+ useFocusEffect(useCallback(() => {
+   for (const note of Object.keys(soundFiles) as Note[]) {
+     if (soundCache.current[note]) continue;
+     soundCache.current[note] = createAudioPlayer(soundFiles[note]);   // 52개
+   }
+ }, []));
```

기타(`app/(tabs)/guitar/_layout.tsx:106-117`)도 같은 커밋에서 28개로 바뀌었습니다.

**세션 15에서 넣은 두 수정(`await seekTo(0)` 항상 / `playing`일 때만 `pause`)은 원인이 아닙니다.**
죽은 플레이어는 뭘 해도 안 울립니다. 무해하지만 문제를 안 고칩니다. 현재 **미커밋** 상태입니다.

---

## 6. 방향 1 — 개수 제한 (LRU 복원)

> **스피커를 다 사놓지 않고, 최근에 친 음 N개만 들고 있는다.**

### 어떻게 동작하나

```
건반 누름
  ├─ 캐시에 있음  → 즉시 재생 (지연 0)
  └─ 캐시에 없음  → 가장 오래 안 쓴 것 하나 버리고
                    새로 만들어 재생 (지연 있음)
```

### 바뀌는 코드

| 파일 | 변경 |
|---|---|
| `MusicTrainingScreen.tsx:582-593` | 전체 프리로드 삭제 |
| `MusicTrainingScreen.tsx:673-688` | `CACHE_LIMIT` + `recentlyUsedNotes` 복원 |
| `guitar/_layout.tsx:106-117` | 동일 |
| `useStopAudioOnBlur` 콜백 | `pause()` → `remove()` + 캐시 비움 |

`b48fec8`을 되돌리는 수준이라 **작업량이 가장 작습니다.**

### 장점

- 되돌리기 수준. 구조를 안 바꿈
- 한 번 친 음은 그 방문 동안 **지연 0**
- 폴리포니(동시에 여러 음) 그대로 유지

### 단점

**1) N을 크게 못 잡습니다 (핵심 문제)**

3·4단계 화면에는 **백건 16 + 흑건 약 11 = 27건반**이 보입니다 (`MusicTrainingScreen.tsx:417`).
"화면에 보이는 건 다 담자"면 N = 27이어야 합니다.

그런데 4장에서 계산한 실제 예산은 **약 14**입니다.

> **화면에 보이는 건반의 절반밖에 못 담습니다.**
> 한 옥타브만 훑어도 앞 음이 쫓겨나고, 다시 누르면 또 지연됩니다.
>
> 예전 `CACHE_LIMIT = 15`가 정확히 이 상태였습니다.
> **없앤 이유였던 "연주 중 지연"이 그대로 돌아옵니다.**

**2) 탭 재진입 첫 음 지연**

캐시를 비워야 하므로 돌아올 때마다 다시 만듭니다.
"캐시는 블러 때 비우지 않는다"는 세션 7의 규칙을 **깨야 합니다.**

**3) 배경 상주분에 취약**

동물게임 12개는 앱 종료까지 안 사라집니다.
동물게임을 한 번이라도 켜면 피아노 예산이 12개 줄어듭니다.
**다른 탭을 어떤 순서로 다녀왔느냐에 따라 피아노가 되기도 안 되기도 합니다.**

### 평가

되돌리기는 쉽지만, **없앴던 문제를 그대로 되살립니다.**
N을 27로 키우면 지연은 해결되지만 한도를 다시 넘습니다. **N 하나로 두 조건을 동시에 만족시킬 수 없습니다.**

---

## 7. 방향 2 — 플레이어 재사용 (고정 풀 + `replace()`)

> **스피커를 12개만 사두고, 건반을 칠 때마다 그 스피커에 음원을 갈아 끼운다.**

### 어떻게 동작하나

핵심은 **"플레이어 = 음"이라는 1:1 대응을 끊는 것**입니다.

```
[스피커 풀 — 12개 고정, 앱 내내 이 12개가 전부]

건반 C4 누름
  → 지금 안 울리는 스피커 하나 고름 (라운드로빈)
  → player.replace(C4 음원)      ← 음원만 교체, 플레이어는 그대로
  → player.play()

건반 E4 누름 (C4 울리는 중)
  → 다음 스피커 고름
  → replace(E4) → play()          ← 동시에 남 (폴리포니 유지)
```

expo-audio가 `replace()`를 제공합니다 (`AudioModule.kt:488-506`).
**플레이어를 만들지도 버리지도 않으므로 AudioTrack 개수가 12에서 절대 안 움직입니다.**

### 바뀌는 코드

| 파일 | 변경 |
|---|---|
| 새 파일 (예: `services/NotePlayerPool.ts`) | 풀 구현 |
| `MusicTrainingScreen.tsx` | `soundCache` 전부 삭제 → 풀 호출로 교체 |
| `guitar/_layout.tsx` | 동일 |
| `useStopAudioOnBlur` | 풀 정지만. 비울 필요 없음 |

피아노·기타가 **같은 풀을 공유**할 수 있습니다. 그러면 두 탭 합쳐서도 12개입니다.

### 장점

- **개수가 상수.** 탭을 얼마나 오래 쓰든, 몇 번 오가든 12개
- **캐시를 비울 필요가 없음** → 세션 7의 "비우지 않는다" 규칙을 유지
- **다른 탭 순서에 영향받지 않음** — 예산 계산이 필요 없어짐
- 음이 52개든 100개든 **음원 개수와 무관**
- 배경 상주 26 + 12 = 38 < 40 ✅

### 단점

**1) 매번 약간의 지연**

`replace()`는 새 음원을 `prepare()` 합니다. 로컬 파일이라 짧지만 0은 아닙니다.

> 방향 1: 대부분 지연 0, **가끔 크게** 걸림
> 방향 2: **항상 조금씩** 걸림

체감이 다릅니다. 방향 2가 더 고르고, 방향 1이 최고점은 더 좋습니다.

**2) 동시 발음이 12개로 제한**

건반 12개를 동시에 누를 일은 없으니 실질 제약은 아닙니다.
다만 피아노는 음이 길어서(감쇠 3초 이상) **빠르게 연타하면 아직 울리는 스피커를 재활용**하게 됩니다.
→ 앞 음이 끊깁니다. 12개면 3초 안에 12타를 쳐야 발생하므로 현실적으로 드뭅니다.

**3) 작업량이 큽니다**

`soundCache` 구조를 걷어내고 풀을 새로 짭니다. 피아노·기타 양쪽 + 낙하노트 모드까지 확인 필요.

**4) `replace()` 동작 확인이 필요합니다**

`replace()`는 `COMMAND_CHANGE_MEDIA_ITEMS` 가능할 때만 동작하고, 재생 중이면 이어서 재생합니다
(`AudioModule.kt:488-506`). **연타 시 의도대로 되는지 실기기 확인이 필요합니다.**

### 평가

**구조적으로 옳은 해법입니다.** 게임 오디오 엔진이 쓰는 표준 방식(보이스 풀)이고,
"음원 개수"와 "동시 재생 개수"를 분리합니다.

---

## 8. 방향 3 — 둘의 실제 중간

> **풀 12개를 두되, 각 스피커가 마지막에 물었던 음을 기억한다.**

```
건반 누름
  ├─ 그 음을 이미 물고 있는 스피커 있음 → replace 없이 seekTo(0) + play()   ← 지연 0
  └─ 없음 → 가장 오래된 스피커에 replace()                                  ← 지연 있음
```

방향 2의 풀 위에 방향 1의 LRU를 얹은 것입니다.

- 최근 12음은 **지연 0** (방향 1의 장점)
- 개수는 **12 고정** (방향 2의 장점)
- 작업량은 방향 2와 비슷

**두 방향의 장점을 실제로 다 가집니다.** 방향 2를 하기로 했다면 사실상 추가 비용 없이 여기까지 갈 수 있습니다.

---

## 9. 비교

| | 방향 1 (LRU) | 방향 2 (풀+replace) | 방향 3 (중간) |
|---|---|---|---|
| 동시 플레이어 | N개 (≈14 이하여야) | **12 고정** | **12 고정** |
| 재생 지연 | 대부분 0, 가끔 큼 | 항상 조금 | **대부분 0** |
| 화면 27건반 커버 | ❌ (예산 부족) | ✅ | ✅ |
| 탭 재진입 지연 | 있음 (캐시 비움) | **없음** | **없음** |
| 다른 탭 영향 | ❌ 받음 | ✅ 안 받음 | ✅ 안 받음 |
| "캐시 안 비움" 규칙 | ❌ 깨야 함 | ✅ 유지 | ✅ 유지 |
| 폴리포니 | 무제한 | 12 | 12 |
| 작업량 | **작음** | 큼 | 큼 |
| 되돌리기 쉬움 | ✅ | ❌ | ❌ |

### 추천

**방향 3.** 방향 1은 예산이 14뿐이라 없앴던 지연 문제가 그대로 돌아옵니다.
방향 2를 할 거면 방향 3이 추가 비용 없이 더 낫습니다.

**빠르게 막는 게 우선이면** 방향 1을 임시로 넣고(피아노 N=12, 기타 N=12, 블러 시 비움)
나중에 방향 3으로 가는 것도 됩니다. 무음은 즉시 사라지고, 지연은 남습니다.

---

## 10. 어느 방향이든 같이 해야 할 것

### ① 재생 에러를 JS에서 볼 수 있게 하기

지금은 스피커가 죽어도 **아무 흔적이 없습니다.** 이번에 3세션을 헤맨 직접적 이유입니다.

```ts
player.addListener('playbackStatusUpdate', (status) => {
  if (status.error) console.warn(`[audio] ${note} 재생 에러:`, status.error);
});
```

**이것만 있었으면 첫 턴에 끝났습니다.**

### ② 동물게임 12개를 앱 수명에서 떼어내기

`services/GameAudioManager.ts:21-32`의 싱글턴은 한 번 로드되면 **앱 종료까지** 12개를 붙잡습니다.
게임 화면을 나갈 때 해제하면 예산이 12개 돌아옵니다.

### ③ 디버그 로그 정리

`MusicTrainingScreen.tsx:856,859`의 `[Touch debug]` 는 이번 진단에 도움이 됐지만
건반을 칠 때마다 2줄씩 남습니다. 원인 확정 후 제거 대상입니다.

### ④ 실기기 확인 방법을 기록으로 남기기

```bash
adb logcat -d | grep -E "no more tracks|Cannot create AudioTrack|ExoPlaybackException"
```

**오디오 이상은 이 명령부터 칩니다.** 코드 읽기로는 안 나옵니다.

---

## 11. 이전 결론 중 정정할 것

| 이전 기록 | 정정 |
|---|---|
| `handoff.md:902` "기타 `CACHE_LIMIT = 15`는 실측값이 아니다" → 삭제 | 실측값은 아니었지만 **한도는 실재**. 삭제가 아니라 측정이 필요했음 |
| `handoff.md:909-911` "85개 남아도 메모리 문제 없음" | 메모리는 맞음. **개수 한도에서 실패** |
| 세션 15 "`currentTime > 0` 조건 때문에 무음" | ❌ 무관 |
| 세션 15 "블러가 미로드 플레이어를 pause해서 무음" | ❌ 무관 |
| 이 대화 "캐시 비우면 기타 28은 그대로 둬도 된다" | ❌ **배경 상주 26개 때문에 28도 안 들어감** (4장) |

---

## 12. 참고 위치

**앱 코드**

- `screens/MusicTrainingScreen.tsx:582-593` — 피아노 52개 프리로드
- `screens/MusicTrainingScreen.tsx:673-688` — `playSound`
- `app/(tabs)/guitar/_layout.tsx:106-117` — 기타 28개 프리로드
- `services/GameAudioManager.ts:21-32` — 동물 12개 싱글턴
- `hooks/useWordAudioPlayer.ts:18` — 단어 LRU 8
- `context/AudioManager.tsx:90-100` — 드럼 5개, `stopAllSounds`는 `pause`만
- `hooks/useStopAudioOnBlur.ts` — 블러 시 정지 (해제 아님)

**expo-audio 네이티브** (`node_modules/expo-audio/android/src/main/java/expo/modules/audio/`)

- `AudioPlayer.kt:36-56` — ExoPlayer 생성
- `AudioPlayer.kt:93-98` — `setMediaSource` → `prepare()` (여기서 AudioTrack 할당)
- `AudioPlayer.kt:158-164` — `onPlayerError` → 이벤트로만 전달
- `AudioModule.kt:467-481` — `play()`, 예외 안 던짐
- `AudioModule.kt:488-506` — `replace()` (방향 2의 핵심)
- `BaseAudioPlayer.kt:52-69` — 상태 갱신 코루틴
- `BaseAudioPlayer.kt:153-166` — 해제 경로
