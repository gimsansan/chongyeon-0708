# 작업 규칙

## 규칙 1 — "이해?"는 실행 금지 신호

사용자 메시지가 **"이해?"** (또는 "이해했어?", "알겠어?" 등)로 끝나면,
**파일 수정·명령 실행을 절대 하지 않는다.**

1. 읽기 전용 탐색(Read / Grep / Glob)까지만 한다.
2. 내가 이해한 **대상·범위·방식**을 한 문단으로 정리해 되묻는다.
3. 사용자가 확인해준 **뒤에** 실행을 시작한다.

"이해?"는 작업 지시가 아니라 **이해 확인 요청**이다.

### 이 규칙이 생긴 실수

**실수 1 — 요구사항 반대 해석**
"이해?"가 붙은 메시지를 작업 지시로 받아들이고 곧장 수정에 들어갔다.
요구사항을 반대로 해석해서 엉뚱한 대상(선택지 UI)을 고쳤다.
먼저 물었다면 대상이 틀렸다는 걸 수정 전에 알 수 있었다.

**실수 2 — 저장을 준수로 착각**
위 실수 후 이 규칙을 메모리에 저장했다. 그런데 **저장한 바로 그 턴에 다시 실행해버렸다.**
"규칙을 기록했다"는 행위를 "지시를 이행했다"로 착각한 것이다.

> **저장은 준수가 아니다.** 규칙을 적어두는 것과 그 규칙대로 행동하는 것은 별개다.
> 규칙을 기록한 직후일수록 같은 턴에서 그 규칙이 적용되는지 먼저 확인한다.

---

## 규칙 2 — 대화 기록과 인계문

### 2-1. 새 세션 = 새 대화 파일

**세션이 새로 시작되면 대화 기록을 담을 md 파일을 새로 만든다.**
기존 파일에 이어 쓰지 않는다. 파일은 `doc/`에 두고 번호를 올린다.

```
doc/conversation-2026-08-14.md   ← 세션 1
doc/conversation_2.md            ← 세션 2
doc/conversation_3.md            ← 세션 3
...
```

기록 방식은 **터미널에서 오간 말을 그대로** 옮긴다. 요약하거나 다듬지 않는다.
사용자 발언은 입력한 문장 그대로 인용하고, 답변도 출력한 텍스트 그대로 옮긴다.
도구 호출(파일 읽기·검색·타입체크)과 그 결과는 포함하지 않는다.
형식은 `## N. 사용자` / `## N. Claude`, 번호는 파일마다 1번부터.

### 2-2. "인계문 작성해" → **현재 인계문**에 누적

사용자가 **"인계문 작성해"**라고 하면 **현재 인계문**에 적는다.
지금은 `doc/handoff8.md`(세션 51~). 구 파일 `doc/handoff7.md`는 세션 49~50 아카이브,
`doc/handoff6.md`는 세션 47~48, `doc/handoff5.md`는 세션 43~46,
`doc/handoff4.md`는 세션 38~42, `doc/handoff3.md`는 세션 34~37,
`doc/handoff2.md`는 세션 28~33, `doc/handoff1.md`는 세션 20~27,
`doc/handoff.md`는 세션 1~9 아카이브,
`doc/handoff_3.md`는 `3-branch`에서 가져온 기록이며
**아홉 다 여기에 이어 쓰지 않는다.**

**누적 방식이다. 기존 내용을 지우거나 덮어쓰지 않는다.**
진행 현황 표를 갱신하고, 작업 로그에 새 항목을 아래로 쌓는다.
주의사항도 지우지 않고 쌓되, 해소된 것은 취소선으로 표시한다.

### 2-3. 새 세션에서 "이어서" → 문맥부터 확인

새 창에서 사용자가 인계문을 언급하며 **"이어서"**라고 하면,
**곧바로 코드를 고치지 않는다.**

1. **현재 인계문**(`doc/handoff8.md`)을 읽는다. 구 `doc/handoff7.md` / `doc/handoff6.md` / `doc/handoff5.md` / `doc/handoff4.md` / `doc/handoff3.md` / `doc/handoff2.md` / `doc/handoff1.md` / `doc/handoff.md` / `doc/handoff_3.md`는 이 명령만으로 읽지 않는다.
2. 거기 적힌 "다음 작업"과 "주의사항 / 확인된 의도 동작"을 확인한다.
3. 관련 코드를 읽어 현재 상태가 인계문과 일치하는지 확인한다.
4. 무엇을 할 것인지 정리해 알린 뒤 시작한다.

인계문은 **작업 지시서가 아니라 상태 보고서**다. 그대로 실행하는 것이 아니라,
지금도 유효한지 확인하고 시작한다.

머리만 읽고 시작하려면 **"인계문 머리 읽어"** 를 쓴다 (2-4).

### 2-4. "인계문 머리 읽어" → 머리만, 해당 항목만

사용자가 **"인계문 머리 읽어"** 라고 하면 **현재 인계문**(`doc/handoff8.md`) **전체를 읽지 않는다.**
구 `doc/handoff7.md` / `doc/handoff6.md` / `doc/handoff5.md` / `doc/handoff4.md` / `doc/handoff3.md` / `doc/handoff2.md` / `doc/handoff1.md` / `doc/handoff.md` / `doc/handoff_3.md`는 읽지 않는다.

읽는 범위 (머리):
- 문서 맨 위 현황 문단
- **진행 현황** 표
- 가장 최근 **남은 일** (취소선이 아닌 것)

하지 않는 것:
- 세션 작업 로그 전부
- 주의사항 전부
- 남은 일이 없는데 코드를 전수 대조
- 이미 답이 있는 확인사항을 다시 묻기

이을 항목이 **있을 때만** 그 항목의 작업 로그·관련 코드를 읽는다.
남은 일이 없으면 머리 요약만 하고 멈춘다.

---

## 규칙 3 — 반응형 디자인을 고려한 구현

UI·레이아웃을 구현하거나 고칠 때 **반응형 디자인**을 전제로 한다.
한 기기 크기만 보고 고정값으로 맞추지 않는다.

잊지 말 것:
- **태블릿**
- **작은 스마트폰**

둘 다에서 깨지지 않게 간격·폰트·영역 비율을 잡는다.

---

## 규칙 4 — 안드로이드 전용 앱이다

이 앱은 **안드로이드 모바일 앱**이다. **iOS 대응은 하지 않는다.**

iOS만을 위한 코드·스타일을 **새로 넣지 않고, 보이면 지운다.**

- **그림자는 `elevation`으로만 낸다.**
  `shadowColor` · `shadowOffset` · `shadowOpacity` · `shadowRadius`는 리액트 네이티브 타입에서
  `ShadowStyleIOS`로 묶인 **iOS 전용** 속성이다. 안드로이드에서는 아무 일도 하지 않는다.
- `Platform.OS === 'ios'` 분기, iOS 전용 API·설정도 넣지 않는다.
- **"iOS에서도 보이게"는 근거가 되지 않는다.** 안드로이드에서 확인되는 것만 남긴다.
  그림자가 부족하면 `elevation` 값이나 테두리·배경 대비로 푼다.

### 이 규칙이 생긴 실수

세션 34에서 결과 화면을 고치며 **"`elevation`은 안드로이드 전용이라 iOS엔 그림자가 없다"**는
이유로 `shadow*` 네 줄을 **새로 넣었다.** 이 앱에는 iOS가 없으므로 그냥 죽은 코드였다.
지시를 받고 저장소 전체에서 95줄(10파일)을 지웠다.

> **플랫폼 대응을 근거로 코드를 늘리기 전에 그 플랫폼이 이 앱의 대상인지 먼저 볼 것.**

---

## 규칙 5 — 화면을 만지면 그 화면 전체를 훑는다

화면·컴포넌트를 고쳤으면 **지시받은 자리에서 멈추지 않는다.**
그 파일을 [`doc/점검표.md`](./doc/점검표.md)로 훑고, 나온 것을 **목록으로 보고**한다.

1. 점검표 **0절(오검 목록)과 0-2절(보류)을 먼저 읽는다.** 양쪽 다 지적하지 않는다 —
   0절은 손으로 맞춘 값·「그대로 둔다」고 정한 것, 0-2절은 보고했으나 아직 안 고른 것이다.
2. **발견한 것을 고치지 않는다.** 지시 밖의 수정은 사용자가 고른 것만 한다.
   보고는 `.claude/commands/점검.md`의 **「보고 형식」과 같다** —
   **표 하나 · 갈래 칸(🔧 코드 / 📱 기기 / ❓ 확인) · 설명 문단 없이 번호만 고른다.**
3. 훑고 나면 [`doc/점검-이력.md`](./doc/점검-이력.md)의 그 줄을 갱신한다 —
   **「점검 커밋」을 반드시 적는다.** 이게 있어야 다음에
   `git diff --name-only <점검 커밋> HEAD -- <파일>`로 **바뀐 것만** 다시 훑는다.
   빼먹으면 그 줄은 `?`가 되고 **중복 점검을 막을 근거가 사라진다.**
4. 고르지 않은 발견을 **셋으로 나눈다** — 고침 / **0-2절(보류)** / **0절(그대로)**.
   0-2절에는 **「왜 보류」**(🔧 안 골랐다 · 📱 기기 대기)를 함께 적는다.
   안 올리면 다음 점검이 같은 것을 또 보고하고, 사용자가 매번 다시 판단해야 한다.
   **이미 0-2절에 있는 것은 다시 묻지 않는다** — 묻는 것은 그 파일에서 새로 나온 것뿐이다.
5. **고친 뒤에는 한 번 더 훑는다 — 딱 한 번이다.** 큰 것을 걷어내면 그 아래가 보인다.
   **재점검에서 나온 것은 고치지 않고 0-2절(보류)로 보낸다.** 안 그러면
   고침 → 점검 → 또 나옴 → 고침이 **끝나지 않는다.** 다시 고칠지는 다음 차례에 사용자가 고른다.
6. 사용자가 **`/점검 <경로>`**로 직접 부를 수도 있다 (`.claude/commands/점검.md`).

### 규칙 1과의 관계 — **「이해?」는 예외다. 규칙 5가 돌지 않는다**

「이해?」가 붙은 메시지에서는 **점검하지 않는다.** 보고도, 이력 기록도 없다.
규칙 1대로 **읽고 되묻는 것까지**만 한다.

**충돌이 아니라 애초에 겹치지 않는다.** 규칙 5의 방아쇠는 「**화면을 고쳤으면**」인데
「이해?」에서는 아무것도 안 고치므로 **방아쇠가 당겨지지 않는다.**

> 세션 41에 「점검은 읽기 전용이라 「이해?」에서도 할 수 있다」고 적었다가 **사용자 지적으로 되돌렸다.**
> 그 문장은 (a) 3번(이력 갱신)·4번(보류 등재)이 **파일 쓰기라 규칙 1과 부딪히고**,
> (b) **짧게 확인받고 싶은 자리에 긴 점검 보고를 끼워 넣어** 되묻는 문단을 묻는다.
> **「이해?」에 답할 것은 이해뿐이다.**
>
> `/점검`은 **사용자가 직접 부르는 것**이라 이 예외와 무관하다.

### 이 규칙이 생긴 이유

세션 39에서 사용자가 「익힘배지 모달창」을 언급해서 그 자리를 봤더니, 지시받은 UI 말고도
**`Modal`이 아니라 뒤로가기가 안 되는 것 · `flexShrink: 0`이라 스크롤이 막힌 것 ·
바닥 인셋 누락**이 함께 나왔다.

> 즉 **문제가 없어서 조용했던 게 아니라, 사용자가 그 자리를 말했기 때문에 발견된 것이다.**
> 말하지 않은 화면은 같은 문제가 있어도 그대로 남는다.
> 사용자가 탭마다 점검을 요청할 수는 없다 — **어디를 봐야 하는지는 내가 알아야 한다.**

세션 41에 UI 파일 42개를 전수로 재 보니 **터치 166개 중 `accessibilityRole`이 18개**이고
**그중 15개가 훑은 열 파일에 있다.** 나머지 32개 파일(7,702줄)은 **한 번도 점검한 적이 없다.**

> **수치는 여기 적지 않는다** — 낡는다. 실측과 화면 묶음은
> [`doc/점검-이력.md`](./doc/점검-이력.md)가 갖는다.

---

## 규칙 6 — 문장 끝에 "간" 을 붙이면 간단히 대답을 해주면 된다.
