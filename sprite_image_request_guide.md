# 4프레임 캐릭터 스프라이트 이미지 요청문

아래 내용을 이미지 생성 요청문에 함께 전달하면 됩니다.

## 요청문

모바일 앱에서 사용할 작은 픽셀 캐릭터용 4프레임 스프라이트 이미지를 만들어 주세요.

- 이미지는 가로로 4개의 프레임이 나란히 배치되어야 합니다.
- 전체 구조는 `[1프레임][2프레임][3프레임][4프레임]` 형태여야 합니다.
- 각 프레임의 크기는 동일해야 합니다.
- 전체 이미지 비율은 4:1에 가깝게 만들어 주세요.
- 추천 크기는 `512x128` 또는 `1024x256`입니다.
- 각 캐릭터는 자기 프레임 칸 안에 완전히 들어가야 합니다.
- 캐릭터의 머리, 발, 손, 무기, 그림자가 프레임 경계를 넘어가면 안 됩니다.
- 캐릭터와 배경 색은 명확히 구분되게 해 주세요.
- 배경은 단순한 단색, 밝은 체커보드, 또는 캐릭터와 쉽게 구분되는 배경이 좋습니다.
- 복잡한 배경 장식, UI, 나무, 돌, 강한 그림자, 반짝임 효과는 넣지 말아 주세요.
- 캐릭터는 작은 픽셀 아트 스타일로 만들어 주세요.
- 최종 사용 시 한 프레임은 `64x64` 슬롯에 들어갈 예정입니다.

## 점프 4프레임 기준

점프 애니메이션이라면 프레임 구성은 다음처럼 만들어 주세요.

- 1프레임: 서 있거나 점프 준비 자세
- 2프레임: 점프 시작 또는 상승 자세
- 3프레임: 점프 최고점 자세
- 4프레임: 착지 또는 기본 자세로 복귀

정렬 기준:

- 1프레임과 4프레임은 발의 기준선이 같아야 합니다.
- 2프레임은 발이 살짝 올라가도 괜찮습니다.
- 3프레임은 점프 최고점이므로 캐릭터 전체가 위쪽에 있어야 합니다.
- 3프레임의 y축 위치가 다른 것은 정상입니다.
- 점프가 아닌데 프레임마다 몸 전체 위치가 흔들리는 것은 피해야 합니다.

## 걷기 4프레임 기준

걷기 애니메이션이라면 다음을 지켜 주세요.

- 4개 프레임 모두 발의 기준선이 거의 같아야 합니다.
- 캐릭터 몸통 중심이 좌우로 심하게 흔들리면 안 됩니다.
- 팔과 다리만 자연스럽게 움직이고, 머리와 몸통 위치는 안정적으로 유지해 주세요.
- 한 프레임에서만 캐릭터가 갑자기 위로 뜨거나 아래로 내려가면 안 됩니다.

## 품질 주의사항

- 모바일에서 캐릭터가 `64x64` 이하로 보이면 4프레임도 자연스럽습니다.
- 모바일에서 캐릭터가 `96x96` 이상으로 보이면 4프레임은 끊겨 보일 수 있으므로 실제 기기 확인이 필요합니다.
- `128x128` 이상으로 크게 보이는 캐릭터라면 6~8프레임 이상이 더 좋습니다.
- 4프레임은 용량과 성능에는 유리하지만, 큰 캐릭터에서는 움직임이 딱딱해 보일 수 있습니다.
- 프레임 수보다 중요한 것은 기준점 정렬입니다.
- 걷기는 발 baseline 통일, 대기는 몸 중심 통일, 점프는 의도된 y축 변화가 중요합니다.

## 자동 분리 코드에 맞는 조건

`auto_detect_sprite.py`로 자동 분리하려면 아래 조건을 만족해야 합니다.

- 이미지 안에 캐릭터가 정확히 4개 있어야 합니다.
- 캐릭터 4개는 가로 방향으로 배치되어야 합니다.
- 전체 이미지 너비는 4로 나누어떨어져야 합니다.
- 각 프레임 칸의 너비는 같아야 합니다.
- 각 캐릭터는 자기 칸 안에 있어야 합니다.
- 캐릭터와 배경의 색 차이가 충분해야 합니다.
- 가능하면 캐릭터 없는 배경 이미지를 별도로 준비하면 분리 정확도가 올라갑니다.

## 피해야 할 이미지

- 캐릭터가 4개보다 많거나 적은 이미지
- 캐릭터가 세로 또는 대각선으로 배치된 이미지
- 프레임 크기가 서로 다른 이미지
- 캐릭터가 프레임 경계를 넘어간 이미지
- 배경과 캐릭터 색이 너무 비슷한 이미지
- 배경에 복잡한 장식이 많은 이미지
- 캐릭터마다 크기나 카메라 위치가 크게 다른 이미지
- 걷기 동작인데 프레임마다 캐릭터 전체가 위아래로 튀는 이미지

## 최종 출력 목표

자동화 코드의 최종 결과는 아래 형태입니다.

```text
64x64 + 64x64 + 64x64 + 64x64 = 256x64
```

즉, 최종 결과물은 가로 `256px`, 세로 `64px` 크기의 투명 PNG 스프라이트 시트입니다.
